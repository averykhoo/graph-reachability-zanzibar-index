# Lake build outputs and fetched dependencies (mathlib clone, oleans, native libs).
# Everything here is regenerated by `lake exe cache get` + `lake build`.
/.lake/

# lake-manifest.json IS committed (pins dependency revisions) — do not ignore it.
