# formal/headline_definitions.txt -- the DEFINITION pin: what the headline
# theorems' WORDS MEAN.
#
# The statement pin (formal/headline_statements.txt) records `(hF : W4Fragment S T)`
# BY NAME.  Delete the `twoStrata` field from `structure W4Fragment` and
# `graph_correct` claims something strictly weaker over a strictly larger class of
# schemas -- while its pinned statement line stays byte-identical and no
# declaration name changes, so neither the statement pin nor the audit identity
# pin fires.  This file closes that: it pins the full text of every project
# declaration the 26 headline statements depend on, TRANSITIVELY, plus the ambient
# `variable` / `open` context of the files that host them.
#
# Rows:
#   def:<name>       [<kind>] fields=(<field/ctor names>) <normalized declaration text>
#   ambient:<path>   the file's `variable` / `open` lines, in order
#
# The closure is unbounded WITHIN THE PROJECT and stops at the project boundary
# (Mathlib/core names resolve to nothing).  Measured 2026-07-27: it converges at
# depth 9 to 132 declarations.  Depth, churn measurements and -- importantly --
# what this pin still CANNOT see are documented in
# formal/conformance/statement_pin.py's module docstring.  Read that before
# trusting this file for more than it claims.
#
# REGENERATE DELIBERATELY (a diff here is a change to what the development's
# claims MEAN -- it belongs in a commit message and in formal/history/):
#
#     ZANZIBAR_PY=/path/to/python
#     "$ZANZIBAR_PY" formal/conformance/statement_pin.py --generate
#
ambient:formal/lean/ZanzibarProofs/GraphIndex/CascadeEnum.lean	open GraphModel
ambient:formal/lean/ZanzibarProofs/GraphIndex/CascadeStrata.lean	open scoped List
ambient:formal/lean/ZanzibarProofs/GraphIndex/CascadeStrataAssemble.lean	open GraphModel
ambient:formal/lean/ZanzibarProofs/GraphIndex/CascadeStrataEnum.lean	open GraphModel
ambient:formal/lean/ZanzibarProofs/GraphIndex/Closure.lean	variable {V : Type} [DecidableEq V] [Fintype V]
ambient:formal/lean/ZanzibarProofs/SetEngine/Correct.lean	open MemberSet SetEngineModel ; variable {s : SubjectRef}
ambient:formal/lean/ZanzibarProofs/SetEngine/Eval.lean	open MemberSet
def:Zanzibar.Acyclic	[def] def Acyclic (g : DirectGraph V) : Prop := ∀ v, pathCount g v v = 0
def:Zanzibar.AllValid	[def] def AllValid (T : Store) : Prop := ∀ tup ∈ T, ValidIdent tup.subject.type ∧ ValidIdent tup.relation ∧ ValidIdent tup.object.type
def:Zanzibar.BARE	[def] def BARE : String := "..."
def:Zanzibar.BareStarStore	[def] def BareStarStore (T : Store) : Prop := ∀ t ∈ T, (t.subject.name = STAR → t.subject.predicate = BARE) ∧ t.object.name ≠ STAR
def:Zanzibar.CompleteKey	[def] def CompleteKey (S : Schema) (T : Store) (σ : GraphState) (dt on R : String) : Prop := (∀ sh ∈ wildcardShapes S, sem S T ⟨starSubj sh, R, ⟨dt, on⟩⟩ = true → (σ.residue (objNode ⟨dt, on⟩ R) R).isSome = true) ∧ (∀ s : SubjectRef, s.predicate = BARE → s.name ≠ STAR → sem S T ⟨s, R, ⟨dt, on⟩⟩ = true → ¬(s.shape ∈ wildcardShapes S ∧ sem S T ⟨starSubj s.shape, R, ⟨dt, on⟩⟩ = true) → (subjNode s, objNode ⟨dt, on⟩ R) ∈ σ.edges) ∧ (∀ s : SubjectRef, s.predicate ≠ BARE → s.name ≠ STAR → sem S T ⟨s, R, ⟨dt, on⟩⟩ = true → ∃ res, σ.residue (objNode ⟨dt, on⟩ R) R = some res ∧ s ∈ res.upos) ∧ (∀ s : SubjectRef, s.name ≠ STAR → s.shape ∈ wildcardShapes S → sem S T ⟨starSubj s.shape, R, ⟨dt, on⟩⟩ = true → sem S T ⟨s, R, ⟨dt, on⟩⟩ = false → ∃ res, σ.residue (objNode ⟨dt, on⟩ R) R = some res ∧ s ∈ res.neg)
def:Zanzibar.ComputedOnly	[def] def ComputedOnly : Expr → Prop | .computed _ => True | .union a b => ComputedOnly a ∧ ComputedOnly b | .inter a b => ComputedOnly a ∧ ComputedOnly b | .excl a b => ComputedOnly a ∧ ComputedOnly b | .direct _ => False | .ttu _ _ => False
def:Zanzibar.ComputedOrDirect	[def] def ComputedOrDirect : Expr → Prop | .computed _ => True | .direct _ => True | .union a b => ComputedOrDirect a ∧ ComputedOrDirect b | .inter a b => ComputedOrDirect a ∧ ComputedOrDirect b | .excl a b => ComputedOrDirect a ∧ ComputedOrDirect b | .ttu _ _ => False
def:Zanzibar.Delta	[structure] fields=(id node relation leaf) structure Delta where id : Nat node : NodeKey relation : String leaf : Bool := false deriving DecidableEq, Repr, Inhabited
def:Zanzibar.DirectArmsBare	[def] def DirectArmsBare : Expr → Prop | .computed _ => True | .direct rs => ∀ r ∈ rs, r.2.1 = BARE | .union a b => DirectArmsBare a ∧ DirectArmsBare b | .inter a b => DirectArmsBare a ∧ DirectArmsBare b | .excl a b => DirectArmsBare a ∧ DirectArmsBare b | .ttu _ _ => True
def:Zanzibar.DirectArmsConcrete	[def] def DirectArmsConcrete (S : Schema) : Prop := ∀ dt R e, S.lookup (dt, R) = some e → isDerived S (dt, R) = true → ∀ rs ∈ exprDirectsAll e, ∀ r ∈ rs, r.2.2 = false
def:Zanzibar.DirectGraph	[structure] fields=(dcount) structure DirectGraph (V : Type) where dcount : V → V → Nat
def:Zanzibar.Drained	[abbrev] abbrev Drained (S : Schema) (σ : GraphState) : Prop := cascadeKeys S σ = []
def:Zanzibar.Expr	[inductive] fields=(direct computed ttu union inter excl) inductive Expr where | direct : List Restriction → Expr | computed : String → Expr | ttu : (targetRel : String) → (tuplesetRel : String) → Expr | union : Expr → Expr → Expr | inter : Expr → Expr → Expr | excl : (base : Expr) → (subtract : Expr) → Expr deriving Repr, DecidableEq, Inhabited
def:Zanzibar.FoldAdmits	[def] def FoldAdmits : GraphState → List Tuple → Prop | _, [] => True | σ, u :: rest => σ.admitEdge (subjNode u.subject) (objNode u.object u.relation) = true ∧ FoldAdmits (σ.writeDirect u) rest
def:Zanzibar.GraphAccepts	[def] def GraphAccepts (S : Schema) : Prop := (∀ tr ∈ S.objectWildcards, isDerived S tr = false) ∧ (∀ d ∈ S.defs, ∀ r ∈ exprRestrictions d.2, r.2.2 = true → r.2.1 ≠ BARE → isDerived S (r.1, r.2.1) = false) ∧ (∀ d ∈ S.defs, ∀ tt ∈ exprTtus d.2, isDerived S (d.1.1, tt.2) = false)
def:Zanzibar.GraphAdmission	[structure] fields=(wf nodup strat ttuDirect matchDecl ranked objWild storeValid) structure GraphAdmission (S : Schema) (T : Store) : Prop where wf : WF S nodup : NodupKeys S strat : Stratifiable S ttuDirect : TtuTuplesetsDirect S matchDecl : RewriteMatchDeclared S ranked : RewriteRanked S objWild : ∀ tr ∈ S.objectWildcards, isDerived S tr = false storeValid : StoreValidRulesD S T
def:Zanzibar.GraphModel.check	[def] def check (σ : GraphState) (q : Query) : Bool := if isDerived σ.schema (q.object.type, q.relation) then probeDerived σ q else probeNonDerived σ q
def:Zanzibar.GraphOp	[inductive] fields=(add remove) inductive GraphOp where | add (t : Tuple) | remove (t : Tuple) deriving Repr, Inhabited, DecidableEq
def:Zanzibar.GraphState	[structure] fields=(schema edges nodes residue outbox watermark) structure GraphState where schema : Schema edges : List (NodeKey × NodeKey) nodes : List NodeKey residue : NodeKey → String → Option Residue outbox : List Delta watermark : Nat
def:Zanzibar.HasDerivedCycle	[def] def HasDerivedCycle (S : Schema) : Prop := ∃ k, Reaches (depEdges S) k k
def:Zanzibar.Inv	[structure] fields=(schemaEq nodeEnc edgesClosed acyclic negStarCovered negEdgeFree uposEdgeFree uposNegDisjoint) structure Inv (S : Schema) (σ : GraphState) : Prop where schemaEq : σ.schema = S nodeEnc : ∀ k ∈ σ.nodes, (k.name = STAR ↔ k.variant ≠ Variant.plain) edgesClosed : ∀ e ∈ σ.edges, e.1 ∈ σ.nodes ∧ e.2 ∈ σ.nodes acyclic : ∀ v, ¬ NReaches σ.edges v v negStarCovered : ∀ k r res, σ.residue k r = some res → ∀ n ∈ res.neg, res.stars.contains n.shape = true negEdgeFree : ∀ k r res, σ.residue k r = some res → ∀ n ∈ res.neg, ¬ NReaches σ.edges (subjNode n) k uposEdgeFree : ∀ k r res, σ.residue k r = some res → ∀ n ∈ res.upos, ¬ NReaches σ.edges (subjNode n) k uposNegDisjoint : ∀ k r res, σ.residue k r = some res → ∀ n ∈ res.upos, res.neg.contains n = false
def:Zanzibar.Key	[abbrev] abbrev Key := String × String
def:Zanzibar.NReaches	[inductive] fields=(edge head) inductive NReaches (edges : List (NodeKey × NodeKey)) : NodeKey → NodeKey → Prop where | edge {u v} : (u, v) ∈ edges → NReaches edges u v | head {u w v} : (u, w) ∈ edges → NReaches edges w v → NReaches edges u v
def:Zanzibar.NoStoreSubjectR	[def] def NoStoreSubjectR (T : Store) (R : String) : Prop := ∀ t ∈ T, t.subject.predicate ≠ R
def:Zanzibar.NoTtuTarget	[def] def NoTtuTarget (S : Schema) (R : String) : Prop := ∀ r ∈ schemaRewrites S, ∀ tr, r.kind = RuleKind.ttu tr → tr ≠ R
def:Zanzibar.NodeKey	[structure] fields=(type name pred variant) structure NodeKey where type : String name : String pred : String variant : Variant deriving DecidableEq, Repr, Inhabited
def:Zanzibar.NodupKeys	[def] def NodupKeys (S : Schema) : Prop := (S.defs.map (·.1)).Nodup
def:Zanzibar.ObjectRef	[structure] fields=(type name) structure ObjectRef where type : String name : String deriving DecidableEq, Repr, Inhabited
def:Zanzibar.Query	[structure] fields=(subject relation object) structure Query where subject : SubjectRef relation : String object : ObjectRef deriving DecidableEq, Repr, Inhabited
def:Zanzibar.Quiescent	[def] def Quiescent (σ : GraphState) : Prop := ∀ d ∈ σ.outbox, d.id ≤ σ.watermark
def:Zanzibar.RRule	[structure] fields=(objectType matchRel outRel kind) structure RRule where objectType : String matchRel : String outRel : String kind : RuleKind deriving DecidableEq, Repr, Inhabited
def:Zanzibar.ReachedBy	[abbrev] abbrev ReachedBy : GraphState → Schema → Store → Prop := ReachedByW3d2E
def:Zanzibar.ReachedByW3d2	[inductive] fields=(empty write remove cascade) inductive ReachedByW3d2 : GraphState → Schema → Store → Prop where | empty (S : Schema) : ReachedByW3d2 (emptyState S) S [] | write {σ : GraphState} {S : Schema} {T : Store} (t : Tuple) (hadm : FoldAdmits σ (rewriteClosure S t)) (hprev : ReachedByW3d2 σ S T) : ReachedByW3d2 (σ.writeLoggedRules S t) S (t :: T) | remove {σ : GraphState} {S : Schema} {T : Store} (t : Tuple) (hadm : RemoveAdmits σ T t) (hdrain : cascadeKeys S σ = []) (hSVT : StoreValidRules S T) (hBST : BareStarStore T) (hTST : TtuStarFree S T) (htermT : ∀ dt R, isDerived S (dt, R) = true → NoTtuTarget S R ∧ NoStoreSubjectR T R) (hprev : ReachedByW3d2 σ S T) : ReachedByW3d2 (σ.removeLoggedRules S t) S (T.erase t) | cascade {σ : GraphState} {S : Schema} {T : Store} (jobs1 jobs2 : List W3cJob) (hjv1 : ∀ j ∈ jobs1, W3cJobValid S j) (hjv2 : ∀ j ∈ jobs2, W3cJobValid S j) (hcover1 : ∀ k ∈ cascadeKeysAbove S σ σ.watermark, ∃ j ∈ jobs1, j.key = k) (hscope1 : ∀ j ∈ jobs1, j.key ∈ cascadeKeysAbove S σ σ.watermark) (hcover2 : ∀ k ∈ cascadeKeysAbove S (reconcileJobsLR S T σ jobs1) (σ.frontierMax σ.watermark), ∃ j ∈ jobs2, j.key = k) (hscope2 : ∀ j ∈ jobs2, j.key ∈ cascadeKeysAbove S (reconcileJobsLR S T σ jobs1) (σ.frontierMax σ.watermark)) (hprev : ReachedByW3d2 σ S T) : ReachedByW3d2 (runCascade2 S T σ jobs1 jobs2) S T
def:Zanzibar.ReachedByW3d2C	[inductive] fields=(empty write remove cascade) inductive ReachedByW3d2C : GraphState → Schema → Store → Prop where | empty (S : Schema) : ReachedByW3d2C (emptyState S) S [] | write {σ : GraphState} {S : Schema} {T : Store} (t : Tuple) (hadm : FoldAdmits σ (rewriteClosure S t)) (hprev : ReachedByW3d2C σ S T) : ReachedByW3d2C (σ.writeLoggedRules S t) S (t :: T) | remove {σ : GraphState} {S : Schema} {T : Store} (t : Tuple) (hadm : RemoveAdmits σ T t) (hdrain : cascadeKeys S σ = []) (hSVT : StoreValidRules S T) (hBST : BareStarStore T) (hTST : TtuStarFree S T) (htermT : ∀ dt R, isDerived S (dt, R) = true → NoTtuTarget S R ∧ NoStoreSubjectR T R) (hprev : ReachedByW3d2C σ S T) : ReachedByW3d2C (σ.removeLoggedRules S t) S (T.erase t) | cascade {σ : GraphState} {S : Schema} {T : Store} (jobs1 jobs2 : List W3cJob) (hjv1 : ∀ j ∈ jobs1, W3cJobValid S j) (hjv2 : ∀ j ∈ jobs2, W3cJobValid S j) (hcover1 : ∀ k ∈ cascadeKeysAbove S σ σ.watermark, ∃ j ∈ jobs1, j.key = k) (hscope1 : ∀ j ∈ jobs1, j.key ∈ cascadeKeysAbove S σ σ.watermark) (hcover2 : ∀ k ∈ cascadeKeysAbove S (reconcileJobsLR S T σ jobs1) (σ.frontierMax σ.watermark), ∃ j ∈ jobs2, j.key = k) (hscope2 : ∀ j ∈ jobs2, j.key ∈ cascadeKeysAbove S (reconcileJobsLR S T σ jobs1) (σ.frontierMax σ.watermark)) (hcovg1 : ∀ j ∈ jobs1, W3dJobOpsSettled S T σ j → W3dJobCoverage S T σ j) (hcovg2 : ∀ j ∈ jobs2, W3dJobOpsSettled S T (reconcileJobsLR S T σ jobs1) j → W3dJobCoverage S T (reconcileJobsLR S T σ jobs1) j) (hprev : ReachedByW3d2C σ S T) : ReachedByW3d2C (runCascade2 S T σ jobs1 jobs2) S T
def:Zanzibar.ReachedByW3d2E	[inductive] fields=(empty write remove cascade) inductive ReachedByW3d2E : GraphState → Schema → Store → Prop where | empty (S : Schema) : ReachedByW3d2E (emptyState S) S [] | write {σ : GraphState} {S : Schema} {T : Store} (t : Tuple) (hadm : FoldAdmits σ (rewriteClosure S t)) (hprev : ReachedByW3d2E σ S T) : ReachedByW3d2E (σ.writeLoggedRules S t) S (t :: T) | remove {σ : GraphState} {S : Schema} {T : Store} (t : Tuple) (hadm : RemoveAdmits σ T t) (hdrain : cascadeKeys S σ = []) (hSVT : StoreValidRules S T) (hBST : BareStarStore T) (hTST : TtuStarFree S T) (htermT : ∀ dt R, isDerived S (dt, R) = true → NoTtuTarget S R ∧ NoStoreSubjectR T R) (hprev : ReachedByW3d2E σ S T) : ReachedByW3d2E (σ.removeLoggedRules S t) S (T.erase t) | cascade {σ : GraphState} {S : Schema} {T : Store} (hprev : ReachedByW3d2E σ S T) : ReachedByW3d2E (runCascade2 S T σ (enumJobs2R1 S T σ) (enumJobs2R2 S T σ)) S T
def:Zanzibar.Reaches	[inductive] fields=(step trans) inductive Reaches (edges : List (Key × Key)) : Key → Key → Prop | step {a b} : (a, b) ∈ edges → Reaches edges a b | trans {a b c} : Reaches edges a b → Reaches edges b c → Reaches edges a c
def:Zanzibar.Rec	[abbrev] abbrev Rec := String → String → String → Bool
def:Zanzibar.RemoveAdmits	[def] def RemoveAdmits (_σ : GraphState) (T : Store) (t : Tuple) : Prop := t ∈ T
def:Zanzibar.Residue	[structure] fields=(stars neg upos) structure Residue where stars : List Shape neg : List SubjectRef upos : List SubjectRef deriving DecidableEq, Repr, Inhabited
def:Zanzibar.Residue.empty	[def] def Residue.empty : Residue := ⟨[], [], []⟩
def:Zanzibar.Restriction	[abbrev] abbrev Restriction := String × String × Bool
def:Zanzibar.RewriteMatchDeclared	[def] def RewriteMatchDeclared (S : Schema) : Prop := ∀ r ∈ schemaRewrites S, (r.objectType, r.matchRel) ∈ S.keys ∧ isDerived S (r.objectType, r.matchRel) = false
def:Zanzibar.RewriteRanked	[def] def RewriteRanked (S : Schema) : Prop := ∃ rrank : (String × String) → Nat, (∀ r ∈ schemaRewrites S, rrank (r.objectType, r.matchRel) < rrank (r.objectType, r.outRel)) ∧ (∀ k, rrank k ≤ S.keys.length)
def:Zanzibar.RuleKind	[inductive] fields=(computed ttu) inductive RuleKind where | computed | ttu (tr : String) deriving DecidableEq, Repr, Inhabited
def:Zanzibar.STAR	[def] def STAR : String := "*"
def:Zanzibar.Schema	[structure] fields=(defs objectWildcards) structure Schema where defs : List ((String × String) × Expr) objectWildcards : List (String × String) deriving Repr, Inhabited
def:Zanzibar.SetEngineModel.check	[def] def check (S : Schema) (T : Store) (q : Query) : Bool := containsShape (expandAux S q.subject T q (fuelBound S T) q.object.type q.object.name q.relation) q.subject q.subject.shape
def:Zanzibar.SettledKey	[def] def SettledKey (S : Schema) (T : Store) (σ : GraphState) (dt on R : String) : Prop := (∀ res, σ.residue (objNode ⟨dt, on⟩ R) R = some res → (∀ sh, res.stars.contains sh = true ↔ (sh ∈ wildcardShapes S ∧ sem S T ⟨starSubj sh, R, ⟨dt, on⟩⟩ = true)) ∧ (∀ n ∈ res.neg, n.name ≠ STAR ∧ sem S T ⟨n, R, ⟨dt, on⟩⟩ = false) ∧ (∀ n ∈ res.upos, n.predicate ≠ BARE ∧ n.name ≠ STAR ∧ sem S T ⟨n, R, ⟨dt, on⟩⟩ = true)) ∧ (∀ s : SubjectRef, s.predicate = BARE → s.name ≠ STAR → (subjNode s, objNode ⟨dt, on⟩ R) ∈ σ.edges → sem S T ⟨s, R, ⟨dt, on⟩⟩ = true)
def:Zanzibar.Shape	[abbrev] abbrev Shape := String × String
def:Zanzibar.Store	[abbrev] abbrev Store := List Tuple
def:Zanzibar.StoreDeclared	[def] def StoreDeclared (S : Schema) (T : Store) : Prop := ∀ tup ∈ T, ∃ e, S.lookup (tup.object.type, tup.relation) = some e ∧ tup.subject.type ∈ directTypes e
def:Zanzibar.StoreValidRules	[def] def StoreValidRules (S : Schema) (T : Store) : Prop := ∀ t ∈ T, ∃ e rs, S.lookup (t.object.type, t.relation) = some e ∧ rs ∈ exprDirects e ∧ restrictionMatches rs t = true
def:Zanzibar.StoreValidRulesD	[def] def StoreValidRulesD (S : Schema) (T : Store) : Prop := ∀ t ∈ T, (isDerived S (t.object.type, t.relation) = false ∧ ∃ e rs, S.lookup (t.object.type, t.relation) = some e ∧ rs ∈ exprDirects e ∧ restrictionMatches rs t = true) ∨ (isDerived S (t.object.type, t.relation) = true ∧ t.subject.predicate = BARE ∧ ∃ e rs, S.lookup (t.object.type, t.relation) = some e ∧ rs ∈ exprDirectsAll e ∧ restrictionMatches rs t = true ∧ (∀ r ∈ rs, r.2.1 = BARE))
def:Zanzibar.Stratifiable	[def] def Stratifiable (S : Schema) : Prop := (stratify S).isSome
def:Zanzibar.SubjectRef	[structure] fields=(type name predicate) structure SubjectRef where type : String name : String predicate : String deriving DecidableEq, Repr, Inhabited
def:Zanzibar.TtuStarFree	[def] def TtuStarFree (S : Schema) (T : Store) : Prop := ∀ t ∈ T, t.subject.name = STAR → ∀ a ∈ schemaRewrites S, ∀ tr, a.kind = RuleKind.ttu tr → ¬(t.relation = a.matchRel ∧ t.object.type = a.objectType)
def:Zanzibar.TtuTuplesetsDirect	[def] def TtuTuplesetsDirect (S : Schema) : Prop := ∀ d ∈ S.defs, ∀ tt ∈ exprTtus d.2, ∀ d' ∈ S.defs, d'.1 = (d.1.1, tt.2) → directsOnly d'.2 = true
def:Zanzibar.Tuple	[structure] fields=(subject relation object) structure Tuple where subject : SubjectRef relation : String object : ObjectRef deriving DecidableEq, Repr, Inhabited
def:Zanzibar.Variant	[inductive] fields=(plain wAny wAll) inductive Variant where | plain | wAny | wAll deriving DecidableEq, Repr, Inhabited
def:Zanzibar.W3cJob	[structure] fields=(dt on R e cands negCands uposCands) structure W3cJob where dt : String on : String R : String e : Expr cands : List SubjectRef negCands : List SubjectRef uposCands : List SubjectRef deriving Repr
def:Zanzibar.W3cJob.applyLoggedR	[def] def W3cJob.applyLoggedR (S : Schema) (T : Store) (σ : GraphState) (j : W3cJob) : GraphState := (j.applyDR S T σ).pushDelta (objNode ⟨j.dt, j.on⟩ j.R) j.R
def:Zanzibar.W3cJobValid	[def] def W3cJobValid (S : Schema) (j : W3cJob) : Prop := j.R ≠ BARE ∧ (∀ c ∈ j.cands, c.predicate = BARE) ∧ (∀ c ∈ j.cands, c.name ≠ STAR) ∧ (∀ c ∈ j.negCands, c.name ≠ STAR) ∧ (∀ c ∈ j.uposCands, c.predicate ≠ BARE) ∧ (∀ c ∈ j.uposCands, c.name ≠ STAR) ∧ isDerived S (j.dt, j.R) = true ∧ S.lookup (j.dt, j.R) = some j.e ∧ j.on ≠ STAR
def:Zanzibar.W3dJobCoverage	[def] def W3dJobCoverage (S : Schema) (T : Store) (σ : GraphState) (j : W3cJob) : Prop := (∀ s : SubjectRef, (subjNode s, objNode ⟨j.dt, j.on⟩ j.R) ∈ σ.edges → s ∈ j.cands) ∧ (∀ s : SubjectRef, s.predicate = BARE → s.name ≠ STAR → sem S T ⟨s, j.R, ⟨j.dt, j.on⟩⟩ = true → ¬(s.shape ∈ wildcardShapes S ∧ sem S T ⟨starSubj s.shape, j.R, ⟨j.dt, j.on⟩⟩ = true) → s ∈ j.cands) ∧ (∀ s : SubjectRef, s.name ≠ STAR → s.shape ∈ wildcardShapes S → sem S T ⟨starSubj s.shape, j.R, ⟨j.dt, j.on⟩⟩ = true → sem S T ⟨s, j.R, ⟨j.dt, j.on⟩⟩ = false → s ∈ j.negCands) ∧ (∀ s : SubjectRef, s.predicate ≠ BARE → s.name ≠ STAR → sem S T ⟨s, j.R, ⟨j.dt, j.on⟩⟩ = true → s ∈ j.uposCands)
def:Zanzibar.W3dJobOpsSettled	[def] def W3dJobOpsSettled (S : Schema) (T : Store) (σ : GraphState) (j : W3cJob) : Prop := ∀ r' ∈ computedRefs j.e, isDerived S (j.dt, r') = true → SettledKey S T σ j.dt j.on r' ∧ CompleteKey S T σ j.dt j.on r'
def:Zanzibar.W4Fragment	[structure] fields=(computedOrDirect directArmsBare directArmsConcrete computedOnlyOperands noUnionDirects twoStrata wsBare bareStar ttuStarFree term) structure W4Fragment (S : Schema) (T : Store) : Prop where computedOrDirect : ∀ dt R e, S.lookup (dt, R) = some e → isDerived S (dt, R) = true → ComputedOrDirect e directArmsBare : ∀ dt R e, S.lookup (dt, R) = some e → isDerived S (dt, R) = true → DirectArmsBare e directArmsConcrete : DirectArmsConcrete S computedOnlyOperands : ∀ dt R e, S.lookup (dt, R) = some e → isDerived S (dt, R) = true → ∀ r' ∈ computedRefs e, isDerived S (dt, r') = true → ∀ e', S.lookup (dt, r') = some e' → ComputedOnly e' noUnionDirects : ∀ dt R e, S.lookup (dt, R) = some e → isDerived S (dt, R) = true → exprDirects e = [] twoStrata : ∀ dt R e, S.lookup (dt, R) = some e → isDerived S (dt, R) = true → ∀ r' ∈ computedRefs e, isDerived S (dt, r') = true → ∀ e', S.lookup (dt, r') = some e' → ∀ r'' ∈ computedRefs e', isDerived S (dt, r'') = false wsBare : ∀ sh ∈ wildcardShapes S, sh.2 = BARE bareStar : BareStarStore T ttuStarFree : TtuStarFree S T term : ∀ dt R, isDerived S (dt, R) = true → NoTtuTarget S R ∧ NoStoreSubjectR T R
def:Zanzibar.W4NarrowT2a	[structure] fields=(computedOnly storeValid) structure W4NarrowT2a (S : Schema) (T : Store) : Prop where computedOnly : ∀ dt R e, S.lookup (dt, R) = some e → isDerived S (dt, R) = true → ComputedOnly e storeValid : StoreValidRules S T
def:Zanzibar.WF	[structure] fields=(relNames) structure WF (S : Schema) : Prop where relNames : ∀ p ∈ S.defs, relNameOK p.1.2
def:Zanzibar.affectedKeys	[def] def affectedKeys (S : Schema) (σ : GraphState) (d : Delta) : List (String × String × String) := (if d.leaf = true ∧ d.node.name ≠ STAR ∧ isDerived S (d.node.type, d.node.pred) = true then [(d.node.type, d.node.pred, d.node.name)] else []) ++ (σ.affectedObjects d).flatMap (fun v => if v.name = STAR then [] else S.keys.filterMap (fun k => if k.1 = v.type ∧ isDerived S k = true ∧ ((S.lookup k).map (fun e => (computedRefs e).contains v.pred)).getD false = true then some (k.1, k.2, v.name) else none))
def:Zanzibar.applyRRule	[def] def applyRRule (r : RRule) (t : Tuple) : Option Tuple := if t.relation = r.matchRel ∧ t.object.type = r.objectType then match r.kind with | .computed => some ⟨t.subject, r.outRel, t.object⟩ | .ttu tr => some ⟨⟨t.subject.type, t.subject.name, tr⟩, r.outRel, t.object⟩ else none
def:Zanzibar.bareStarStoreB	[def] def bareStarStoreB (T : Store) : Bool := T.all fun t => (!(t.subject.name == STAR) || t.subject.predicate == BARE) && !(t.object.name == STAR)
def:Zanzibar.baseTaint	[def] def baseTaint (S : Schema) (k : Key) : Bool := match S.lookup k with | some e => containsBool e | none => false
def:Zanzibar.cascadeKeys	[def] def cascadeKeys (S : Schema) (σ : GraphState) : List (String × String × String) := σ.frontierRows.flatMap (affectedKeys S σ)
def:Zanzibar.cascadeKeysAbove	[def] def cascadeKeysAbove (S : Schema) (σ : GraphState) (n : Nat) : List (String × String × String) := (σ.frontierRowsAbove n).flatMap (affectedKeys S σ)
def:Zanzibar.cascadeLeg	[def] def cascadeLeg (S : Schema) (T : Store) (σ : GraphState) : GraphState := runCascade2 S T σ (enumJobs2R1 S T σ) (enumJobs2R2 S T σ)
def:Zanzibar.computedRefs	[def] def computedRefs : Expr → List String | .computed r' => [r'] | .union a b => computedRefs a ++ computedRefs b | .inter a b => computedRefs a ++ computedRefs b | .excl a b => computedRefs a ++ computedRefs b | .direct _ => [] | .ttu _ _ => []
def:Zanzibar.containsBool	[def] def containsBool : Expr → Bool | .inter _ _ => true | .excl _ _ => true | .direct _ => false | .computed _ => false | .ttu _ _ => false | .union a b => containsBool a || containsBool b
def:Zanzibar.depEdges	[def] def depEdges (S : Schema) : List (Key × Key) := (taintedKeys S).flatMap (fun a => (refsOf S a).filterMap (fun b => if (taintedKeys S).contains b then some (a, b) else none))
def:Zanzibar.directLeaf	[def] def directLeaf (rec : Rec) (subject : SubjectRef) (T : Store) (q : Query) (rs : List Restriction) (otype oname : String) (rel : String) : Bool := let grants := grantsOf T rs otype oname rel let s := subject if s.name == STAR then grants.any (fun g => g.subject.name == STAR && g.subject.type == s.type && g.subject.predicate == s.predicate) || memberOfGranted rec T q grants else if s.predicate == BARE then grants.any (fun g => (g.subject.name != STAR && g.subject.predicate == BARE && g.subject.type == s.type && g.subject.name == s.name) || (g.subject.name == STAR && g.subject.predicate == BARE && g.subject.type == s.type)) || memberOfGranted rec T q grants else grants.any (fun g => (g.subject.name != STAR && g.subject.predicate != BARE && g.subject.type == s.type && g.subject.name == s.name && g.subject.predicate == s.predicate) || (g.subject.name == STAR && g.subject.predicate != BARE && g.subject.type == s.type && g.subject.predicate == s.predicate)) || memberOfGranted rec T q grants
def:Zanzibar.directTypes	[def] def directTypes : Expr → List String | .direct rs => rs.map (·.1) | .computed _ => [] | .ttu _ _ => [] | .union a b => directTypes a ++ directTypes b | .inter a b => directTypes a ++ directTypes b | .excl a b => directTypes a ++ directTypes b
def:Zanzibar.directsOnly	[def] def directsOnly : Expr → Bool | .direct _ => true | .union a b => directsOnly a && directsOnly b | .computed _ => false | .ttu _ _ => false | .inter _ _ => false | .excl _ _ => false
def:Zanzibar.drainedB	[def] def drainedB (S : Schema) (σ : GraphState) : Bool := (cascadeKeys S σ).isEmpty
def:Zanzibar.edgeHolders	[def] def edgeHolders (σ : GraphState) (dt on R : String) : List SubjectRef := (σ.edges.filter (fun ed => ed.2 == objNode ⟨dt, on⟩ R)).map (fun ed => nodeSubj ed.1)
def:Zanzibar.emptyState	[def] def emptyState (S : Schema) : GraphState := { schema := S, edges := [], nodes := [], residue := fun _ _ => none, outbox := [], watermark := 0 }
def:Zanzibar.enum2Base	[def] def enum2Base (σ : GraphState) (dt on : String) (e : Expr) : List SubjectRef := leafConcretes σ dt on e ++ (computedRefs e).flatMap (fun r' => residueNamed σ dt on r')
def:Zanzibar.enum2BaseD	[def] def enum2BaseD (σ : GraphState) (T : Store) (dt on R : String) (e : Expr) : List SubjectRef := enum2Base σ dt on e ++ storedDirectSubjects T dt on R e
def:Zanzibar.enumJob2D	[def] def enumJob2D (σ : GraphState) (T : Store) (dt on R : String) (e : Expr) : W3cJob := { dt := dt, on := on, R := R, e := e, cands := (enum2Base σ dt on e).filter (fun u => u.predicate == BARE) ++ freshDirectCands σ T dt on R e ++ edgeHolders σ dt on R, negCands := (enum2BaseD σ T dt on R e).filter (fun u => u.predicate == BARE), uposCands := (enum2BaseD σ T dt on R e).filter (fun u => u.predicate != BARE) }
def:Zanzibar.enumJobs2At	[def] def enumJobs2At (S : Schema) (T : Store) (σe : GraphState) (keys : List (String × String × String)) : List W3cJob := keys.filterMap (fun k => (S.lookup (k.1, k.2.1)).map (fun e => enumJob2D σe T k.1 k.2.2 k.2.1 e))
def:Zanzibar.enumJobs2R1	[def] def enumJobs2R1 (S : Schema) (T : Store) (σ : GraphState) : List W3cJob := enumJobs2At S T σ (cascadeKeysAbove S σ σ.watermark)
def:Zanzibar.enumJobs2R2	[def] def enumJobs2R2 (S : Schema) (T : Store) (σ : GraphState) : List W3cJob := enumJobs2At S T (reconcileJobsLR S T σ (enumJobs2R1 S T σ)) (cascadeKeysAbove S (reconcileJobsLR S T σ (enumJobs2R1 S T σ)) (σ.frontierMax σ.watermark))
def:Zanzibar.evalE	[def] def evalE (rec : Rec) (subject : SubjectRef) (T : Store) (q : Query) (otype oname rel : String) : Expr → Bool | .union a b => evalE rec subject T q otype oname rel a || evalE rec subject T q otype oname rel b | .inter a b => evalE rec subject T q otype oname rel a && evalE rec subject T q otype oname rel b | .excl b s => evalE rec subject T q otype oname rel b && !evalE rec subject T q otype oname rel s | .computed r => rec otype oname r | .direct rs => directLeaf rec subject T q rs otype oname rel | .ttu tr ts => ttuLeaf rec subject T q tr ts otype oname
def:Zanzibar.exprArms	[def] def exprArms (ot outRel : String) : Expr → List RRule | .computed r => [⟨ot, r, outRel, .computed⟩] | .ttu tr ts => [⟨ot, ts, outRel, .ttu tr⟩] | .union a b => exprArms ot outRel a ++ exprArms ot outRel b | .direct _ => [] | .inter _ _ => [] | .excl _ _ => []
def:Zanzibar.exprDirects	[def] def exprDirects : Expr → List (List Restriction) | .direct rs => [rs] | .union a b => exprDirects a ++ exprDirects b | .computed _ => [] | .ttu _ _ => [] | .inter _ _ => [] | .excl _ _ => []
def:Zanzibar.exprDirectsAll	[def] def exprDirectsAll : Expr → List (List Restriction) | .direct rs => [rs] | .computed _ => [] | .ttu _ _ => [] | .union a b => exprDirectsAll a ++ exprDirectsAll b | .inter a b => exprDirectsAll a ++ exprDirectsAll b | .excl a b => exprDirectsAll a ++ exprDirectsAll b
def:Zanzibar.exprRefs	[def] def exprRefs (S : Schema) (t : String) : Expr → List Key | .direct rs => rs.filterMap (fun r => if r.2.1 = BARE then none else some (r.1, r.2.1)) | .computed r => [(t, r)] | .ttu tr ts => (t, ts) :: (match S.lookup (t, ts) with | some e => (directTypes e).map (fun pt => (pt, tr)) | none => []) | .union a b => exprRefs S t a ++ exprRefs S t b | .inter a b => exprRefs S t a ++ exprRefs S t b | .excl a b => exprRefs S t a ++ exprRefs S t b
def:Zanzibar.exprRestrictions	[def] def exprRestrictions : Expr → List Restriction | .direct rs => rs | .computed _ => [] | .ttu _ _ => [] | .union a b => exprRestrictions a ++ exprRestrictions b | .inter a b => exprRestrictions a ++ exprRestrictions b | .excl a b => exprRestrictions a ++ exprRestrictions b
def:Zanzibar.exprTtus	[def] def exprTtus : Expr → List (String × String) | .direct _ => [] | .computed _ => [] | .ttu tr ts => [(tr, ts)] | .union a b => exprTtus a ++ exprTtus b | .inter a b => exprTtus a ++ exprTtus b | .excl a b => exprTtus a ++ exprTtus b
def:Zanzibar.foldAdmitsB	[def] def foldAdmitsB : GraphState → List Tuple → Bool | _, [] => true | σ, u :: rest => σ.admitEdge (subjNode u.subject) (objNode u.object u.relation) && foldAdmitsB (σ.writeDirect u) rest
def:Zanzibar.freshDirectCands	[def] def freshDirectCands (σ : GraphState) (T : Store) (dt on R : String) (e : Expr) : List SubjectRef := (storedDirectSubjects T dt on R e).filter (fun u => decide (u.predicate = BARE ∧ u ∉ enum2Base σ dt on e ∧ u ∉ edgeHolders σ dt on R))
def:Zanzibar.fuelBound	[def] def fuelBound (S : Schema) (T : Store) : Nat := S.keys.length * (T.length * 2 + 4)
def:Zanzibar.grantsOf	[def] def grantsOf (T : Store) (rs : List Restriction) (otype oname rel : String) : List Tuple := T.filter (fun tup => tup.relation == rel && tup.object.type == otype && (matchingObjects oname).contains tup.object.name && restrictionMatches rs tup)
def:Zanzibar.graphRun	[def] def graphRun (S : Schema) (ts : List Tuple) : Option (GraphState × Store) := graphRunAux S ts (emptyState S) []
def:Zanzibar.graphRunAux	[def] def graphRunAux (S : Schema) : List Tuple → GraphState → Store → Option (GraphState × Store) | [], σ, T => some (σ, T) | t :: ts, σ, T => if foldAdmitsB σ (rewriteClosure S t) then let σw := σ.writeLoggedRules S t graphRunAux S ts (cascadeLeg S (t :: T) σw) (t :: T) else none
def:Zanzibar.graphRunOps	[def] def graphRunOps (S : Schema) (ops : List GraphOp) : Option (GraphState × Store) := graphRunOpsAux S ops (emptyState S) []
def:Zanzibar.graphRunOpsAux	[def] def graphRunOpsAux (S : Schema) : List GraphOp → GraphState → Store → Option (GraphState × Store) | [], σ, T => some (σ, T) | GraphOp.add t :: ops, σ, T => if foldAdmitsB σ (rewriteClosure S t) then graphRunOpsAux S ops (cascadeLeg S (t :: T) (σ.writeLoggedRules S t)) (t :: T) else none | GraphOp.remove t :: ops, σ, T => if removeGateB S σ T t then graphRunOpsAux S ops (cascadeLeg S (T.erase t) (σ.removeLoggedRules S t)) (T.erase t) else none
def:Zanzibar.hitsLeaf	[def] def hitsLeaf (σ : GraphState) (u : NodeKey) (dt on : String) (e : Expr) : Bool := (computedRefs e).any (fun r' => σ.reach u (objNode ⟨dt, on⟩ r') || σ.reach u (wAllNode dt r'))
def:Zanzibar.htermB	[def] def htermB (S : Schema) (T : Store) : Bool := (taintedKeys S).all fun k => noTtuTargetB S k.2 && noStoreSubjectRB T k.2
def:Zanzibar.instances	[def] def instances (T : Store) (q : Query) (t : String) : List String := universeOf T q t false
def:Zanzibar.isDerived	[def] def isDerived (S : Schema) (k : Key) : Bool := (taintedKeys S).contains k
def:Zanzibar.iterate	[def] def iterate {α : Type} (f : α → α) : Nat → α → α | 0, x => x | n + 1, x => iterate f n (f x)
def:Zanzibar.kahn	[def] def kahn (edges : List (Key × Key)) : Nat → List Key → List (List Key) → Option (List (List Key)) | 0, remaining, acc => if remaining.isEmpty then some acc.reverse else none | fuel + 1, remaining, acc => if remaining.isEmpty then some acc.reverse else let ready := readyNodes remaining edges if ready.isEmpty then none else kahn edges fuel (remaining.filter (fun n => ¬ ready.contains n)) (ready :: acc)
def:Zanzibar.leafConcretes	[def] def leafConcretes (σ : GraphState) (dt on : String) (e : Expr) : List SubjectRef := (σ.nodes.filter (fun u => u.variant == Variant.plain && u.name != STAR && hitsLeaf σ u dt on e)).map nodeSubj
def:Zanzibar.matchingObjects	[def] def matchingObjects (oname : String) : List String := if oname = STAR then [STAR] else [oname, STAR]
def:Zanzibar.memberOfGranted	[def] def memberOfGranted (rec : Rec) (T : Store) (q : Query) (grants : List Tuple) : Bool := grants.any (fun g => if g.subject.predicate == BARE then false else if g.subject.name != STAR then rec g.subject.type g.subject.name g.subject.predicate else (instances T q g.subject.type).any (fun inst => rec g.subject.type inst g.subject.predicate))
def:Zanzibar.noStoreSubjectRB	[def] def noStoreSubjectRB (T : Store) (R : String) : Bool := T.all fun t => !(t.subject.predicate == R)
def:Zanzibar.noTtuTargetB	[def] def noTtuTargetB (S : Schema) (R : String) : Bool := (schemaRewrites S).all fun r => match r.kind with | RuleKind.ttu tr => !(tr == R) | RuleKind.computed => true
def:Zanzibar.nodeSubj	[def] def nodeSubj (u : NodeKey) : SubjectRef := ⟨u.type, u.name, u.pred⟩
def:Zanzibar.objNode	[def] def objNode (o : ObjectRef) (R : String) : NodeKey := if o.name = STAR then ⟨o.type, STAR, R, Variant.wAll⟩ else ⟨o.type, o.name, R, Variant.plain⟩
def:Zanzibar.pathCount	[def] def pathCount (g : DirectGraph V) (u v : V) : Nat := ∑ k ∈ Finset.Ico 1 (Fintype.card V + 1), pathsOfLength g k u v
def:Zanzibar.pathsOfLength	[def] def pathsOfLength (g : DirectGraph V) : Nat → V → V → Nat | 0, u, v => if u = v then 1 else 0 | k + 1, u, v => ∑ w : V, g.dcount u w * pathsOfLength g k w v
def:Zanzibar.phat	[def] def phat (g : DirectGraph V) (u v : V) : Nat := pathCount g u v + (if u = v then 1 else 0)
def:Zanzibar.readyNodes	[def] def readyNodes (remaining : List Key) (edges : List (Key × Key)) : List Key := remaining.filter (fun n => ¬ edges.any (fun e => e.1 == n && remaining.contains e.2))
def:Zanzibar.reconcileJobsLR	[def] def reconcileJobsLR (S : Schema) (T : Store) (σ : GraphState) (jobs : List W3cJob) : GraphState := jobs.foldl (W3cJob.applyLoggedR S T) σ
def:Zanzibar.refsOf	[def] def refsOf (S : Schema) (k : Key) : List Key := match S.lookup k with | some e => exprRefs S k.1 e | none => []
def:Zanzibar.relNameOK	[def] def relNameOK (name : String) : Prop := ¬ name.contains '.'
def:Zanzibar.removeGateB	[def] def removeGateB (S : Schema) (σ : GraphState) (T : Store) (t : Tuple) : Bool := decide (t ∈ T) && drainedB S σ && storeValidRulesB S T && bareStarStoreB T && ttuStarFreeB S T && htermB S T
def:Zanzibar.residueNamed	[def] def residueNamed (σ : GraphState) (dt on r' : String) : List SubjectRef := ((σ.residue (objNode ⟨dt, on⟩ r') r').getD Residue.empty).neg ++ ((σ.residue (objNode ⟨dt, on⟩ r') r').getD Residue.empty).upos
def:Zanzibar.restrictionMatches	[def] def restrictionMatches (rs : List Restriction) (tup : Tuple) : Bool := rs.any (fun r => tup.subject.type == r.1 && tup.subject.predicate == r.2.1 && ((tup.subject.name == STAR) == r.2.2))
def:Zanzibar.rewriteClosure	[def] def rewriteClosure (S : Schema) (t : Tuple) : List Tuple := (rewriteClosureRaw S t).dedup
def:Zanzibar.rewriteClosureAux	[def] def rewriteClosureAux (S : Schema) : Nat → List Tuple → List Tuple | 0, cur => cur | n + 1, cur => cur ++ rewriteClosureAux S n (cur.flatMap (rewriteStep S))
def:Zanzibar.rewriteClosureRaw	[def] def rewriteClosureRaw (S : Schema) (t : Tuple) : List Tuple := rewriteClosureAux S (S.keys.length + 1) [t]
def:Zanzibar.rewriteStep	[def] def rewriteStep (S : Schema) (t : Tuple) : List Tuple := (schemaRewrites S).filterMap (applyRRule · t)
def:Zanzibar.runCascade2	[def] def runCascade2 (S : Schema) (T : Store) (σ : GraphState) (jobs1 jobs2 : List W3cJob) : GraphState := if ((reconcileJobsLR S T (reconcileJobsLR S T σ jobs1) jobs2).frontierRowsAbove ((reconcileJobsLR S T σ jobs1).frontierMax (σ.frontierMax σ.watermark))).all (fun d => (affectedKeys S (reconcileJobsLR S T (reconcileJobsLR S T σ jobs1) jobs2) d).isEmpty) then { reconcileJobsLR S T (reconcileJobsLR S T σ jobs1) jobs2 with watermark := (reconcileJobsLR S T (reconcileJobsLR S T σ jobs1) jobs2).maxOutboxId } else σ
def:Zanzibar.schemaRewrites	[def] def schemaRewrites (S : Schema) : List RRule := (S.defs.filter (fun d => !(isDerived S d.1))).flatMap (fun d => exprArms d.1.1 d.1.2 d.2)
def:Zanzibar.sem	[def] def sem (S : Schema) (T : Store) (q : Query) : Bool := semAux S q.subject T q (fuelBound S T) q.object.type q.object.name q.relation
def:Zanzibar.semAux	[def] def semAux (S : Schema) (subject : SubjectRef) (T : Store) (q : Query) : Nat → String → String → String → Bool | 0, _, _, _ => false | fuel + 1, otype, oname, rel => step S subject T q (semAux S subject T q fuel) otype oname rel
def:Zanzibar.starSubj	[def] def starSubj (sh : Shape) : SubjectRef := ⟨sh.1, STAR, sh.2⟩
def:Zanzibar.step	[def] def step (S : Schema) (subject : SubjectRef) (T : Store) (q : Query) (rec : Rec) (otype oname rel : String) : Bool := match S.lookup (otype, rel) with | none => false | some e => evalE rec subject T q otype oname rel e
def:Zanzibar.storeValidRulesB	[def] def storeValidRulesB (S : Schema) (T : Store) : Bool := T.all fun t => match S.lookup (t.object.type, t.relation) with | some e => (exprDirects e).any (fun rs => restrictionMatches rs t) | none => false
def:Zanzibar.storedDirectSubjects	[def] def storedDirectSubjects (T : Store) (dt on R : String) (e : Expr) : List SubjectRef := ((exprDirectsAll e).flatMap (fun rs => (grantsOf T rs dt on R).map (·.subject))).filter (fun s => s.name != STAR)
def:Zanzibar.stratify	[def] def stratify (S : Schema) : Option (List (List Key)) := kahn (depEdges S) (taintedKeys S).length (taintedKeys S) []
def:Zanzibar.subjNode	[def] def subjNode (s : SubjectRef) : NodeKey := if s.name = STAR then ⟨s.type, STAR, s.predicate, Variant.wAny⟩ else ⟨s.type, s.name, s.predicate, Variant.plain⟩
def:Zanzibar.taintStep	[def] def taintStep (S : Schema) (cur : List Key) : List Key := S.keys.filter (fun k => baseTaint S k || (refsOf S k).any (fun r => cur.contains r))
def:Zanzibar.taintedKeys	[def] def taintedKeys (S : Schema) : List Key := iterate (taintStep S) (S.keys.length) []
def:Zanzibar.ttuLeaf	[def] def ttuLeaf (rec : Rec) (subject : SubjectRef) (T : Store) (q : Query) (targetRel tuplesetRel : String) (otype oname : String) : Bool := let objs := matchingObjects oname let s := subject T.any (fun tup => if tup.relation == tuplesetRel && tup.object.type == otype && objs.contains tup.object.name then let pt := tup.subject.type let pn := tup.subject.name if pn != STAR then (s.type == pt && s.name == pn && s.predicate == targetRel) || rec pt pn targetRel else (s.type == pt && s.predicate == targetRel) || (instances T q pt).any (fun inst => rec pt inst targetRel) else false)
def:Zanzibar.ttuStarFreeB	[def] def ttuStarFreeB (S : Schema) (T : Store) : Bool := T.all fun t => !(t.subject.name == STAR) || (schemaRewrites S).all (fun a => match a.kind with | RuleKind.ttu _ => !((t.relation == a.matchRel) && (t.object.type == a.objectType)) | RuleKind.computed => true)
def:Zanzibar.universeOf	[def] def universeOf (T : Store) (q : Query) (t : String) (includeEndpoints : Bool) : List String := let fromTuples := T.foldr (fun tup acc => let acc := if tup.subject.type = t ∧ tup.subject.name ≠ STAR then tup.subject.name :: acc else acc if tup.object.type = t ∧ tup.object.name ≠ STAR then tup.object.name :: acc else acc) [] let endpoints := if includeEndpoints then (if q.subject.type = t ∧ q.subject.name ≠ STAR then [q.subject.name] else []) ++ (if q.object.type = t ∧ q.object.name ≠ STAR then [q.object.name] else []) else [] (fromTuples ++ endpoints).dedup
def:Zanzibar.wAllNode	[def] def wAllNode (t R : String) : NodeKey := ⟨t, STAR, R, Variant.wAll⟩
def:Zanzibar.wildcardShapes	[def] def wildcardShapes (S : Schema) : List Shape := S.defs.flatMap (fun d => (exprRestrictions d.2).filterMap (fun r => if r.2.2 then some (r.1, r.2.1) else none))
