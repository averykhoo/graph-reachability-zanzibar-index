# `P6` step 3b — the measured plan (the cone payment)

**ACTIVE-PLAN, opened 2026-09-13f — the body is provenance, not a living status.** This is
the execution map for `P6` step 3b, produced by a nine-agent reconnaissance sweep on
2026-09-13f and reconciled first-hand against the live tree. Every figure below is as-of
that date. Live state is the task row (`python scripts/task.py show P6`) and
`python scripts/gate_status.py` — never this file. Corrections append **dated at the top**,
never edited into the body. Freeze it when `P6` closes.

⚠ **This plan CONTRADICTS the `P6` row on five load-bearing points**, each verified
first-hand before it was written down (§ Corrections). The largest: the row's standing claim
that *"the carry terminates in `GraphAdmission`, so NO HEADLINE GAINS A HYPOTHESIS"* is
**false** — two statement-pinned headlines gain one. Read § Blockers before step 1.

## Corrections appended 2026-09-13g -- what the recon sweep changed

**These corrections SUPERSEDE the body sections they name, and the body is deliberately left
unedited.** This file's own rule is that corrections append dated at the top and never get edited
into the body, so the body stays as provenance for how the earlier view was reached; where a line
below and a body line disagree, the line below wins. Source: a SECOND nine-agent sweep, run
2026-09-13g against commit `30ad44a` (plan steps 1 and 2 landed, ten-phase gate COVERED on that
tree) -- six recon reports keyed shadow-T1T2T3 / symbol-table / headline-pins / python-fidelity /
occurrence-counts / downstream-volume, plus THREE independent adversarial verifiers pointed at the
shadow report with three different lenses (missing edge SOURCE / type-index escape hatch /
vacuity).

**Every claim below carries a provenance tag, and an untagged claim is worse than no claim --
it gets re-verified from scratch.**
  `(READ)` -- a recon agent read it first-hand and returned `file:line::symbol` evidence.
  `(READ, recorder)` -- the recorder re-grepped it against the live tree while writing this section.
  `(VERIFIED)` -- one of the session's two first-hand verifications (T3 terminality; the headline
  pin). **These OUTRANK any recon report they contradict.**
  `(REASONED)` -- derived on paper from READ facts. ⚠ **Nothing in this sweep was machine-checked:
  all nine agents were read-only and ran no `lake build`**, so every "discharges" and every "goes
  FALSE" is a paper proof, not a kernel refutation.
  `(UNVERIFIED)` -- nobody closed it. Collected in `C8`.

⚠ **LINE-NUMBER ANCHOR, AND IT IS ALREADY DRIFTING.** Every line number in this section resolves at
commit `30ad44a` unless marked *live*. The working tree has ALREADY moved past that commit with
uncommitted step-3/4 work in `Cascade.lean`, `CascadeSettle.lean`, `CascadeStable.lean` and
`UsStarWrite.lean`. Measured by the recorder this session: every `CascadeStable.lean` declaration
has shifted by `-4` lines, and `UsStarWrite.lean` grew `1573` -> `1694` lines, with three new
declarations (`ensureInBridges_nodes_incident`, `writeBridgedOne_nodesFromEdges`,
`foldl_writeBridgedOne_nodesFromEdges`) inserted at the old `:1187`, moving `namespace
BridgedWriteWitness` from `1187` to `1308`. **Everything at or below `UsStarWrite.lean:1186` is
unmoved; the two cycle-attribution pins are not** (`bridged_probe_refuses_the_cycle` `1291` ->
*live* `1412`). Grep for the symbol; do not trust a number in this file, including these.

### C1. T3 is REFUTED -- `BareStarStore` does NOT make a bridge node terminal

*supersedes:* § Step 8's third `term` obligation (T3), and the DESIGN BLOCKER bullet in § Blockers.

(VERIFIED) **The body's T3 justification is FALSE as written.** It says `W4Fragment.bareStar :
BareStarStore T` "forces every stored STAR subject to have predicate BARE ... and
`isSubjectWildcardUserset _ BARE = false` by the OUTER `p != BARE` guard". The guard is real and
outermost, but `BareStarStore` quantifies over STORED tuples only
(`BareStarCorrect.lean:44::BareStarStore`, `∀ t ∈ T, …`), while the obligation is about members of
`rewriteClosureL S (rawWriteTuples S t)` -- the list the re-pointed write leg folds. The closure
MANUFACTURES a non-BARE star subject: `rawWriteTuples` (`Leaf.lean:793`) re-addresses the RELATION
only and carries `t.subject` through unchanged, but `applyRRule`'s ttu arm
(`RulesWrite.lean:86-90`) emits `some ⟨⟨t.subject.type, t.subject.name, tr⟩, r.outRel, t.object⟩`
-- subject NAME preserved (`STAR` survives), predicate OVERWRITTEN with the TTU target `tr`. `tr`
is a declared relation name so `tr ≠ BARE`, the outer guard does not fire, `subjNode` of that
member IS `wAnyNode (ty, tr)`, and it is an edge source. **The hazard is the DESIGNED case, not a
corner.**

(VERIFIED) **The tree already knows this and already pays for it.** `RulesBareStar.lean:168::
rewriteClosure_star_bare` exists for exactly this purpose and takes THREE premises --
`(hTT : TtuTuplesetsDirect S) (hTS : TtuStarFree S T) (hBS : BareStarStore T)`. Its engine
`starSeed_step` (`RulesBareStar.lean:98`) discharges the ttu arm in two cases, neither of them from
`BareStarStore`. And `TtuStarWide.lean:72::TtuStarFreeW` -- already landed -- is the WIDENED
predicate that ADMITS a stored star tuple feeding a TTU tupleset provided
`S.isSubjectWildcardUserset t.subject.type tr = true`, i.e. the tree records this exact
configuration as reachable and as producing a bridged-in through-shape.

(READ) **All three adversarial verifiers reached the same verdict independently.** Verifier 2 built
a concrete counter-model out of the tree's own parts: `t = ⟨⟨"folder","*",BARE⟩, "parent",
⟨"doc","d1"⟩⟩` over `ThroughShapeWitness.Sthru`, where `BareStarStore [t]` HOLDS, `StoreValidRules`
holds via `Spec/Semantics.lean:37::restrictionMatches`, every premise `reachedByW3d_shadow`
currently binds holds, `Sthru.isSubjectWildcardUserset "folder" "viewer" = true` is already
machine-checked by `decide` in `UsStarWrite.lean`, and `ShadowOver.term` FAILS. `TtuStarFree S [t]`
is exactly FALSE at that model -- which is the point: the fragment field is what excludes it.

**WHAT STEP 8 MUST NOW THREAD.** (VERIFIED) `reachedByW3d_shadow` binds NEITHER
`TtuTuplesetsDirect S` NOR `TtuStarFree S T`. (READ, recorder) Its full signature is one named
binder `(h : ReachedByW3d σ S T)` plus EIGHT arrow-form hypotheses -- `NodupKeys S`, the
ComputedOnly-at-derived clause, `StoreValidRules S T`, `hterm`, `TtuTargetsSat S NotLeafName`,
`DirectRestrictionsNotLeaf S`, `LeafScope S`, `BareStarStore T` -- so adding a premise means
editing the arrow chain AND every `intro` list in the induction (the empty case is literally
`intro _ _ _ _ _ _ _ _`). Both new premises must be added there and to the two W3d-2 twins
(`CascadeStrataSettle.lean:809::reachedByW3d2_shadow` and `:1448::reachedByW3d2_shadow_d`;
(READ, recorder) -- ⚠ two verifiers cite the `_d` twin as `:1348`, which is wrong).

(READ, recorder) **The threading is cheap and forces NO new admission field.** Both providers
already exist -- `FullScope.lean:188::GraphAdmission.ttuDirect` and
`FullScope.lean:340::W4Fragment.ttuStarFree` -- and all FIVE application sites of
`reachedByW3d_shadow` already bind both, with the store-weakening line for `hTS` already written in
`CascadeStable.lean`. So T3 CONSUMES `W4Fragment.ttuStarFree`; it does not narrow or touch it, and
the `P6` standing bound is respected.

(VERIFIED) **A THIRD gap the recon missed, and it is the one that is not free.** `TtuStarFree`
(`RulesBareStar.lean:43`) quantifies over `a ∈ schemaRewrites S` -- the UNTAINTED-only rule set
(`RulesWrite.lean:82-83` filters `!(isDerived S d.1)`). But `rewriteStepL` steps over
`schemaRewritesL S = schemaRewrites S ++ leafRewrites S` (`LeafRules.lean:118`), and `leafRewrites`
mints `.ttu` arms from DERIVED defs' closure leaves. A leaf ttu arm is OUTSIDE `TtuStarFree`'s
quantifier entirely. So `TtuTuplesetsDirect + TtuStarFree + BareStarStore` is still not sufficient
for the L-closure on its own; the leaf arms must be killed by `hCO` (`ComputedOnly` maps
`| .ttu _ _ => False`, `ReconcileCorrect.lean:34-40`), which `reachedByW3d_shadow` already binds.
Verifier 1 reached the same conclusion independently and states the owed L-twin's premise list as
`hTT + hTS + hBS + hCO`.

(READ, recorder) **No L-variant of the star-bare family exists.** `rewriteClosure_star_bare`
(`:168`) and `rewriteClosure_star_subject` (`:142`) are stated over the PLAIN single-seed closure;
their engine `StarSeed` (`:94`) and `starSeed_step` (`:98`) are `private`, so an L version cannot
reuse them and must be re-minted in whichever module owns `writeLegSubjectsWide_L`. A tree-wide
grep for any `rewriteClosureL_star*` returns nothing.

### C2. T2's "ONE new lemma owed" is wrong -- the list is FOUR, and the cited precedent does not transfer

*supersedes:* § Step 8's (T2) paragraph, the PREMATURE bullet in § Corrections to the `P6` row, and
the `isSubjectWildcardUserset_false_of_notLeafName` entry in § Toolbox gaps.

(READ, recorder) **The body's adjudication SURVIVES: no new `LeafScope`/`GraphAdmission` field is
needed for T2, and `reachedByW3d_shadow` really does already bind `DirectRestrictionsNotLeaf S`,
`TtuTargetsSat S NotLeafName` and `LeafScope S`.** All three verifiers confirm the route is sound.
(READ) Verifier 2 additionally closed the quantifier-vacuity question the body did not ask:
`DirectRestrictionsNotLeaf` (`CascadeStable.lean:1028`) ranges over ALL `S.defs`, derived included,
so it is not hollowed out by a taint filter; `TtuTargetsSat` (`ReconcileCorrect.lean:652`) DOES
range over the derived-dropping `schemaRewrites`, and the gap is plugged by `hCO`.

(READ) **But the body's "The ONE new lemma owed" is an undercount of four-to-one**, and all three
verifiers list the same four absences, each confirmed by an exhaustive grep:
  (i) `r ∈ exprRestrictions e → ∃ rs ∈ exprDirectsAll e, r ∈ rs` -- ABSENT. Only the CONVERSE
      exists, `ReconcileStarsComplete.lean:294::mem_exprRestrictions_of_directsAll`. This is the
      flattening step disjunct (a) needs, and the body does not mention it at all.
  (ii) `ComputedOnly e → exprTtus e = []` -- ABSENT. The structurally identical twin
      `ReconcileCorrect.lean:1046::exprDirectsAll_computedOnly` exists and is the template.
  (iii) untainted def ⇒ `containsBool = false` ⇒ `exprTtus` targets ⊆ what `exprArms` collects.
      The pieces exist (`Spec/Stratify.lean` `containsBool`/`baseTaint`, `Spec/Stabilize.lean::
      untainted_closed`, `RulesSound.lean:44::lookup_of_mem`); the composition does not.
  (iv) the `exprTtus` -> `schemaRewrites` membership step -- the body's "ONE" lemma.
(READ) ⚠ **The body's cited precedent for (iv) is the WRONG SHAPE and does not transfer.**
`ReconcileCorrect.lean:658::rewriteStep_subject_pred_gen` CONSUMES `TtuTargetsSat` over a
`rewriteStep` membership; it maps nothing from `exprTtus` into `schemaRewrites`. Two verifiers
independently name the transferable precedents instead: `RulesSound.lean:165` (the converse
direction) and `RulesComplete.lean:225::lookup_exprArms_sub` (right direction, but requires
whole-schema untaintedness, so too strong).

(READ) **Do NOT route T2 through `hLS.noLeafSubjects` instead.** It looks stronger (quantified over
`schemaRewritesL`, which covers derived defs) but has a real hole: `atomLeaves`' `.ttu` arm
(`Leaf.lean:414`) drops an IMPURE TTU entirely, so a TTU with a derived tupleset mints no leaf rule
and `NoLeafSubjects` says nothing about its target.

### C3. The headline-pin verdict: CONFIRMED, and the pin surface is NARROWER than § Step 15 assumes

*supersedes:* nothing in § Step 10 or § Blockers -- both stand. EXTENDS § Step 15.

(VERIFIED) **The body's HEADLINE BLOCKER is right on every load-bearing point.**
`CascadeStrata.lean:1743::runCascade2_no_abort` and `:1911::cascade2_drains` have byte-for-byte the
SAME binder list -- implicits `{σ S T} {jobs1 jobs2}` then exactly six explicit hypotheses
`hterm`, `hLU2`, `hjv1`, `hjv2`, `hscope2`, `h : ReachedByW3d2 σ S T` -- with no `GraphAdmission`
bundle and nothing mentioning `isSubjectWildcardUserset`. Both are in `verify.sh`'s
`HEADLINE_AUDITS` (`verify.sh:659-663`, consumed by the loop at `:856-871`) and in
`statement_pin.py`'s `HEADLINE` list. The carry is NOT locally derivable: the decisive structural
check is that `isSubjectWildcardUserset` occurs in only FIVE `.lean` files and `CascadeStrata.lean`
is not among them.

(READ, recorder) **Reconciling two figures that look contradictory and are not.** The body says the
two headlines are "rows 7 and 8 of 51"; the headline-pins report says "rows 25 and 26". Both are
right: `formal/headline_statements.txt` has `51` data rows, and the two are FILE LINES `25` and
`26`, i.e. data rows `7` and `8`. Same for the definition pin -- the body's "`244` `def:` rows" and
the report's "`253` rows" are the same file: `244` `def:` + `9` `ambient:` = `253` non-comment rows.
Use the file-line form when talking to `sed`, the data-row form when talking to the pin script.

(READ) **The pin surface `3b` actually hits is smaller than the body's § Step 15 implies.** Of the
`29` symbols the pin agent swept, NONE is in `headline_statements.txt` -- the two statement-pin rows
that move are moved by the new BINDER, not by any of the restated lemmas. `16` are name-pinned in
`audited_theorems.txt` + `Audit.lean`, so a RESTATEMENT is invisible to step 4a and only a
rename/delete reds it (`verify.sh:836-852` is one-directional, `comm -23 pin live`). SIX are in no
pin file at all and are entirely free: `reachedByW3d2_edge_source_ne_R`,
`reconcileJobsL_Rnode_not_source`, `reachedByW3d2_edges_target_plain`,
`mem_removeLoggedOne_edges_iff_of_ne`, `removeLoggedRules_edge_delta`, `reachedByW3d2_srcOccCount`.

(READ) ⚠ **The trap the body's § Step 15 does NOT state: `check_definitions` fails on `added` rows
exactly as it does on changed ones** (`statement_pin.py:836-844`). The `MIN_PINNED_DEFS=139` floor
is `-ge` and therefore floor-SAFE against growth, but the CHECK is not -- the `~8` new bridge defs
red step 4c until `--generate` is run. The body's "moves `MIN_PINNED_DEFS` the safe direction" is
true and irrelevant.

(READ) **Measured baseline, so that everything red after step 3b is attributable to step 3b.** On
the `30ad44a` tree the pin agent ran both checkers first-hand: `statement_pin.py` exits `0` with
`51/51 statements match` / `253/253 definitions match (floor 139)`, and `anchor_check.py` exits `0`
with `643 parsed … 643 resolved`. Steps 1-2 disturbed neither golden.

(READ) **Three more pin facts the body omits.** `GraphState.writeRulesRaw` is NOT in the definition
pin, so re-point #2 changes no pinned body -- but it IS name-pinned (`audited_theorems.txt:25` /
`Audit.lean:1985`) and carries THREE `CORRESPONDENCE.md` anchors, so it may be restated and must
never be renamed. Row `88` (`writeDirect`) will NOT drop out of the closure when `writeLoggedOne`
stops calling it, because rows `56`/`81`/`209` still reference it -- no `gone` discrepancy. And
there is NO row-count floor on `headline_statements.txt` anywhere in `verify.sh`; its non-vacuity
is enforced only inside `statement_pin.py`.

### C4. The occurrence-count family has a FIFTH member, and § Step 14 is a PRECONDITION of § Step 10

*supersedes:* § Step 10's THE OCCURRENCE-COUNT FAMILY block, § Step 12, and § Step 14's
"legitimately deferrable IF recorded".

(READ, recorder) **A fifth false-statement member the body's § Step 10 list omits, and it is the one
with live consumers**: `CascadeStrata.lean:952::reachedByW3d2_untOccCount`, the LOW-chain twin of
the `RemoveOccCount` theorem. It goes FALSE by the identical argument (its guard
`isDerived S (b.type,b.pred) = false` is SATISFIED at a bridge target under `NoBridgedDerived`), and
it is consumed by the R5b shadow transport at `CascadeStrataSettle.lean:763` and `:1389` inside the
same tactic block as `count_removeLoggedRules` and `untOccCount_erase`. **Any repair chosen for
`reachedByW3d2E_untOccCount` must be applied here in the same edit or step 11 will not build.**

(READ) **The single root cause, stated once so it is not re-derived per theorem.** A bridge edge is
`(c, wAnyNode (c.type, c.pred))`, and `wAnyNode` carries `Variant.wAny`
(`State.lean:122`), while `untOccCount` sums `edgeOfTuple`, whose target is `objNode` and therefore
`plain` or `wAll` but NEVER `wAny` (`State.lean:126-128`). So `untOccCount` is structurally `0` at
every bridge edge, for every `S` and every `T`. Every FALSE verdict in this family is that one fact
seen through a different guard.

(READ) ⚠ **`count_writeLoggedRules` goes FALSE a SECOND way that NO bridge term repairs, and this
re-ranks the whole plan.** Its hypothesis is `hadm : FoldAdmits σ (rewriteClosureL …)`, and
`FoldAdmits` (`RulesComplete.lean:50-56`) probes `σ.admitEdge` recursing on `σ.writeDirect u` -- the
UNBRIDGED state -- while `writeBridgedOne` probes `(σ.bridgePre t).admitEdge`. The tree already pins
the two DISAGREEING (`bridged_probe_refuses_the_cycle = false` vs
`unbridged_probe_admits_the_cycle = true`, both by `decide`). So `hadm` can hold while the bridged
fold REJECTS a member: LHS `+0`, RHS `+1`, at an ordinary PLAIN grant edge. **§ Step 14 is therefore
a hard precondition of § Step 10, not a deferrable honesty item.**

(READ) **`count_foldl_writeDirect` does not go false -- it goes ORPHANED.** It quantifies over a
`writeDirect` fold, which `3b` does not touch; what dies is its APPLICABILITY, because its sole
consumer unfolds `writeRulesRaw`. Keep it verbatim as the unbridged baseline the sabotage story
needs, re-label it, and add `count_foldl_writeBridgedOne` beside it. The body's "genuine RE-PROOF"
is right about the work and wrong about the verdict.

(READ) **The adjudication the body defers to the executor, decided.** Adding
`(hplain : b.variant = Variant.plain)` IS the shape refused at `RemoveOccCount.lean:176-194` -- and
refused a SECOND time, independently, at `CascadeStrata.lean:1118-1121`. Three first-hand reasons:
it leaves the `wAny` class characterised by nothing; it would force `Exec.lean`'s deliberately
unguarded `reachedByW3d2E_edgeCount_store_indexed` back into the `_offLeaf` form the tree DELETED as
"strictly weaker"; and it is not even tight, since it also drops the `wAll` targets `untOccCount`
legitimately counts. Extend the recorded Wall-2 decision instead: one new store-and-schema-only
`inBridgeOccCount` summand on the three chain theorems, fold-collected `bridgesAdded`/
`bridgesReleased` summands on the three step lemmas. **The operational test to hand the executor: is
every `(a,b)` still characterised by SOME theorem in the file? A guard is admissible only as half of
a PARTITION whose complementary arm lands in the same commit.**

(READ) **Two prose corrections owed in the same edit, because the words become false the moment the
release lands**: `CascadeStrata.lean:737` says `count_writeLoggedRules` is "UNCONDITIONAL", and
`RemoveConfluence.lean:26-29` says the count laws hold "UNCONDITIONALLY".

(READ) **`RemoveConfluence.lean` is a LEAF module** -- (READ, recorder) imported only by the
aggregator at `formal/lean/ZanzibarProofs.lean:76`, with zero consumers of any of its four R4 names
tree-wide. Repairing it costs one file with no cone. ⚠ The body's cite `RemoveConfluence.lean:16`
for "what `graph_correct`'s REMOVE leg stands on" is WRONG -- `:16` is a `#eval` grid sentence; the
claim lives at `RemoveOccCount.lean:192-194`.

(READ) **The existing round-trip pin goes green BY VACUITY after `3b`.**
`CascadeStrata.lean:1130::writeThenRemove_edge_count_zero` is stated over `SlV`, which bridges
nothing. Add the identical round trip over `ThroughShapeWitness.Sthru` with a bridging tuple, or the
whole release epilogue ships with no write/remove round-trip witness. This is the same gap
§ Step 5's ASSURANCE BLOCKER names for `writeLoggedOne`; it is now known to apply to the count stack
too.

### C5. Python fidelity -- claim (a) is PARTIAL, claim (b) buys nothing, claim (c) is TWO divergences

*supersedes:* § Step 15's CORRESPONDENCE.md bullet, and the final UNVERIFIED BY ME bullet of
§ Corrections to the `P6` row.

(READ, recorder) **Claim (a) is PARTIAL and its headline clause is REFUTED.** The guard is verbatim
correct -- `index_v4/wildcard.py:375` is `if not (fresh.implicit and fresh.reference_count ==
degree): return` -- and the sticky promote at `index_v4/processor.py:1012-1018` is real. But
"after which Python KEEPS the bridge forever (`:1063-1067`)" is FALSE: those lines are INSIDE the
2026-08-21 BL-1 fix comment (`processor.py:1058` opens `# DEMOTE BEFORE STRIP (BL-1, …)`)
explaining why `_gc_subject_node` must DEMOTE (`:1073-1074`) before it STRIPS (`:1076`), and
`tests/test_userset_bridge_release_leak.py` pins that add-then-remove now restores the exact row
multiset. **Do not write the body's sentence into `CORRESPONDENCE.md`.** (READ) The corrected
boundary line: Python DECLINES the strip at the raw-remove epilogue (`wildcard.py:569-570`) while
the node is still explicit and catches up later in the same transaction's cascade; Lean, which
models no explicitness and has no cascade-side release, strips at the write leg. That is an
intra-transaction ORDERING gap on the un-recorded path and a genuine STATE gap only while the
recording survives.

(READ, recorder) **Two further (a) facts the body omits.** `_maybe_remove_bridges` has FIVE exit
points, and the body quotes one: there is a second declination at `wildcard.py:377-385`, the I14
crossable-middle case. And the promoted class and the BRIDGED class are the SAME class -- both are
exactly the non-bare, concrete, userset-shaped nodes -- so the collision is structural, which
strengthens the body's claim rather than weakening it.

(READ) **Claim (b) implies NO new boundary line.** `_map_rows`' `o_name == '*'` raise sits inside
the `LeafFamily` arm and is unreachable from a bridge row, because bridged shapes carry declared,
dot-free predicates while `LeafFamily` keys are minted `<R>.<i>`. The star-object channel that IS
live -- the unguarded `tupleset_feeders`/`target_feeders` fan-out -- is already inside the recorded
`_map_deltas_to_keys` scope gap at `Cascade.lean:1038-1041`. Drop this item from § Step 15's list.

(READ, recorder) **Claim (c) is CONFIRMED and is TWO divergences, not one.** (1) a rejected BRIDGE:
Lean skips the bridge and proceeds (`ensureInBridges`' reject branch interns the node and returns),
while Python aborts the whole `RuleSet.apply` fold -- there is genuinely no handler at
`wildcard.py:274`. (2) a rejected GRANT: Lean's `else σ` discards `bridgePre`'s nodes AND bridge
edges, while Python leaves them in the session and depends on the CALLER's rollback. ⚠ Minor cite
fix: the `try:` keyword is `wildcard.py:525`, not `:527` -- `:527` is the `except`.

(READ) **What DOES match, exactly, so nobody re-derives it**: both write-path orders, including
DELTA ORDER (bridge rows are emitted and flushed before the grant's), and the remove-path order the
body's § Step 5 reads off `wildcard.py:566-570`. The body's re-point #3 shape is right, including
the bare `else σ`.

(READ) **FOUR further Python bridge/unbridge sites the body's § Step 15 does not name**, none
modelled in Lean, none in `3b`'s scope, but §7.3 should not imply the write leg plus two cascade
sites is the complete list: `WildcardIndex.backfill`, `WildcardIndex.remove_node`'s strip-first,
`_sync_entity_middles`, and the offline `bulk_backfill.py`/`bulk_build.py` mirrors.

### C6. Downstream volume -- the carry is FOUR levels deep, and two modules are misranked

*supersedes:* § Step 9's CascadeInv paragraph, § Step 11's CascadeStrataAssemble line, and the
figures in § Step 13.

(READ) **The 12-site R-node census is EXACTLY RIGHT** -- re-counted comment-stripped over all `71`
`.lean` files, module for module. But `12` is only LEVEL ONE. Threading `(hNBD : NoBridgedDerived
S)` through those 12 forces it onto `17` FURTHER declarations at levels 2-4, none of which binds a
`GraphAdmission` bundle -- they all spell the carries out as POSITIONAL arrows, so every `intro`
list and every argument list gains a slot. (READ) The worked example:
`CascadeStrataEdge.lean:371` passes `17` positional arguments and becomes `18`; `CascadeInv.lean`'s
`reachedByW3dC_edgeHygienic` opens with `intro` and seventeen underscores.

(READ) ⚠ **`CascadeInv` is NOT "the CHEAPEST downstream module".** It gains a carry at
`reachedByW3dC_edgeHygienic` (`:491`, applied `:552`) and at `reachedByW3dC_inv` (`:621`, applied
`:639`). The body's reason for calling it cheap -- that the `structInv_*` bridge twins already exist
-- is true but ORTHOGONAL to the carry. The actually-cheapest module is `CascadeStrataEnum`: zero
write-leg references, zero R-node references, zero carry sites; it breaks only by recompile.

(READ) ⚠ **`CascadeStrataAssemble` is listed with zero carry sites and has TWO**, at
`graph_correct_w3d2E_d` (`:721`) and `graph_correct_w3d2E` (`:766`) -- and the first is what
`FullScope`'s `graph_correct` calls.

(READ) **The value-change risk is CONFIRMED and stronger than the body states.**
`FullScope.lean:1129-1133` pins `isSubjectWildcardUserset = true` on BOTH `SxThruDerived` and
`SxThruPlain` by `decide`, and `FullScope.lean:1258::sxThruPlain_admitted` proves the FULL
`GraphAdmission SxThruPlain []` bundle -- not merely the `usWild` field. **Bridging provably fires
on an ADMITTED schema.** It is inert today only because no downstream module applies a write leg to
one. (READ) Every live write-leg fixture was traced and NONE bridges (`SlV`, `SlSw`, `SlStP`, `SlA`,
`SlN`, `SlUnt`, `Sd`, `Sx`, `Sy`); the bridging schemas (`SwT`, `SdRouting`, `Sthru`) are never fed
to a fold. ⚠ (READ, recorder) The body's cite `FullScope.lean:1034 sxThruPlain_usWild` is WRONG --
`:1034` is `sxUsPlain_usWild`, a different theorem about a different schema; the live one is
`FullScope.lean:1122`.

(READ) **The one live VALUE-CARRYING surface in the tail is `Cli.lean`, and it is not a Lean
obligation.** `stateJson` serializes `σ.edges` plus edge multiplicities across the conformance seam,
and that `σ` comes from `Exec.lean`'s `writeLoggedRules`. For any conformance corpus schema whose
shapes bridge, `zcli` graph-state output changes VALUE and `formal/conformance/extractor.py` must
materialize the same bridges -- an obligation OUTSIDE the 19 Lean modules, and outside `verify.sh`'s
default target (`Cli.lean` is the `zcli` `lean_exe` root, built by its own gate step).

(READ, recorder) ⚠ **`Scratch4cii.lean:921` is NOT a live site** -- it is inside the
`## Observed outputs` block comment (`:861-960`), a quoted sabotage transcript. The live
`writeRulesRaw` sites are `:79`, `:212`, `:216`, `:217`, `:457` -- FIVE, not the body's four.

(READ) **Three of the body's decide counts are LOW, all in the direction that under-budgets
§ Step 13.** Measured comment-stripped: `Exec.lean` `49` `by decide` (body says `28`),
`Scratch4cii.lean` `53` (body says `37`), `FullScope.lean` `157` (body says `152`). (READ, recorder)
The body's figures reproduce as NAIVE per-line greps, so the disagreement is methodology, not
error -- but the executor should re-measure rather than plan off either. Thirty of `Scratch4cii`'s
decides compute a concrete `.edges` list through a write leg: by far the largest
decide-through-a-write-leg surface in the tree, and all thirty pay the new `bridgedInConcrete` scan
per fold member per endpoint.

(READ) **`CascadeStrataEdge.lean` is not imported by the aggregator** -- (READ, recorder) it reaches
the build solely via `FullScope.lean:3`. Worth knowing before someone concludes it is dead.

### C7. Where the recon CONTRADICTED ITSELF -- recorded, not averaged

The repo rule is that contradicted reports get reconciled, not averaged. Four disagreements, each
adjudicated by the recorder against the tree this session.

- **`reachedByW3d_shadow`'s call-site count.** The shadow report said "~16 across five modules";
  all three verifiers said FIVE, and verifier 3 traced the "16" to a DOCSTRING figure at
  `CascadeStable.lean:1769`. (READ, recorder) That sentence is about how many POSITIONAL call sites
  `TtuTargetsSat` has, not about `reachedByW3d_shadow`'s applications. **FIVE is right**
  -- `CascadeEnum.lean:363`, `CascadeSettle.lean:589`, `:935`, `CascadeStable.lean:2095`, `:2096`.
  Everything else that greps is prose or an `#print axioms` row. Verifiers win.
- **The `DerNode … ∨ LeafNode` hand-widening census.** The shadow report said `22` sites and listed
  `20`; verifier 1 said `24` matches; verifier 3 said `24` lines of which `19` are CODE and `5`
  prose. (READ, recorder) **Verifier 3 is exactly right**: `24` lines total --
  `CascadeStable.lean` `10`, `CascadeStrataSettle.lean` `8`, `Scratch4cii.lean` `5`,
  `LeafRules.lean` `1` -- of which `CascadeStable.lean:1709`/`:1883`, `LeafRules.lean:646`,
  `Scratch4cii.lean:512`/`:623` are prose, leaving `19` code sites. `Scratch4cii` has THREE code
  sites (`:633`, `:678`, `:701`), not the shadow report's two.
- **Whether `shadow_reach_agree`'s target can be a `wAnyNode`.** The shadow report said it has
  "exactly three call sites" and that its target is never a `wAnyNode`, so the READ-side widening
  is free. Verifier 2 refuted BOTH halves: FOUR distinct lines and FIVE applications, and the
  freeness holds only of the `:1929-1931` uses -- `shadow_admitEdge_agree` (`:1252`) is
  `Extra`-GENERIC and is instantiated by every consumer at `subjNode u.subject`, which IS
  `wAnyNode (ty,p)` exactly when the subject is a star. (READ, recorder) **The count is verifier
  2's** -- four lines, five applications (`:1252`, `:1925` twice, `:1926`, `:1927`, *live*). The
  substantive half is UNRESOLVED and is in `C8`: nobody re-checked whether the `abbrev`-reducibility
  argument survives at the `shadow_admitEdge_agree` instantiation.
- **Whether the T2 route's vacuity hazard is live.** Verifier 1 says the `hQ` route inherits
  `LeafRules.lean:659-667`'s documented trap -- (READ, recorder) `LeafRules.lean:1840::
  lrV_untainted_layer_silent` proves `schemaRewrites SlV = []` by `decide`, so ANY
  `∀ r ∈ schemaRewrites S` premise discharges VACUOUSLY at `SlV` -- and demands a non-vacuity
  witness per `docs/sabotage-procedure.md` before T2 counts as checked. Verifier 3 hunted the same
  hazard as its assigned lens and calls it BENIGN, because at `SlV` the T2 obligation discharges
  OUTRIGHT (`SlV` has no `.ttu` anywhere, so `exprTtus` is `[]` and `isStarTuplesetThrough` is
  identically false). **Both are right about different questions** and the reconciliation is: T2 is
  SOUND at `SlV`, and the premise is nonetheless VACUOUS there, so `SlV` cannot serve as the
  non-vacuity witness. Land T2 with a witness schema carrying a real untainted TTU whose target is
  leaf-named, following `LeafRules.lean:764::wf_does_not_give_keysNonempty`.

**One EXTENSION rather than a contradiction, and it is decision-relevant.** Verifier 2 listed "did
not trace whether widening `Extra` breaks any `ShadowOver` consumer outside the write/read analysis
-- specifically the reconcile legs" as a residual risk; verifier 1, working the missing-edge-source
lens, went and found it. (READ) **The CASCADE leg is an independent `term` obligation the body's
§ Step 8 EDITS list does not carry.** `reconcileKeyD` folds `writeDirect` per candidate, and three
more declarations state the subject obligation at the two-disjunct form and consume it with an
idiom that cannot typecheck at three (`untaintedShadow_applyD` `CascadeStable.lean:1552`;
`untaintedShadow_applyLoggedR` `CascadeStrataSettle.lean:318`; the `_d` twin `:1192`). They are FREE
of new premises -- `W3cJobValid` (`ReconcileStarsComplete.lean:99`) gives
`∀ c ∈ j.cands, c.predicate = BARE` and `c.name ≠ STAR`, so `subjNode c` is plain and the outer
`p != BARE` guard kills it -- but they are absent from the body's cost.

(READ, recorder) **A second extension with real cost, from verifier 1 and confirmed here.** The
step-2 keystone `foldl_writeBridgedOne_edges_sound` DELIBERATELY drops `bridgedInConcrete`'s
`variant = plain ∧ name ≠ STAR` conjuncts from its third disjunct -- the file's own note says "the
strengthenings belong with the consumer that needs them". At that disjunct the `BridgeNode` case of
`term` has NO contradiction available, because a `wAnyNode (ty,p)` satisfies the disjunct's
hypothesis shape verbatim. **So § Step 8 also owes a STRENGTHENED soundness lemma in
`UsStarWrite.lean` -- a module § Step 8's declared file set (`CascadeStable.lean`, `Leaf.lean`) does
not include, and which the body reports as finished.**

### C8. Still UNVERIFIED after this sweep

Nothing here was closed. Each is owed before the thing it gates is written into a tracked file.

- **Every "discharges" and every "goes FALSE" in this document is a PAPER proof.** All nine agents
  were read-only and ran no `lake build`. The four owed T2 lemmas (`C2`) and the owed L-closure
  star-bare family (`C1`) are asserted routine on the strength of structurally identical existing
  proofs; none was elaborated.
- **Whether the L-twin of `starSeed_step` actually goes through over `schemaRewritesL`.**
  `TtuTuplesetsDirect` is stated over `exprTtus` of `S.defs`, and its role in the PLAIN proof is to
  block a ttu arm firing on a rewrite OUTPUT at the tupleset relation; whether that survives when
  the output set also contains leaf-named relations was not checked. Verifier 2 adds that the joint
  induction may additionally need `WF S`, which `reachedByW3d_shadow` does NOT bind -- a FOURTH
  threaded premise if so (provider exists, `FullScope.lean:185`).
- **Whether the READ-side widening is still free at `shadow_admitEdge_agree`'s instantiation** (the
  live half of the third contradiction in `C7`). This is the only place the `abbrev`-reducibility
  argument was shown not to obviously apply.
- **Whether `Scratch4cii.lean`'s `shadowB` / `shadowB_correct` are CONSUMED as `UntaintedShadow`
  producers.** All three verifiers and the shadow report flagged the same gap and none closed it.
  Both are stated at the explicit two-disjunct lambda, so after the widening they keep type-checking
  and silently STOP deciding `UntaintedShadow`. `Scratch4cii.lean` is a live build target
  (`ZanzibarProofs.lean:86`), so a silently-retired `decide`-backed witness is exactly the house
  failure mode.
- **Whether the ~10 W3d2-twin shadow call sites all have `hTT`/`hTS` in scope.** Verified for the
  five `reachedByW3d_shadow` sites; NOT verified for `reachedByW3d2_shadow`'s. `TtuStarFree S T` is
  store-indexed, so the remove legs need a `T → T.erase t` weakening lambda -- the cost profile
  `UsStarWrite.lean`'s own docstring warns about for `W4Fragment.term`. Neither the body nor any
  report prices it.
- **Whether `inBridgeOccCount` as sketched in `C4` is PROVABLY the state's bridge count.** The risky
  conjunct is the release guard `inBridgeOnly`, a predicate on the LIVE edge list rather than the
  store. Treat the def as a PROPOSAL to be `#eval`-tested on `Sthru` before it enters any statement.
- **Whether `ReachedByW3d2E`'s / `ReachedByW3d2`'s `remove` constructor FIELDS are themselves stated
  over the unbridged removal.** If any is, that is a SIXTH false-statement site and nobody read it.
- **Whether `CascadeStrata.lean:1537::tvDer_edges_nil`'s terminal `rfl` survives the re-point.** It
  depends on the exact placement of the bridge step relative to the fold, which does not exist yet.
  Flagged FRAGILE, not broken.
- **Whether `formal/conformance/` already materializes in-bridges on the schemas `zcli`'s
  graph-state mode runs against** (`C6`'s `Cli.lean` seam). The seam is identified; whether it
  diverges is unknown.
- **Whether `GraphAdmission.usWild`'s seven `by decide` construction sites are non-vacuous.** If any
  fixture has `taintedKeys = []`, that discharge is vacuous. A `TK68` witness-quality question, not
  a step-8 soundness question, but nobody has pinned it the way `keysNonempty` is pinned.
- **The operational reachability of the rejected-BRIDGE divergence in `C5`(c).** The code-shape
  difference is real and read first-hand; no one constructed a write sequence that reaches
  `wildcard.py:274`.

### C9. What the ADDITIVE-FIRST re-plan landed, and the four toolbox-gap entries it corrected

*supersedes:* § Ordered steps' claim that *"the only commit-safe stopping points are the end of
step 2 and the end of step 15"*, and four entries in § Toolbox gaps.

(BUILT) **THE RE-PLAN.** The body treats steps 3-15 as one red run. It is not: a large slice of that
work is **purely additive** -- new lemmas about definitions that already exist, plus relocations that
change no statement -- and therefore lands GREEN and COMMITTABLE *before* any re-point. Executed
2026-09-13g: **18 declarations** (14 new, 4 relocated), whole-tree `lake build` rc=0
(`Build completed successfully (1089 jobs).`), audited CLEAN-ADDITIVE by an independent agent that
md5-matched the four moved blocks against `30ad44a` and diffed sixteen definition bodies to confirm
none moved. **So there is a THIRD commit-safe point, and future step-3b sessions should look for more
of them: ask of every owed lemma "does this mention a definition that already exists?" before
assuming it must wait for the re-point.**
  Landed additively: `UsStarWrite.lean::{ensureInBridges_nodes_incident, writeBridgedOne_nodesFromEdges,
  foldl_writeBridgedOne_nodesFromEdges}`; `Cascade.lean::{ensureInBridgesLogged_outbox_mono,
  bridgePreLogged_outbox_mono, ensureInBridgesLogged_edge_delta, bridgePreLogged_edge_delta,
  releaseInBridges_outbox, releaseInBridgesLogged_outbox_mono, releasePostLogged_outbox_mono,
  releaseInBridgesLogged_edges_subset, releasePostLogged_edges_subset,
  releaseInBridgesLogged_edge_delta, releasePostLogged_edge_delta}`; and the § Step 4 quartet
  (`runCascade_cases`, `foldl_writeDirect_schema`, `reconcileJobsD_schema`, `reachedByW3d_schema`)
  relocated into `Cascade.lean`.

(BUILT) **§ Toolbox gaps is WRONG about `foldl_writeBridgedOne_nodesFromEdges`, in the safe
direction.** The body predicts it "needs `edgesClosed` as a side condition. Do not assume it is a
copy." The landed statement is a **verbatim twin** of the `writeDirect` original -- one hypothesis,
no invariant -- so its 6+ consumers call it exactly like the original and carry NO extra obligation.
The body's *structural* diagnosis was right (`ensureInBridges` really does intern a `wAnyNode` with
no incident edge when its own `admitEdge` refuses) but its *refutation route* was the long way round:
`admitEdge a b = (a != b) && !σ.reach b a`, so a refusal is either `c = wAnyNode (c.type, c.pred)` --
impossible, `bridgedInConcrete_elim` gives `c.name ≠ STAR` -- or `reach w c = true`, and `reach_sound`
turns that into an `NReaches` whose first constructor argument IS the incident edge the goal wants.
`edgesClosed` would have bought a strictly weaker lemma by a strictly longer path.

(BUILT) **Three § Toolbox gaps entries were incomplete, each in a way that blocks the naive reading.**
  (a) family (iii) is unprovable as listed: `releaseInBridges_outbox` **did not exist** -- the unlogged
      core's projection family (`_schema`/`_nodes`/`_residue`/`_watermark`) never got an outbox member,
      and both `outbox_mono` proofs need it in BOTH branches. Landed `@[simp]`, tree still green with
      it in the simp set (corroborated: warning count identical at `88` before and after, so nothing
      in the global simp set shifted).
  (b) family (iv) is asymmetric -- it names the per-leg form on the write side and the epilogue form
      on the remove side. BOTH levels are needed at BOTH: `releasePostLogged_edge_delta` is unprovable
      without a per-leg `releaseInBridgesLogged_edge_delta`, and the re-pointed `writeLoggedOne`
      presents `bridgePreLogged`, not a single leg, so `bridgePreLogged_edge_delta` is what `:118`
      will actually call.
  (c) family (ii)'s recipe is longer than necessary: `releaseInBridgesLogged_edges` is already
      `@[simp]` and `releaseInBridges_edges_subset` already exists, so the logged leg is a two-line
      rewrite. The `removeEdgeOne_edges` + `List.mem_of_mem_erase` reasoning the body prescribes is
      needed only INSIDE `releaseInBridgesLogged_edge_delta`, where the goal is identifying WHICH
      edge was lost.

(READ, recorder) **`d.node = ab.2` is machine-checked now, not eyeballed.** The body's § Step 7
correction predicted that `writeLoggedRules_edge_delta` and `removeLoggedRules_edge_delta` survive
verbatim because the bridge arm emits at the TARGET. That is now discharged by proof on both sides,
and the orientation is consumed from `ensureInBridges_edges_mem` rather than read off the branch.

### Post-step-2 symbol table (as of 2026-09-13g)

**Anchored at commit `30ad44a`.** Taken from the symbol-table report; the recorder SPOT-CHECKED
`31` of these against `git show 30ad44a:…` this session and every one matched to the line. The
eight the task asked for, plus the rest, are named under the table. ⚠ Re-read the LINE-NUMBER
ANCHOR note at the head of this section before using any number: the working tree has already moved.

`UsStarWrite.lean` -- the file steps 1-2 rewrote, so nothing the body cites for it is usable:

```
:89   def Schema.isStarTuplesetThrough (S : Schema) (t p : String) : Bool
:115  def Schema.isSubjectWildcardUserset (S : Schema) (t p : String) : Bool   -- outer `p != BARE`
:146  def NoBridgedDerived (S : Schema) : Prop
:156  def GraphState.bridgedInConcrete (σ : GraphState) (c : NodeKey) : Bool   -- reads σ.schema
:166  theorem bridgedInConcrete_elim            -- MOVED here from UsStarCorrect.lean by step 1
:277  def GraphState.ensureInBridges (σ) (c : NodeKey) : GraphState            -- + presence guard
:338  def GraphState.bridgePre (σ) (t : Tuple) : GraphState
:344  def GraphState.writeBridgedOne (σ) (t : Tuple) : GraphState
:351  theorem ensureInBridges_schema            :363 ensureInBridges_residue
:383  theorem ensureInBridges_mono              (nodes)
:405  theorem ensureInBridges_edges_mono        -- MOVED here from UsStarClosure.lean by step 1
:417  theorem ensureInBridges_nodes_mem         -- MOVED here from UsStarClosure.lean by step 1
:438  theorem ensureInBridges_edges_mem         -- MOVED here from UsStarCorrect.lean by step 1
:467  theorem ensureInBridges_edges_of_mem      :483 ensureInBridges_count_le_one   -- THE real pin
:574  theorem structInv_ensureInBridges         :660 _outbox   :670 _watermark
:715  writeBridgedOne_schema  :722 _outbox  :728 _watermark  :735 _residue
:779  structInv_foldl_writeBridgedOne           :786 residueEmpty_foldl_writeBridgedOne
:793  inv_foldl_writeBridgedOne                 :802 quiescent_foldl_writeBridgedOne
:809  schema_foldl_writeBridgedOne
:865  theorem writeBridgedOne_edges_sound       -- 3 disjuncts
:916  theorem foldl_writeBridgedOne_edges_sound -- THE KEYSTONE, 3 disjuncts, START-state schema
:985  edgesClosed_ensureInBridges  :1127 edgesClosed_writeBridgedOne
:1162 edgesClosed_foldl_writeBridgedOne         -- BARE-predicate form, not StructInv
:1007 writeBridgedOne_edges_mono                :1021 foldl_writeBridgedOne_edges_mono
:1036 writeBridgedOne_monoNodes                 -- ⚠ `monoNodes`, NOT `nodes_mono`
:1051 foldl_writeBridgedOne_nodes_mono
:1072 writeBridgedOne_nodes_sound               :1102 foldl_writeBridgedOne_nodes_sound (widened)
:1291 BridgedWriteWitness.bridged_probe_refuses_the_cycle   -- ⚠ live :1412
:1298 BridgedWriteWitness.unbridged_probe_admits_the_cycle  -- ⚠ live :1419
```

`LeafRules.lean` and `Cascade.lean` -- (READ, recorder) NOT touched by steps 1-2, so these numbers
are also HEAD's:

```
LeafRules.lean :1 and :10 are the WHOLE import block (Leaf, RulesSound) -- no UsStarWrite
LeafRules.lean:118  def schemaRewritesL := schemaRewrites S ++ leafRewrites S
LeafRules.lean:324  def GraphState.writeRulesRaw     -- ⚠ the body's :255 cites are stale
LeafRules.lean:331  rewriteClosureL_rawWriteTuples_untaintedSchema   (pure list eq, unpinned)
LeafRules.lean:368  writeRulesRaw_untaintedSchema    (gate-pinned NAME; restate, never delete)
LeafRules.lean:380/:384/:388/:392/:396  structInv_ / residueEmpty_ / inv_ / quiescent_writeRulesRaw
                                        and writeRulesRaw_schema  (⚠ SUFFIX form)
LeafRules.lean:742  structure LeafScope (4 fields)   :1840 lrV_untainted_layer_silent
LeafRules.lean:1934 LeafRuleWitness.lrV_writeRulesRaw_edges_ne  (audited, `by decide`)
Cascade.lean :1, :8, :19 are the WHOLE import block (ReconcileDiff, LeafRules, UsStarWrite)
Cascade.lean:214 ensureInBridgesLogged  :225 inBridgeOnly  :246 releaseInBridges
Cascade.lean:253 releaseInBridgesLogged :464 releaseInBridges_edges_subset
Cascade.lean:505 bridgePreLogged        :518 writeLoggedOne (UN-re-pointed) :534 writeLoggedRules
Cascade.lean:639 writeBridgedOne_logged_evalEq   -- the re-point's body, already spelled out
Cascade.lean:654/:672/:689/:707  writeLoggedOne_ / foldl_writeLoggedOne_ / writeLoggedRules_evalEq
                                 and writeLoggedRules_watermark;  :701 bridgePreLogged_watermark
Cascade.lean:754 removeLoggedOne (UN-re-pointed)  :766 releasePostLogged
Cascade.lean:800/:806 releaseInBridges(Logged)_watermark; :812/:821/:830 removeLoggedOne_*;
                      :839/:843/:847 releasePostLogged_schema/nodes/watermark
Cascade.lean:1270 inductive ReachedByW3d (empty / write / cascade -- NO remove ctor)
Cascade.lean:1316/:1349/:1360 the three R-node declarations
Cascade.lean:272 namespace InBridgeLegWitness  :864 namespace BridgedLegWitness
```

The T1/T2/T3 chain and its providers -- (READ, recorder) all re-grepped this session:

```
BareStarCorrect.lean:44   BareStarStore      RulesBareStar.lean:43  TtuStarFree
RulesBareStar.lean:94/:98 StarSeed / starSeed_step   -- ⚠ both `private`, cannot be reused
RulesBareStar.lean:142    rewriteClosure_star_subject   :168 rewriteClosure_star_bare
TtuStarWide.lean:72       TtuStarFreeW       RulesCorrect.lean:184  TtuTuplesetsDirect
RulesWrite.lean:86        applyRRule (:90 is the ttu arm)    Leaf.lean:793  rawWriteTuples
ReconcileCorrect.lean:34  ComputedOnly       :652 TtuTargetsSat      Leaf.lean:635 NotLeafName
CascadeStable.lean:1028   DirectRestrictionsNotLeaf     :551 DerNode   :568 structure ShadowOver
CascadeStable.lean:1774   reachedByW3d_shadow (1 named + 8 ARROW hypotheses)
CascadeStrataSettle.lean:809 reachedByW3d2_shadow   :1448 reachedByW3d2_shadow_d  (⚠ not :1348)
FullScope.lean:188 GraphAdmission.ttuDirect   :192 usWild   :260 GraphAdmission.noBridgedDerived
FullScope.lean:339 W4Fragment.bareStar        :340 W4Fragment.ttuStarFree
CascadeStrata.lean:694 edgeOfTuple  :709 untOccCount  :952 reachedByW3d2_untOccCount (the 5th)
RemoveOccCount.lean:118/:143/:204/:220 -- the file's FOUR theorems, complete
```

**The spot-checks the recorder ran first-hand this session** (`git show 30ad44a:<file>` plus a live
grep, both compared): `foldl_writeBridgedOne_edges_sound`, `bridgedInConcrete_elim`,
`GraphState.ensureInBridges`, `ensureInBridges_edges_mono`, `schema_foldl_writeBridgedOne`,
`edgesClosed_foldl_writeBridgedOne`, `foldl_writeBridgedOne_nodes_sound`,
`bridged_probe_refuses_the_cycle`, `ensureInBridges_count_le_one`, `NoBridgedDerived`,
`GraphState.writeBridgedOne`, `GraphState.writeRulesRaw`, `GraphState.writeLoggedOne` -- thirteen,
plus the whole T1/T2/T3 provider block above, both import blocks, and the `reachedByW3d_shadow`
signature. All matched the symbol-table report except the two witness pins, whose drift is the
LINE-NUMBER ANCHOR note.

## How this plan was made, and what that is worth

Eight read-only agents each owned one file or family (`Cascade`, `LeafRules`,
`CascadeStable`, the R-node twins, the step-3a toolbox, the downstream tail, the Python
fidelity surface, the still-owed list) and returned verdicts plus `file:line::symbol`
evidence; a ninth reconciled them. **A subagent report is evidence, not a finding**
(`CLAUDE.md` § Delegation). What the session verified first-hand before landing this file,
rather than taking on report: the nonexistence of `ensureInBridges_idem`; `LeafRules.lean`'s
import block; `writeRulesRaw_schema` vs the reported `schema_writeRulesRaw`; the two
headlines' pinned statements and their binder lists in `formal/headline_statements.txt`;
`Cascade.lean`'s import block; the four move-candidates' locations; and the absence of
`foldl_writeBridgedOne_edges_sound`. All seven held.

**Two items in this plan are NOT first-hand verified and are flagged in place**: the
`47`-vs-`49` cone count (last entry of § Corrections — immaterial to the work, both figures
name the same 12 files) and the Python-fidelity report's two `inferred` claims about
`_maybe_remove_bridges`. Confirm the latter against the Python before writing either into a
tracked file.

## Verdict

NO — step 3b does not finish in one session, and the honest split is roughly three. Measured fallout is 40 errors in 3 modules (LeafRules 6→7, Cascade 7→10, CascadeStable 27/18 decls). Beyond that the build stops at CascadeStable, and the downstream set is exactly 19 modules (measured first-hand by reverse-import BFS over `^import ZanzibarProofs`: CascadeSettle, CascadeInv, CascadeEnum, CascadeStrata, CascadeStrataSettle/Resettle/Enum/Assemble/Inv/Edge, RemoveOccCount, RemoveConfluence, Equiv, FullScope, Exec, TtuStarWide, Cli, Scratch4cii, Audit). Five independent statement families go FALSE, not the brief's two. Realistic size 190-260 declarations. What IS achievable in one session and leaves a green tree: steps 1-6 below (toolbox + both re-points + LeafRules + Cascade + the R-node restatement), stopping before CascadeStable — but note that leaves the build RED at CascadeStable onward, so it is a known-intermediate, not a commit point. The only commit-safe stopping points are step 2 (purely additive, still inert) and the end.

THE SINGLE SCHEDULE RISK: the `ShadowOver` / `UntaintedShadow` widening in CascadeStable.lean (step 8). It is 11 of the 27 measured errors, it is the ONE piece with no landed design, it sits in module 1 of the 19 downstream so nothing past it can be walked until it closes, and its worst case is minting a new `GraphAdmission` field — which is a TK68-sized job (47 decls / 12 files) bolted onto 3b. My adjudication (step 8) is that it probably does NOT need a new field, because `reachedByW3d_shadow` (CascadeStable.lean:1774) already binds `DirectRestrictionsNotLeaf S`, `TtuTargetsSat S NotLeafName` and `BareStarStore T` — but that must be PROVEN before step 8 starts, not assumed. CascadeStrataSettle.lean (5327 lines, 139 decls, 375 write-leg refs) is the volume risk behind it, not the stall risk.

## Blockers — read all six before step 1

- HEADLINE BLOCKER, decide before starting: `runCascade2_no_abort` (CascadeStrata.lean:1743) and `cascade2_drains` (:1911) ARE statement-pinned (formal/headline_statements.txt rows 7 and 8 of 51) and ARE in verify.sh's HEADLINE_AUDITS, they consume `reachedByW3d2_Rnode_not_source` at :1793/:1842, and their binder lists carry NO GraphAdmission bundle -- I read both signatures in full. FullScope.lean:260::GraphAdmission.noBridgedDerived is one module downstream and cannot be projected there. So the brief's standing claim 'NO HEADLINE GAINS A HYPOTHESIS' is FALSE: two statement-pinned headlines gain `(hNBD : NoBridgedDerived S)` and headline_statements.txt moves by two rows. Legal and honest, but a deliberate reviewed change requiring the RED-first / --generate / dated formal/history/ note sequence. Discovering this at the end of a session is what kills it.

- IMPORT-CONE BLOCKER: Cascade.lean's transitive cone is 36 modules and contains NEITHER GraphIndex.UsStarCorrect NOR GraphIndex.UsStarClosure (measured first-hand by BFS over `^import ZanzibarProofs`). So `ensureInBridges_edges_mem` (UsStarCorrect.lean:61), `bridgedInConcrete_elim` (UsStarCorrect.lean:37), `ensureInBridges_edges_mono` (UsStarClosure.lean:133) and `ensureInBridges_nodes_mem` (UsStarClosure.lean:145) are ALL invisible from Cascade/CascadeStable/LeafRules. One recon report's proposed proof of the restated `reachedByW3d_edge_source_ne_R` uses two of them directly from Cascade.lean and DOES NOT COMPILE. They must be MOVED up into UsStarWrite.lean (step 1) -- not duplicated, because Equiv.lean imports UsStarClosure and transitively UsStarWrite, so a duplicate collides there.

- IMPORT-ORDER BLOCKER: the restated `reachedByW3d_edge_source_ne_R` (Cascade.lean:1316) needs `σ.schema = S` to convert the bridge's state-keyed `isSubjectWildcardUserset` claim into a claim about S, but `reachedByW3d_schema` is at CascadeSettle.lean:77 -- DOWNSTREAM of Cascade via CascadeStable -- as are its helpers `foldl_writeDirect_schema` (CascadeSettle.lean:50), `reconcileJobsD_schema` (CascadeSettle.lean:60) and `runCascade_cases` (CascadeStable.lean:166). Relocate all four up into Cascade.lean (step 4) before attempting step 6. The W3d2 side is unaffected: `reachedByW3d2_schema` is in-file at CascadeStrata.lean:455.

- IMPORT BLOCKER: `import ZanzibarProofs.GraphIndex.UsStarWrite` is ABSENT from LeafRules.lean (its whole import block is `:1 Leaf` and `:10 RulesSound`), so re-point #2 does not even elaborate. It is a FOURTH mandatory edit, not one of the brief's three. Verified acyclic first-hand: LeafRules' cone is 18 modules with no UsStarWrite; UsStarWrite's cone is 9 modules with no LeafRules.

- DESIGN BLOCKER (the schedule risk): the `ShadowOver`/`UntaintedShadow` widening has no landed design and gates all 19 downstream modules. Three `term` obligations arise from widening the extras predicate; two of the three have no written-down provider. My adjudication is that all three discharge from hypotheses `reachedByW3d_shadow` (CascadeStable.lean:1774) ALREADY binds -- `NoBridgedDerived` for DerNode, `DirectRestrictionsNotLeaf S` + `TtuTargetsSat S NotLeafName` for LeafNode, `BareStarStore T` for the new BridgeNode disjunct -- but that must be PROVEN on paper before step 8 starts. If it fails, 3b acquires a new GraphAdmission field, which is a TK68-sized job (47 decls / 12 files) bolted on.

- ASSURANCE BLOCKER: the step-3a `decide` witnesses (Cascade.lean:864-919 BridgedLegWitness, :272 InBridgeLegWitness) are all stated on bridgePreLogged / bridgePre / writeBridgedOne / releasePostLogged DIRECTLY, never through writeLoggedOne or removeLoggedOne -- I checked every theorem in the block. So neither re-point can flip any of them, and the composition itself gains ZERO red-to-green evidence. Per docs/sabotage-procedure.md, add one `decide` pin stated on the re-pointed round trip before believing the composition landed.

## Ordered steps

The import chain is effectively linear (`LeafRules → Cascade → CascadeStable →
CascadeSettle → …`), so the fix is walked forward one module at a time and **a mutation
upstream stops the build before downstream declarations are elaborated** — every error count
below means "at least these", never "only these".

**The only commit-safe stopping points are the end of step 2 and the end of step 15.**
Steps 3–14 leave the build RED at `CascadeStable` onward; that is a known-intermediate, not
a resume point.

### Step 1. Import-cone prep: MOVE four bridge lemmas up into UsStarWrite.lean

*files:* `formal/lean/ZanzibarProofs/GraphIndex/UsStarWrite.lean`, `formal/lean/ZanzibarProofs/GraphIndex/UsStarCorrect.lean`, `formal/lean/ZanzibarProofs/GraphIndex/UsStarClosure.lean`  
*build after:* yes

MEASURED FIRST-HAND THIS SESSION: Cascade.lean's transitive import cone is 36 modules and contains GraphIndex.UsStarWrite but NOT GraphIndex.UsStarCorrect and NOT GraphIndex.UsStarClosure. Every proposal in the recon that reaches for a UsStarCorrect/UsStarClosure lemma from Cascade.lean therefore does not compile. Fix once, first.

MOVE (cut, do not copy -- Equiv.lean imports UsStarClosure AND transitively UsStarWrite, so a duplicate collides there):
  (a) UsStarCorrect.lean:37::bridgedInConcrete_elim  ->  UsStarWrite.lean, just after GraphState.bridgedInConcrete (:156). Statement: `c.variant = Variant.plain AND c.name != STAR AND c.pred != BARE AND σ.schema.isSubjectWildcardUserset c.type c.pred = true` from `σ.bridgedInConcrete c = true`.
  (b) UsStarCorrect.lean:61::ensureInBridges_edges_mem  ->  UsStarWrite.lean beside ensureInBridges_edges_of_mem (:389). Statement: `{σ} {c} {e} (he : e ∈ (σ.ensureInBridges c).edges) : e ∈ σ.edges ∨ (e = (c, wAnyNode (c.type, c.pred)) ∧ σ.bridgedInConcrete c = true)`.
  (c) UsStarClosure.lean:133::ensureInBridges_edges_mono  ->  UsStarWrite.lean beside ensureInBridges_mono (:363). Statement: `{σ} {c} {e} (he : e ∈ σ.edges) : e ∈ (σ.ensureInBridges c).edges`.
  (d) UsStarClosure.lean:145::ensureInBridges_nodes_mem  ->  same place. Statement: `{σ} {c k} (hk : k ∈ (σ.ensureInBridges c).nodes) : k ∈ σ.nodes ∨ k = wAnyNode (c.type, c.pred)`.

WHY MOVE RATHER THAN IMPORT: adding `import ZanzibarProofs.GraphIndex.UsStarClosure` to Cascade is cycle-free but drags the ObjStarClosure + UsStarCorrect + Spec.WellDef cones in for four ten-line lemmas. UsStarClosure imports UsStarCorrect imports (transitively) UsStarWrite, so all four existing use sites (UsStarClosure.lean:195, :222, :224, :400) keep resolving after the move with zero call-site edits.

PIN CHECK BEFORE MOVING: `bridgedInConcrete_elim` is audited. Grep `formal/audited_theorems.txt` and `Audit.lean` for it; a MOVE keeps the name so verify.sh step 4a stays green, but check formal/CORRESPONDENCE.md does not anchor it as `UsStarCorrect.lean::bridgedInConcrete_elim` -- step 4d resolves `file::symbol` by PARSE, so a moved decl with a file-qualified anchor REDS the lean phase until the anchor is updated.

ALSO FIX HERE (stale trap, verified 0 declarations tree-wide): UsStarWrite.lean:255 and UsStarCorrect.lean:60 both cite a pin called `ensureInBridges_idem` -- `grep -rn ensureInBridges_idem formal/` returns exactly two hits, both inside docstrings. No such theorem has ever existed. The real pin is `ensureInBridges_count_le_one` (UsStarWrite.lean:405). Correct both docstrings; an unenforceable trap citing a nonexistent symbol is worse than none.

### Step 2. Land the missing bridged fold toolbox in UsStarWrite.lean (purely additive, still inert)

*files:* `formal/lean/ZanzibarProofs/GraphIndex/UsStarWrite.lean`  
*build after:* yes

This is the single largest mechanical block and EVERY later module needs it. Land it before touching any definition, so the tree stays green and committable through this step.

NAME COLLISION SWEEP DONE: all names below return 0 hits tree-wide. ⚠ `writeLoggedOne_watermark` and `writeLoggedOne_outbox_mono` ARE taken -- they live at CascadeStable.lean:70 and :78, NOT in Cascade.lean. Grep BOTH files before minting anything in the writeLoggedOne_* / writeLoggedRules_* families.

THE KEYSTONE (nothing of this shape exists; `foldl_writeDirect_edges_sound` is at RulesCorrect.lean:98 with only TWO disjuncts, verified verbatim):
  theorem foldl_writeBridgedOne_edges_sound (us : List Tuple) :
      ∀ {σ : GraphState} {a b : NodeKey},
        (a, b) ∈ (us.foldl (fun acc u => acc.writeBridgedOne u) σ).edges →
        (a, b) ∈ σ.edges ∨
          (∃ u ∈ us, a = subjNode u.subject ∧ b = objNode u.object u.relation) ∨
          (σ.schema.isSubjectWildcardUserset a.type a.pred = true ∧
            b = wAnyNode (a.type, a.pred))
The third disjunct is keyed on the FOLD'S START-STATE schema and needs no existential, because `bridgedInConcrete` reads only σ.schema (UsStarWrite.lean:156) and the schema is fold-invariant by `schema_foldl_writeBridgedOne` (:731). That is what makes it usable at every downstream site. Also land the one-step `writeBridgedOne_edges_sound` in the same shape.

THE REST (copy the writeDirect originals named, widen where noted):
  foldl_writeBridgedOne_edges_mono   -- mirror RulesComplete.lean:39; one-step core is the moved ensureInBridges_edges_mono
  foldl_writeBridgedOne_nodes_mono   -- mirror CascadeStable.lean:177; core is ensureInBridges_mono (:363)
  foldl_writeBridgedOne_nodes_sound  -- mirror CascadeStable.lean:192, STATEMENT WIDENS: `k ∈ σ.nodes ∨ ∃ u ∈ us, k = subjNode u.subject ∨ k = objNode u.object u.relation ∨ k = wAnyNode ((subjNode u.subject).type, (subjNode u.subject).pred) ∨ k = wAnyNode ((objNode u.object u.relation).type, (objNode u.object u.relation).pred)`
  edgesClosed_writeBridgedOne / edgesClosed_foldl_writeBridgedOne -- mirror CascadeStable.lean:213, BARE-PREDICATE form (`∀ ab ∈ .edges, ab.1 ∈ .nodes ∧ ab.2 ∈ .nodes`), NOT StructInv; four CascadeStable proofs call the writeDirect version directly. TRUE and easy: ensureInBridges interns wAnyNode via addNode on EVERY bridged branch before it can add the edge, and bridgePre addNodes both endpoints first -- reuse structInv_ensureInBridges' shape (:496).

⚠ NON-TRIVIAL, budget separately: `foldl_writeBridgedOne_nodesFromEdges` (twin of CascadeStrataSettle.lean:681). The reject branch of writeBridgedOne returns the ORIGINAL σ so bridgePre's addNodes are discarded -- good. But the accept branch can intern wAnyNode with NO incident edge (the `admitEdge` fails sub-branch of ensureInBridges). That case is refutable only via edgesClosed (an isolated wAnyNode reaches nothing, so admitEdge cannot fail), so this lemma needs edgesClosed as a side condition. Do not assume it is a copy.

DEFER: `foldl_writeBridgedOne_edge_complete` (RulesComplete.lean:87 twin) -- it is stated over `FoldAdmits`, which is the step-14 honesty question. Land it only once FoldAdmitsB exists, or state it over the bridged probe directly.

### Step 3. RE-POINT #2 + the mandatory LeafRules import + 7 LeafRules declarations

*files:* `formal/lean/ZanzibarProofs/GraphIndex/LeafRules.lean`  
*build after:* yes

⚠ THE BRIEF SAYS 'EXACTLY THREE RE-POINTS'. IT IS FOUR EDITS. LeafRules.lean's entire import block is two lines (`:1 import ZanzibarProofs.GraphIndex.Leaf`, `:10 import ZanzibarProofs.GraphIndex.RulesSound`); UsStarWrite is absent, so `writeBridgedOne` is not in scope and re-point #2 does not elaborate. MEASURED: LeafRules' cone is 18 modules with no UsStarWrite; UsStarWrite's cone is 9 modules (Core.Ident/Refs/Schema/Store, Spec.Stratify, GraphIndex.Closure/State/Write/ObjStarWrite) with no LeafRules. NO CYCLE. Extend the existing cycle-justification comment at LeafRules.lean:2-9 rather than adding a bare import line.

RE-POINT #2 (LeafRules.lean:324-325, two tokens):
  BEFORE:  (rewriteClosureL S (rawWriteTuples S t)).foldl (fun acc u => acc.writeDirect u) σ
  AFTER:   (rewriteClosureL S (rawWriteTuples S t)).foldl (fun acc u => acc.writeBridgedOne u) σ
No signature change: `GraphState.writeDirect (σ) (t) : GraphState` (Write.lean:77) and `GraphState.writeBridgedOne (σ) (t) : GraphState` (UsStarWrite.lean:324) have identical arity and explicitness.

FIVE MECHANICAL COROLLARIES -- statements UNCHANGED, proof term is a one-name swap. All five twins verified present with byte-identical binder shape and NO extra premise:
  :380 structInv_writeRulesRaw    `structInv_foldl_writeDirect _ h`   -> `structInv_foldl_writeBridgedOne _ h`    (twin UsStarWrite.lean:701)
  :384 residueEmpty_writeRulesRaw `residueEmpty_foldl_writeDirect _ h`-> `residueEmpty_foldl_writeBridgedOne _ h` (twin :708)
  :388 inv_writeRulesRaw          `inv_foldl_writeDirect _ h hre`     -> `inv_foldl_writeBridgedOne _ h hre`      (twin :715)
  :392 quiescent_writeRulesRaw    `quiescent_foldl_writeDirect _ h`   -> `quiescent_foldl_writeBridgedOne _ h`    (twin :724)
  :396 writeRulesRaw_schema       `schema_foldl_writeDirect _`        -> `schema_foldl_writeBridgedOne _`         (twin :731)
⚠ THE FIFTH IS `writeRulesRaw_schema`, SUFFIX FORM -- the brief's `schema_writeRulesRaw` does not exist (0 hits). Its four downstream consumers (CascadeStrata.lean:461, CascadeStrataAssemble.lean:467, CascadeStrataSettle.lean:2491, :4594) stay green because the statement does not move.

THE ONE RESTATEMENT (LeafRules.lean:368::writeRulesRaw_untaintedSchema). ⚠ ITS NAME IS GATE-PINNED (audited_theorems.txt:616 + `#print axioms` at Audit.lean:1986) and it has ZERO applications tree-wide -- so KEEP THE NAME, move only the statement; deleting it reds verify.sh step 4a:
  theorem writeRulesRaw_untaintedSchema {σ : GraphState} {S : Schema} {t : Tuple}
      (h : ∀ d ∈ S.defs, isDerived S d.1 = false) :
      σ.writeRulesRaw S t
        = (rewriteClosure S t).foldl (fun acc u => acc.writeBridgedOne u) σ := by
    unfold GraphState.writeRulesRaw
    rw [rewriteClosureL_rawWriteTuples_untaintedSchema h]
The proof DROPS `GraphState.writeRules` from the unfold. Do NOT try to keep `= σ.writeRules S t` under any added premise: it is false for the FUEL reason, not the bridging reason -- `bridgePre`'s two unconditional `addNode`s shift `GraphState.reach`'s fuel (`nodes.length + 1`, State.lean:160), so the admission probe reads a different state even where nothing is bridged. The file's own docstring at :355-358 already says this. The surviving mathematical content is LeafRules.lean:331::rewriteClosureL_rawWriteTuples_untaintedSchema, which is a pure LIST equation, names no GraphState, is unpinned, and needs no edit.

THE 7TH DECLARATION THE BRIEF OMITS: LeafRules.lean:1934::LeafRuleWitness.lrV_writeRulesRaw_edges_ne, `((emptyState SlV).writeRulesRaw SlV tlEditor).edges ≠ ((emptyState SlV).writeRules SlV tlEditor).edges := by decide`, audited at audited_theorems.txt:37 / Audit.lean:2001. Should stay TRUE (SlV carries no wildcard restriction and no TTU, so isSubjectWildcardUserset is false on every key and ensureInBridges is the identity) but `decide`'s kernel reduction now evaluates bridgedInConcrete per fold step per endpoint -- budget a possible maxHeartbeats bump.

STALE CITE TO FIX WHILE HERE: Cascade.lean:4, :531 and :687 all cite `GraphState.writeRulesRaw (LeafRules.lean:255)`; the live def is :324.

### Step 4. Relocate the schema-preservation quartet UP into Cascade.lean (unblocks step 6)

*files:* `formal/lean/ZanzibarProofs/GraphIndex/Cascade.lean`, `formal/lean/ZanzibarProofs/GraphIndex/CascadeSettle.lean`, `formal/lean/ZanzibarProofs/GraphIndex/CascadeStable.lean`  
*build after:* yes

IMPORT-ORDER BLOCKER, and it is invisible unless you plan for it. The restated `reachedByW3d_edge_source_ne_R` (step 6) must convert `σ'.schema.isSubjectWildcardUserset dt R = true` into a claim about `S`, because bridging is keyed on the STATE's schema (`GraphState.bridgedInConcrete` reads `σ.schema`, UsStarWrite.lean:156-157). The lemma that supplies `σ.schema = S` is `reachedByW3d_schema` at CascadeSettle.lean:77 -- DOWNSTREAM of Cascade via CascadeStable. So are its helpers.

MOVE, keeping every name (both files import Cascade, so there are no call-site edits and no collisions):
  CascadeSettle.lean:50::foldl_writeDirect_schema   -> Cascade.lean
  CascadeSettle.lean:60::reconcileJobsD_schema      -> Cascade.lean
  CascadeStable.lean:166::runCascade_cases          -> Cascade.lean
  CascadeSettle.lean:77::reachedByW3d_schema        -> Cascade.lean (after the ReachedByW3d inductive at :1272)
Note `foldl_writeDirect_schema` will need its bridged sibling `schema_foldl_writeBridgedOne` (UsStarWrite.lean:731) in the moved proof of `reachedByW3d_schema`'s write case -- Cascade already imports UsStarWrite (:19), so it resolves.

ALTERNATIVE IF THE MOVE FIGHTS BACK: strengthen the induction motive of reachedByW3d_edge_source_ne_R in place to `σ.schema = S ∧ (…)`. Rejected as the default because it still needs a schema-preservation fact for `reconcileJobsD`, i.e. the same helper, and it duplicates an existing theorem.

THE W3d2 SIDE NEEDS NOTHING: `reachedByW3d2_schema` is in-file at CascadeStrata.lean:455, 1141 lines before its use at :1596.

⚠ CORRESPONDENCE.md anchor check after any move: verify.sh step 4d resolves `file::symbol` by parse. If any of these four is anchored with its old file, the lean phase reds until the map is updated.

### Step 5. RE-POINT #1 and #3 in Cascade.lean + the 7 mechanical repairs

*files:* `formal/lean/ZanzibarProofs/GraphIndex/Cascade.lean`  
*build after:* yes

DO RE-POINT #3 FIRST -- it is fully closed in 4 lines of tactic edit and `ReachedByW3d` has NO remove constructor (write and cascade only, Cascade.lean:1272-1275), so it has ZERO effect on the W3d closure theory.

RE-POINT #3 (Cascade.lean:755-758), wrap ONLY the then-branch:
  if (subjNode t.subject, objNode t.object t.relation) ∈ σ.edges
  then ((σ.removeEdgeOne (subjNode t.subject) (objNode t.object t.relation)).pushDelta
    (objNode t.object t.relation) t.relation true).releasePostLogged t
  else σ
The wrap goes OUTSIDE pushDelta, matching Python's `_remove_tuple_trusted`: `remove_edge_by_id` (which emits) THEN `_maybe_remove_bridges` on both endpoints (index_v4/wildcard.py:566-570). The else-branch stays bare `σ` -- Python raises AdmissionRejected out of remove_edge_by_id (index_v4/core.py:1135-1141) and never reaches the release pair, so a removal that found no edge must not run the epilogue.
THEN the trio, statements UNCHANGED, dischargers pre-paid by 3a:
  :812 removeLoggedOne_schema    prepend `rw [releasePostLogged_schema]`    (Cascade.lean:839, @[simp])
  :821 removeLoggedOne_nodes     prepend `rw [releasePostLogged_nodes]`     (:843, @[simp]) -- still TRUE, releaseInBridges uses removeEdgeOne and this model has no node GC (:238-245 records it as a deliberate model/Python gap)
  :830 removeLoggedOne_watermark prepend `rw [releasePostLogged_watermark]` (:847, @[simp])
The `removeLoggedRules` folds at :927/:938/:950 are `exact removeLoggedOne_*` one-liners and need no edit.

RE-POINT #1 (Cascade.lean:519-521). DO NOT INVENT THE BODY -- 3a already pinned it. Write EXACTLY the LHS of `writeBridgedOne_logged_evalEq` (Cascade.lean:639-644):
  if (σ.bridgePreLogged t).admitEdge (subjNode t.subject) (objNode t.object t.relation)
  then ((σ.bridgePreLogged t).addEdge (subjNode t.subject)
    (objNode t.object t.relation)).pushDelta (objNode t.object t.relation) t.relation true
  else σ
Note `addEdge`, NOT `writeDirect`: the admission guard moves out to the `if`. Leaving writeDirect would re-probe admitEdge on the UNBRIDGED σ and double-guard against the wrong state.

THE FOUR EVALEQ REPAIRS (the brief calls these '6 mechanical'; all SEVEN Cascade-local names it lists are mechanical and each has an existing 3a discharger):
  :654 writeLoggedOne_evalEq -- STATEMENT CHANGES. RHS moves `σ.writeDirect t` -> `σ.writeBridgedOne t`. Proof becomes `by unfold GraphState.writeLoggedOne; exact writeBridgedOne_logged_evalEq h t`.
  :672 foldl_writeLoggedOne_evalEq -- STATEMENT CHANGES. Second fold's body `acc.writeDirect u` -> `acc.writeBridgedOne u`. Proof body UNCHANGED (`exact ih (writeLoggedOne_evalEq h u)` at :681).
  :689 writeLoggedRules_evalEq -- EXPECT ZERO EDIT. Its statement mentions `σ.writeRulesRaw S t`, which after re-point #2 IS the restated foldl lemma's RHS definitionally. If it errors, the fix is `by unfold GraphState.writeRulesRaw; exact foldl_writeLoggedOne_evalEq …`. This statement staying verbatim is what makes every CascadeStable repair a lemma substitution under an unchanged `rw [(writeLoggedRules_evalEq …).edges]`.
  :707 writeLoggedRules_watermark -- statement UNCHANGED; only the tactic block at :716-719 breaks. Replace `rw [pushDelta_watermark, writeDirect_watermark]` with `rw [pushDelta_watermark]; exact bridgePreLogged_watermark σ u` (Cascade.lean:701, @[simp]). ⚠ There is NO `addEdge_watermark` lemma anywhere (0 hits; State.lean:797-803 has addEdge_nodes/residue/schema/edges but no watermark twin), so this cannot be a pure `rw` chain of named lemmas -- use `simp [GraphState.addEdge]` or an rfl step.

NEW RED-TO-GREEN EVIDENCE OWED HERE: the step-3a `decide` witnesses (BridgedLegWitness, Cascade.lean:864-919, and InBridgeLegWitness at :272) are all stated on bridgePreLogged / bridgePre / writeBridgedOne / releasePostLogged DIRECTLY, never through writeLoggedOne or removeLoggedOne. So neither re-point can flip them and the composition gains NO evidence from them. Per docs/sabotage-procedure.md, add ONE `decide` pin stated on the re-pointed `writeLoggedOne`/`removeLoggedOne` round trip -- that is the composition's only red-to-green arm.

### Step 6. THE R-NODE RESTATEMENT in Cascade.lean -- carry threaded, conclusion type-carrying

*files:* `formal/lean/ZanzibarProofs/GraphIndex/Cascade.lean`  
*build after:* yes

⚠ MATERIAL CORRECTION TO THE BRIEF, VERIFIED FIRST-HAND: only TWO of the four theorems go false, not two PAIRS. Both `*_Rnode_not_source` STATEMENTS stay TRUE. Their node is `objNode ⟨dt, on⟩ R`, whose `.type` is `dt` (State.lean:145::objNode_type, @[simp]) and `.pred` is `R` (:147::objNode_pred) -- i.e. exactly the key `hder` says is derived, which is exactly the key `NoBridgedDerived` un-bridges. The type-index escape hatch ([x:*#R] at an untainted (x,R)) cannot reach a node whose type is pinned to `dt`. They gain a hypothesis, NOT a restatement. That halves the work and means 10 of the 12 application sites consume an UNCHANGED conclusion.

APPLICATION-SITE CENSUS, RE-RUN AND CONFIRMED THIS SESSION (exactly 12):
  reachedByW3d_edge_source_ne_R: 1  -> Cascade.lean:1356
  reachedByW3d_Rnode_not_source: 2  -> Cascade.lean:1369, CascadeSettle.lean:595
  reachedByW3d2_edge_source_ne_R: 1 -> CascadeStrata.lean:1674
  reachedByW3d2_Rnode_not_source: 8 -> CascadeStrata.lean:1793, :1842; CascadeStrataEdge.lean:307; CascadeStrataResettle.lean:922, :975, :2233, :2283; CascadeStrataSettle.lean:2361

EDIT 1 -- Cascade.lean:1316, RESTATE:
  theorem reachedByW3d_edge_source_ne_R {σ : GraphState} {S : Schema} {T : Store}
      {dt R : String} (hRne : R ≠ BARE) (hNBD : NoBridgedDerived S)
      (h : ReachedByW3d σ S T) :
      isDerived S (dt, R) = true → NoTtuTarget S R → NoStoreSubjectR T R →
        ∀ a b, (a, b) ∈ σ.edges → a.type = dt → a.pred ≠ R
Prefer the arrow form over `¬(a.type = dt ∧ a.pred = R)`: equivalent, but it needs no And-destructuring at the consumer. `NoBridgedDerived` resolves unqualified -- it is at UsStarWrite.lean:146 in `namespace Zanzibar`, and Cascade.lean:19 already imports UsStarWrite. NO import edit.
PROOF: the two existing cases (:1322 empty, :1338 cascade) ignore the new hypothesis unchanged. The `@write` case (:1326-1333) currently does `rw [(writeLoggedRules_evalEq …).edges]; unfold GraphState.writeRulesRaw; rcases foldl_writeDirect_edges_sound …`. Swap in `foldl_writeBridgedOne_edges_sound` from step 2. Disjuncts 1 and 2 reuse the existing proof VERBATIM (`ih`, and `rewriteClosureL_subject_pred_ne_of_noTtuTarget`). Disjunct 3 is killed by: substitute `a.type = dt` and `a.pred = R` into `σp.schema.isSubjectWildcardUserset a.type a.pred = true`, rewrite `σp.schema = S` by the relocated `reachedByW3d_schema` (step 4), and contradict `hNBD dt R hder : S.isSubjectWildcardUserset dt R = false`.

EDIT 2 -- Cascade.lean:1349, HYPOTHESIS ONLY, conclusion unchanged:
  add `(hNBD : NoBridgedDerived S)` after `(hRne : R ≠ BARE)`; change :1356 from
    `… hy (objNode_pred ⟨dt, on⟩ R)`  to  `… hy (objNode_type ⟨dt, on⟩ R) (objNode_pred ⟨dt, on⟩ R)`
⚠ Do NOT add hterm-shaped store-indexed premises -- the standing bound forbids widening W4Fragment.term (134 sites, 22 weakening lambdas). `NoBridgedDerived S` mentions no Store, so it passes through a `write` (`t :: T`) or `remove` (`T.erase t`) leg VERBATIM with no weakening lambda anywhere. Put it immediately after `hRne` at every threading site for a uniform argument position.

EDIT 3 -- Cascade.lean:1360::reconcileJobsL_Rnode_not_source, NOT IN THE BRIEF'S FALLOUT LIST: it applies the above at :1369, so it gains and forwards the same carry. Its own cascade-edge arm (:1370-1374) is unaffected. Budget Cascade.lean at ~10 errors, not 7.

LEGALITY CONFIRMED FIRST-HAND: `reachedByW3d_edge_source_ne_R` is audited_theorems.txt:413, `reachedByW3d_Rnode_not_source` :411, `reachedByW3d2_Rnode_not_source` :393; `reachedByW3d2_edge_source_ne_R` is in NO pin file. None of the four appears in headline_statements.txt or headline_definitions.txt. So only the NAMES are pinned -- restate freely, never rename or delete. Audit.lean:714/715/974 carry `#print axioms` rows keyed on the NAME and survive untouched; all three must stay axiom-standard after the restatement.

DOCSTRING FIX: UsStarWrite.lean:131-132 states the cone as '1 + 2 + 8' = 11 application sites. It omits `reachedByW3d2_edge_source_ne_R`'s site (CascadeStrata.lean:1674). The true count is 12.

### Step 7. CascadeStable -- the 13 mechanical repairs and 2 restatements OUTSIDE the shadow

*files:* `formal/lean/ZanzibarProofs/GraphIndex/CascadeStable.lean`, `formal/lean/ZanzibarProofs/GraphIndex/Cascade.lean`  
*build after:* no

⚠ The brief's '27 errors across 17 declarations' is off by one: the named list is 18 DISTINCT declarations (lines 70, 78, 107, 118, 254, 292, 362, 485, 506, 1260, 1354, 1411, 1969, 2002, 2309, 2337, 2363, 2586) and the per-name counts sum to exactly 27. Error count right, decl count wrong.

STATEMENTS UNCHANGED, substitute the step-2 twins:
  :70  writeLoggedOne_watermark      -- `unfold GraphState.writeLoggedOne; split; · simp [bridgePreLogged_watermark]; · rfl`
  :107 writeLoggedRules_edges_mono   -- swap foldl_writeDirect_edges_mono -> foldl_writeBridgedOne_edges_mono
  :254 reachedByW3d_edgesClosed      -- AUDITED. only the @write arm; swap edgesClosed_foldl_writeDirect -> edgesClosed_foldl_writeBridgedOne
  :485 affectedObjects_writeLeg_mono -- swap foldl_writeDirect_nodes_mono/_edges_mono for the bridged twins. Bridging only ADDS nodes (ensureInBridges_mono, :363) and edges, so monotonicity is unthreatened.
  :506 cascadeKeys_writeLeg_mono     -- AUDITED. repair entirely inherited from :485 + edgesClosed_foldl_writeBridgedOne. Frontier rows only grow, so dirty-stays-dirty is strictly easier post-bridge.
  :1969 writeLoggedRules_residue     -- AUDITED. one line: change the `show` to name the writeBridgedOne fold, and swap writeDirect_residue -> writeBridgedOne_residue (already @[simp] at UsStarWrite.lean:657)
  :2002 writeLeg_derived_inedges_eq  -- AUDITED, 11+ consumers. Swap in foldl_writeBridgedOne_edges_sound; THE NEW THIRD DISJUNCT DIES FOR FREE BY VARIANT -- a bridge target is always `wAnyNode … = ⟨_, STAR, _, Variant.wAny⟩` (State.lean:122) while `objNode o R` is `plain` or `wAll` (State.lean:126-128), NEVER `wAny`. One `congrArg NodeKey.variant` exfalso branch, no premise, any `on` including STAR. The hRok/relNameOK machinery is untouched. Notice this fact once and reuse it everywhere.

NEW LEMMAS OWED IN Cascade.lean (all names verified free; the release epilogue has schema/nodes/watermark from 3a but NO edge or outbox lemmas):
  ensureInBridgesLogged_outbox_mono / bridgePreLogged_outbox_mono   -> unblocks :78 writeLoggedOne_outbox_mono
  releaseInBridgesLogged_edges_subset / releasePostLogged_edges_subset -> unblocks :2309 removeLoggedOne_edges_subset (mirror the existing proof at :2313: removeEdgeOne_edges + List.mem_of_mem_erase)
  releaseInBridgesLogged_outbox_mono / releasePostLogged_outbox_mono   -> unblocks :2337 removeLoggedOne_outbox_mono
  ensureInBridgesLogged_edge_delta / releasePostLogged_edge_delta      -> unblocks :118 and :2363

⚠ CORRECTION -- :118 writeLoggedRules_edge_delta SURVIVES VERBATIM; the brief and task row :687/:1052 are wrong to list it as an honest-direction restatement. Verified first-hand: `ensureInBridgesLogged` (Cascade.lean:216) pushes `pushDelta (wAnyNode (c.type, c.pred)) c.pred true` while the edge `ensureInBridges` adds is exactly `(c, wAnyNode (c.type, c.pred))` (UsStarWrite.lean:257-263) -- so `d.node = ab.2` holds on the bridge arm at the TARGET, the same denormalisation the routed arm uses, and pushDelta leaves the watermark fixed. Only the inner induction's case analysis widens (three added-edge shapes instead of one). This is exactly what choosing the LOGGED bridge at step 2 bought; the forecast predates that choice. Same reasoning saves :2363 removeLoggedRules_edge_delta (releaseInBridges erases `(c, wAnyNode (c.type,c.pred))`, Cascade.lean:246, so `d.node = ab.2` again).

RESTATEMENT A -- :292 reachedByW3d_edges_target_plain. GOES HARD-FALSE (a bridge target has `Variant.wAny`). AUDITED (audited_theorems.txt:415) so the NAME must survive; not in headline_statements.txt so the statement is free.
  Prefer `… → ∀ ab ∈ σ.edges, ab.2.variant ≠ Variant.wAll` over the disjunctive `plain ∨ wAny`. Its sole consumer is CascadeStable.lean:2098 feeding writeLeg_reach_wAll_false (:417), whose htp' only needs to kill paths landing on `wAllNode dt r'` -- `≠ wAll` is exactly sufficient and needs NO case split at the consumer. Same restatement is owed at the unmeasured twin CascadeStrataSettle.lean:193::reachedByW3d2_edges_target_plain (NOT audited), consumed at :2694, :3289, :3366, :5104, :5250 and CascadeStrataAssemble.lean:588 -- where the extra disjunct discharge is the real work.

RESTATEMENT B -- :2586 mem_removeLoggedOne_edges_iff_of_ne. GOES FALSE: take `b = wAnyNode ((subjNode u.subject).type, (subjNode u.subject).pred)`; `hne` HOLDS (an objNode is never wAny) yet releasePostLogged can erase `(subjNode u.subject, b)`. FIX: add ONE hypothesis `(hb : b.variant ≠ Variant.wAny)` and keep everything else. Strictly sufficient (every edge the release erases has a wAny target) and FREE at the whole consumer chain: :2596 mem_foldl_removeLoggedOne_edges_iff_of_notarget -> :2612 removeLeg_derived_inedges_eq instantiates `b := objNode ⟨dt,on⟩ R`, discharged by `congrArg NodeKey.variant`. Do NOT widen `hne` into a disjunction over the two endpoints' bridge shapes -- that pushes two obligations where one variant fact suffices. Neither name is audited.

:362 writeLeg_reach_stable -- AUDITED, statement unchanged, one added case. ⚠ CORRECTION to task row :163-164 ('hunmapped excludes the bridged key by design'): imprecise and could send the executor hunting for an exclusion clause. CascadeStable.lean:367 quantifies over the POST-write `cascadeKeys`; the bridge rows ARE in that state's outbox and the watermark is unmoved (bridgePreLogged_watermark, Cascade.lean:701), so a bridged key is a DIRTY key and simply FAILS the premise. The bridge is INCLUDED, not excluded; nothing is added to the statement. Repair is downstream of :118 plus swapping the local hclσ' onto edgesClosed_foldl_writeBridgedOne.

### Step 8. CascadeStable -- THE SHADOW (undesigned; decide the scope question BEFORE editing)

*files:* `formal/lean/ZanzibarProofs/GraphIndex/CascadeStable.lean`, `formal/lean/ZanzibarProofs/GraphIndex/Leaf.lean`  
*build after:* yes

THE ONE PIECE WITH NO LANDED DESIGN. 11 of the 27 errors. Read this whole step before typing.

WHY IT BREAKS: `structure ShadowOver (P) (σ σ0)` has `classify : ∀ ab ∈ σ.edges, ab ∈ σ0.edges ∨ P ab.2` and `term : ∀ k, P k → ∀ y, (k, y) ∉ σ.edges` (CascadeStable.lean:568-574). σ0 stays UNBRIDGED -- `ReachedByRulesAdmitted.step` folds `writeRules` (RulesComplete.lean:113-115) and the recorded decision is NOT to bridge writeRules (40-module reverse cone vs UsStarWrite's 24, and it re-opens checkFn_eq_sem_bs). So bridge edges on the left have no home in `classify`. `DerNode` (:551) and `LeafNode` (Leaf.lean:569) both force `k = objNode ⟨ty,on⟩ p` with `on ≠ STAR`, hence `Variant.plain`, so neither can hold at a `wAnyNode`.

THE DECISION (Route A, widen a THIRD time -- it already widened once at 4c-ii 2026-09-02d):
  def BridgeNode (S : Schema) (k : NodeKey) : Prop :=
    ∃ ty p, S.isSubjectWildcardUserset ty p = true ∧ k = wAnyNode (ty, p)
  abbrev UntaintedShadow (S) (σ σ0) := ShadowOver (fun k => DerNode S k ∨ LeafNode S k ∨ BridgeNode S k) σ σ0
KEEP THE `abbrev` (reducible) -- the 2026-09-02d note records that is what made the last widening cost 2 deleted `have`s instead of a re-proof. The widening is CHEAP on the READ side, and that is verifiable, not lucky: `shadow_reach_agree` (:1227) is applied tree-wide at only two target shapes, `objNode ⟨dt',on'⟩ r'` and `wAllNode dt' r'` (CascadeStable.lean:1929-1931 plus the file's own :1256), and a `wAnyNode` matches neither -- the new disjunct dies by the SAME variant mismatch `hv3` already uses for `wAll`. Extend `shadow_graphRec_agree`'s `hv1`/`hv3` (:1869, :1921) from 2-way to 3-way `rintro`.
REJECTED ALTERNATIVE, record it on the row: bridging σ0 too makes `classify` free but pays the RulesWrite reverse cone (40 modules) and re-opens the reference the shadow exists to reach.

THREE `term` OBLIGATIONS ARISE. RESOLVE ALL THREE ON PAPER BEFORE EDITING -- this is the go/no-go for the whole step:
  (T1) no DerNode is a bridge SOURCE. This is exactly `NoBridgedDerived S`: `bridgedInConcrete` (UsStarWrite.lean:156) needs `S.isSubjectWildcardUserset c.type c.pred = true`, which NoBridgedDerived sets false at every derived key. Provider exists and is store-free (FullScope.lean:260). SOLVED.
  (T2) no LeafNode is a bridge SOURCE. ⚠ ONE RECON REPORT CLAIMS 'NO PROVIDER IN THE TREE' AND RECOMMENDS MINTING A NEW LeafScope + GraphAdmission FIELD. MY ADJUDICATION: that is PREMATURE and expensive (a GraphAdmission field propagates to FullScope's 152 decides, the witness bundles, and the audit pin -- a TK68-sized job). TRY THIS FIRST: `reachedByW3d_shadow` (CascadeStable.lean:1774) ALREADY binds `DirectRestrictionsNotLeaf S` and `TtuTargetsSat S NotLeafName` and `BareStarStore T`, verified first-hand in its signature. `DirectRestrictionsNotLeaf S := ∀ d ∈ S.defs, ∀ rs ∈ exprDirectsAll d.2, ∀ r ∈ rs, NotLeafName r.2.1` (:1028) kills disjunct (a) of isSubjectWildcardUserset at a leaf pred, because a leaf pred contains '.' and is ≠ BARE so `NotLeafName` is false for it. Disjunct (b), `isStarTuplesetThrough S t p` (UsStarWrite.lean:89-95), requires `tt.1 == p` over `exprTtus` -- i.e. p is a TTU TARGET -- which `TtuTargetsSat S NotLeafName` (ReconcileCorrect.lean:652) forbids at a leaf name. The ONE new lemma owed is the bridge from `exprTtus` to `schemaRewrites`' `RuleKind.ttu`; precedent for exactly that shape is ReconcileCorrect.lean:658::rewriteStep_subject_pred_gen. Mint a scope field ONLY if that bridge genuinely fails, and if you do, pin its non-vacuity following LeafRules.lean:764::wf_does_not_give_keysNonempty.
  (T3) the NEW BridgeNode disjunct must itself be terminal: no edge out of `wAnyNode (ty,p)` where `isSubjectWildcardUserset ty p = true`. A tuple whose subject is `ty:*#p` has `subjNode = ⟨ty, STAR, p, wAny⟩ = wAnyNode (ty,p)` (State.lean:117) and IS an edge source -- so this is a real hazard, not a formality. IT IS KILLED BY THE FRAGMENT: `W4Fragment.bareStar : BareStarStore T` (FullScope.lean:339) forces every stored STAR subject to have predicate BARE (BareStarCorrect.lean:44), and `isSubjectWildcardUserset _ BARE = false` by the OUTER `p != BARE` guard which the file insists must stay outermost. `reachedByW3d_shadow` already binds `BareStarStore T`. But the `untaintedShadow_*` lemmas are LIST-GENERIC with no fragment hypothesis, so T3 must travel as a list-quantified hypothesis discharged at reachedByW3d_shadow's write case.

THE EDITS, once T1-T3 are settled:
  :1260 untaintedShadow_writeLoggedOne -- keeps its shape, gains two hypotheses:
    (hbrT : ∀ c, (c = subjNode u.subject ∨ c = objNode u.object u.relation) → σ.bridgedInConcrete c = true → Extra (wAnyNode (c.type, c.pred)))
    (hbrS : ∀ c, (c = subjNode u.subject ∨ c = objNode u.object u.relation) → σ.bridgedInConcrete c = true → ¬ Extra c)
  hbrS at the SUBJECT endpoint is already `ha`; at the OBJECT endpoint it is the genuinely new obligation and is where T1/T2 are consumed. Replace `edgesClosed_writeDirect` (ReconcileDiff.lean:529) with edgesClosed_writeBridgedOne.
  :1411 untaintedShadow_writeLegL -- THE BIGGEST BLOCK, 8 of the 27. All 8 are σ-side uses of the writeDirect fold theory after `shadowOver_evalEq_left (foldl_writeLoggedOne_evalEq …)` retargets the left fold: foldl_writeDirect_edges_sound (classify, term), _edges_mono (sub), _edge_complete (sub, nodesSub), _nodes_mono (nodesSub), edgesClosed_foldl_writeDirect (nodesSub, closed). The vs/σ0-side uses stay green. Add hbrT/hbrS list-quantified over `us`.
  :1354 untaintedShadow_foldAdmits -- AUDITED. See step 14: its hypothesis is FoldAdmits-shaped and is where the FoldAdmits honesty question first bites.
  :1774 reachedByW3d_shadow -- discharge hbrT/hbrS here from BareStarStore + DirectRestrictionsNotLeaf + TtuTargetsSat + a new NoBridgedDerived premise. CHEAPER ALTERNATIVE WORTH COSTING: add NoBridgedDerived as a 5th field on `LeafScope` (LeafRules.lean:742), discharged at the FullScope builder by `hA.noBridgedDerived` -- then it rides the existing `hLS` carrier and costs ZERO new binders at the 5 shadow call sites.

⚠ FOURTH FAMILY MEMBER NOT IN THE 27: :1325 untaintedShadow_writeLeg (the one-list form) stayed GREEN in the 3a measurement because it only calls the step lemma, whose signature the composition alone did not move. It WILL red once :1260 gains hbrT/hbrS. Do not report the module done on a count measured before that.

### Step 9. CascadeSettle, CascadeInv, CascadeEnum -- carry threading and structInv routing

*files:* `formal/lean/ZanzibarProofs/GraphIndex/CascadeSettle.lean`, `formal/lean/ZanzibarProofs/GraphIndex/CascadeInv.lean`, `formal/lean/ZanzibarProofs/GraphIndex/CascadeEnum.lean`  
*build after:* yes

CascadeSettle (1150 lines, 23 decls, 16 decides) is the first downstream module.
  :595 -- one of the 12 carry sites, inside :535 settledComplete_cascade_targeted. Add and forward `hNBD : NoBridgedDerived S`.
  :50 foldl_writeDirect_schema, :60 reconcileJobsD_schema, :77 reachedByW3d_schema -- RELOCATED UP by step 4; delete the originals here, nothing else changes (CascadeSettle imports Cascade).
  :100 reachedByW3d_edge_target_ne_bare (breaks at :110 on foldl_writeDirect_edges_sound), :317 completeKey_writeLeg (AUDITED), :795 reachedByW3dC_settled, :914 graph_correct_w3d -- carry binders per the cone list.

CascadeInv is the CHEAPEST downstream module because 3a already landed exactly the twins it needs, and `structInv_ensureInBridgesLogged` / `structInv_releaseInBridges` / `structInv_releaseInBridgesLogged` already exist at CascadeInv.lean:139/:149/:158.
  :167 structInv_writeLoggedOne, :175 structInv_writeLoggedRules, :192 structInv_removeLoggedOne, :203 structInv_removeLoggedRules -> route onto UsStarWrite.lean:701 structInv_foldl_writeBridgedOne
  residue/Inv sites -> :708 residueEmpty_foldl_writeBridgedOne and :715 inv_foldl_writeBridgedOne
  :261 reachedByW3d_structInv, :385 residueHygienic_writeLoggedRules, :396 reachedByW3d_residueHygienic, :446 reachedByW3d_residueDeclared, :491 reachedByW3dC_edgeHygienic (+ carry), :621 reachedByW3dC_inv (+ carry)
Mirror is CascadeStrataInv.lean:151/160/167 and :255/264/270 -- same routing, do them together in step 11.

CascadeEnum: :729 graph_correct_w3dE, :756 reachedByW3dE_inv -- carry binders only.

### Step 10. CascadeStrata -- the W3d2 twin restatement, the carry, AND the two statement-pinned headlines

*files:* `formal/lean/ZanzibarProofs/GraphIndex/CascadeStrata.lean`, `formal/headline_statements.txt`  
*build after:* yes

⚠⚠ THE BRIEF'S CENTRAL STANDING CLAIM IS FALSE HERE AND THIS MUST BE DECIDED BEFORE ANY EDIT. The brief says the carry 'terminates in GraphAdmission so NO HEADLINE GAINS A HYPOTHESIS'. VERIFIED FIRST-HAND: `runCascade2_no_abort` (CascadeStrata.lean:1743) and `cascade2_drains` (:1911) are BOTH in formal/headline_statements.txt (rows 7 and 8 of 51) AND in verify.sh's HEADLINE_AUDITS. Their full binder lists are `{σ S T jobs1 jobs2} (hterm) (hLU2) (hjv1) (hjv2) (hscope2) (h : ReachedByW3d2 σ S T)` -- NO GraphAdmission bundle, and none of those hypotheses mentions `isSubjectWildcardUserset`, so the carry is NOT derivable from what they hold. Both consume `reachedByW3d2_Rnode_not_source` (at :1793 and :1842). FullScope.lean:260::GraphAdmission.noBridgedDerived lives one module DOWN and cannot be projected here.
CONSEQUENCE: two statement-pinned headlines gain `(hNBD : NoBridgedDerived S)` and headline_statements.txt moves by 2 rows. That is LEGAL and honest (it is a schema-scope condition Python enforces at compile time, matching TK68's precedent), but it is a DELIBERATE REVIEWED CHANGE, not a regeneration you do silently: run `statement_pin.py` RED first and capture the failure text, then `--generate`, then write the dated formal/history/ note (step 15). DECIDE THIS ON THE ROW BEFORE STARTING, not at the end -- discovering it late is what torpedoes the session.

THE W3d2 RESTATEMENT (verbatim twins of step 6; a normalised diff of both pairs is 0 lines):
  :1596 reachedByW3d2_edge_source_ne_R -- NOT audited (confirmed: absent from every pin file), so entirely free. Same restatement: add `(hNBD : NoBridgedDerived S)`, conclusion `… → ∀ a b, (a, b) ∈ σ.edges → a.type = dt → a.pred ≠ R`. ⚠ THE BRIEF CITES :1595 -- that is the last DOCSTRING line; the `theorem` keyword is at 1596.
  :1667 reachedByW3d2_Rnode_not_source -- AUDITED (audited_theorems.txt:393). Hypothesis only; conclusion UNCHANGED. Body at :1674 gains `(objNode_type ⟨dt, on⟩ R)` before the existing `(objNode_pred ⟨dt, on⟩ R)`. The W3d2 side needs NO relocation: `reachedByW3d2_schema` is in-file at :455.

CARRY BINDERS in this file: :1743 runCascade2_no_abort, :1911 cascade2_drains.

THE OCCURRENCE-COUNT FAMILY -- REAL WORK, NOT MECHANICAL, and a DEPENDENCY that no consumer-cone scan finds:
  :1286 reachedByW3d2_srcOccCount `∀ a b, a.pred ≠ BARE → σ.edges.count (a,b) = untOccCount S T a b`. GOES FALSE: a bridge edge `(c, wAnyNode (c.type,c.pred))` has `c.pred ≠ BARE` GUARANTEED (the outer `p != BARE` of isSubjectWildcardUserset, recovered by bridgedInConcrete_elim), so the guard cannot fence it out; count = 1, untOccCount = 0. It carries no admission/fragment hypothesis, so nothing rescues it. It is USED at :1626 inside the remove case of reachedByW3d2_edge_source_ne_R.
  :813 count_foldl_writeDirect / :836 count_writeLoggedRules -- the step at :823-824 is `have hstep : (σ.writeDirect u).edges = edgeOfTuple u :: σ.edges`; the bridged step is NOT a single cons, so this is a genuine RE-PROOF. Use UsStarWrite.lean:405::ensureInBridges_count_le_one to bound the bridge contribution (the brief forbids weakening it -- correctly).
  :738 count_removeLoggedRules -- goes FALSE once releasePostLogged wraps the remove leg (the release drops bridge edges that are never `edgeOfTuple` images, so the subtrahend under-counts). NOT audited but IS a CORRESPONDENCE.md anchor (:328), so the NAME must survive or step 4d reds. The recorded Wall-2 decision is the explicit released-bridge term, not `≤`.

### Step 11. CascadeStrataSettle / Resettle / Edge / Assemble / Enum / Inv -- the volume

*files:* `formal/lean/ZanzibarProofs/GraphIndex/CascadeStrataSettle.lean`, `formal/lean/ZanzibarProofs/GraphIndex/CascadeStrataResettle.lean`, `formal/lean/ZanzibarProofs/GraphIndex/CascadeStrataEdge.lean`, `formal/lean/ZanzibarProofs/GraphIndex/CascadeStrataAssemble.lean`, `formal/lean/ZanzibarProofs/GraphIndex/CascadeStrataInv.lean`, `formal/lean/ZanzibarProofs/GraphIndex/CascadeStrataEnum.lean`  
*build after:* yes

CascadeStrataSettle is the schedule bulk: 5327 lines, 139 decls, 375 write-leg symbol references, 18 decides. Direct break sites with enclosing decls: :120 reachedByW3d2_edgesClosed (site 132, AUDITED), :148 reachedByW3d2_edge_target_ne_bare (158), :193 reachedByW3d2_edges_target_plain (204, the restatement from step 7), :681 foldl_writeDirect_nodesFromEdges, :705 reachedByRulesAdmitted_nodesFromEdges (714), :2471 writeLeg_checkFnR_stable (2491, 2498), :4046 writeLeg_derived_inedges_eq_d (4057), :4174 (4186), :4227 (4245), :4306 (4324), :4573 writeLeg_checkFnR_stable_d (4594, 4601). Carry site :2338 round2_key_reads_derived (consumes at :2361). htp' bind sites :2694, :3289, :3366, :5104, :5250.
The two recurring line-level edits at :2491/:4594 and :2498/:4601: swap `writeRulesRaw_schema` for `schema_foldl_writeBridgedOne`, and `edgesClosed_foldl_writeDirect` for `edgesClosed_foldl_writeBridgedOne`.

⚠ THE STANDING BOUND THAT BITES HERE: do NOT route the stratum-2 userset-subject obligation through `GraphState.checkFn`. The consumer is :2651 writeLeg_sem_stable2. The LEGAL chain is already in the tree and must be preserved shape-for-shape: writeLeg_sem_stable2 (:2736-2747) calc's through checkFnR_eq_sem_settled / checkFnR_store_irrel / writeLeg_checkFnR_stable / checkFnR_eq_sem_settled; and writeLeg_checkFnR_stable splits at :2502 on `isDerived S (dt, r')` -- the FALSE arm goes to writeLeg_graphRec_stable, the TRUE arm to writeLeg_probeDerived_stable (:2428), which is residue-equality plus reach-into-R-node, NOT a guard evaluation. By contrast CascadeStable.lean:467::writeLeg_checkFn_stable is ONE arm, graphRec for every leaf -- substituting it at a derived operand is the forbidden move. The divergence is attack-confirmed at CascadeStrataSettle.lean:30-33 (checkFnR = sem = false while the stored representation reads check = true, at exactly 4 keys).
GOOD NEWS TO STATE RATHER THAN RE-DERIVE: writeLeg_derived_inedges_eq (AUDITED) survives -- a bridge target is wAnyNode, never the R-node objNode ⟨dt,on⟩ r', so I5 in-edge fixity at derived keys is UNAFFECTED.

CascadeStrataEdge is the cheapest of the ten (425 lines, 13 decls, 0 decides): carry at :307 inside :237 reachedByW3d2E_edgeHyg1; :349, :393 follow.
CascadeStrataResettle: carry at :874, :1171, :1449, :2170, :2377, :2575 plus the four Rnode sites :922/:975/:2233/:2283.
CascadeStrataAssemble: :721 graph_correct_w3d2E_d gains `(hNBD : NoBridgedDerived S)` -- its hypothesis block already spells out 19 such binders including the store-indexed hterm; put hNBD immediately after hterm. :766 graph_correct_w3d2E follows.
CascadeStrataInv: :151/:160/:167 structInv and :255/:264/:270 residueHygienic -- same routing as CascadeInv in step 9.

### Step 12. RemoveOccCount + RemoveConfluence -- the R3/R4 remove-leg count stack

*files:* `formal/lean/ZanzibarProofs/GraphIndex/RemoveOccCount.lean`, `formal/lean/ZanzibarProofs/GraphIndex/RemoveConfluence.lean`  
*build after:* yes

A FIFTH FALSE-STATEMENT FAMILY the brief does not name at all.
  RemoveOccCount.lean:143::reachedByW3d2E_untOccCount `(h : ReachedByW3d2E σ S T) : ∀ a b, isDerived S (b.type, b.pred) = false → σ.edges.count (a, b) = untOccCount S T a b`. GOES FALSE: a bridge edge is `(c, wAnyNode (c.type,c.pred))` so `b.type = c.type, b.pred = c.pred`; under NoBridgedDerived that key is UNTAINTED, so the guard `hb` is SATISFIED and the equation is exercised -- count = 1, untOccCount = 0 (untOccCount sums `edgeOfTuple` over rewriteClosureL, which never emits a wAny target).
  FIX: add `(hplain : b.variant = Variant.plain)`. ⚠ BUT the P6 row's recorded Wall-2 decision explicitly FORBIDS repairing `count_removeLoggedRules` this way (it is the rescue-by-inadmissible-counterexample shape refused at RemoveOccCount.lean:178-194). Re-read :178-194 before choosing; the two repairs must be made consistent, and the honest one may be the explicit released-bridge term on both.
  Thread whichever guard is chosen through RemoveConfluence.lean:48 removeLoggedRules_untOccCount, :62 drain_removeLoggedRules_untOccCount, :85 mem_drain_removeLoggedRules_untainted, :234 untEdgeMem_drain_removeLoggedRules_rebuild. RemoveConfluence.lean:16 records that this stack is what graph_correct's REMOVE leg stands on; RemoveConfluence.lean:25-27 describes count_removeLoggedOne/count_removeLoggedRules as holding UNCONDITIONALLY -- that word stops being true once releasePostLogged wraps the removal, and the docstring must be corrected too.

### Step 13. Equiv, FullScope, Exec, TtuStarWide, Cli, Scratch4cii -- carry discharge and the decide surface

*files:* `formal/lean/ZanzibarProofs/Equiv.lean`, `formal/lean/ZanzibarProofs/FullScope.lean`, `formal/lean/ZanzibarProofs/GraphIndex/Exec.lean`, `formal/lean/ZanzibarProofs/GraphIndex/Scratch4cii.lean`, `formal/lean/ZanzibarProofs/Audit.lean`  
*build after:* yes

Equiv.lean has ZERO direct write-leg references (`grep -c 'writeDirect|writeRulesRaw|writeLoggedOne|writeLeg' Equiv.lean` = 0); it breaks only through changed import statements. Its six wrappers -- :505 backend_equivalence_w3d, :533 exclusion_effective_w3d, :563 no_ghost_grant_w3d, :600 backend_equivalence_w3d2, :629 exclusion_effective_w3d2, :662 no_ghost_grant_w3d2 -- need `NoBridgedDerived S` as an EXPLICIT hypothesis that FullScope then supplies, because GraphAdmission.noBridgedDerived is only visible one module later.

FullScope: the carry DISCHARGES here, proof term only, no new binder, at 4 sites: :616 graph_correct and :781 graph_reached_inv (both `hA.noBridgedDerived`), :1911 correct_applies and :2158 w3d2E_correct_applies (witness form `accepts.noBridgedDerived` / `admission.noBridgedDerived` / `admission4.noBridgedDerived`). graph_correct's own statement stays BYTE-IDENTICAL: the edit is one new binder on graph_correct_w3d2E_d (step 11) and one new argument at FullScope.lean:623.
⚠ ROUTE THE WITNESS SITES THROUGH THE BUNDLE, NOT BY RE-DECIDING THE CARRY. FullScope.lean:1164::sx_noBridgedDerived_applies is the worked precedent (audited_theorems.txt:102) and formal/history/tk68-uswild-admission-field-2026-09-13.md M11 records the deliberate refusal to prove it by `decide` -- re-deciding stops the pin observing the field.
FullScope has 152 decides and a LIVE value-change risk, not just a recompile risk: :1130-1131 pins `SxThruDerived.isSubjectWildcardUserset "folder" "viewer" = true` AND `SxThruPlain...= true` by decide, while :1034 sxThruPlain_usWild shows SxThruPlain still satisfies usWild -- i.e. bridging FIRES on a schema GraphAdmission ACCEPTS. Any decide computing a concrete `.edges` list through writeLoggedRules on a through-shape schema changes VALUE.

Exec.lean (69 decls, 28 decides): :1122 lrV_writeLeg_eq (`unfold GraphState.writeLoggedRules` at :1127), :1146, :1301. Should stay TRUE if SlV bridges nothing, but elaboration cost rises (bridgedInConcrete runs a full `S.defs.any` + isStarTuplesetThrough scan per fold member per endpoint) -- expect maxHeartbeats/maxRecDepth failures in the decide-heavy files even where no value changes. CascadeStrata.lean:1029 already records this exact blow-up mode.

Scratch4cii.lean IS A LIVE BUILD TARGET (ZanzibarProofs.lean:86), 72 decls, 37 decides, writeRulesRaw at :79, :212-228, :457, :921. Budget it; do not discover it at the end.

Audit.lean has ZERO declarations (`grep -cE '^(theorem|def|lemma) ' Audit.lean` = 0) so it cannot type-error -- but 39 of its lines name write-leg symbols and its PROSE at :710, :957, :1190, :2004, :2014, :2027 goes stale. verify.sh step 4a requires the live `#print axioms` set to be a SUPERSET of audited_theorems.txt, so every restated name must keep its row.

### Step 14. The FoldAdmits honesty obligation -- do it or record the gap, but do not leave it silent

*files:* `formal/lean/ZanzibarProofs/GraphIndex/RulesComplete.lean`, `formal/lean/ZanzibarProofs/GraphIndex/Exec.lean`, `formal/CORRESPONDENCE.md`  
*build after:* yes

3b COMPILES WITHOUT MOVING FoldAdmits AT ALL -- it is a standalone recursive Prop over writeDirect and none of the three re-points references it. So this is an HONESTY obligation, not a build blocker, and it is legitimately deferrable IF recorded.
WHY IT MATTERS: `FoldAdmits : … | σ, u :: rest => σ.admitEdge (subjNode u.subject) (objNode u.object u.relation) = true ∧ FoldAdmits (σ.writeDirect u) rest` (RulesComplete.lean:54) probes the UNBRIDGED state, while post-3b the leg's real probe is `(σ.bridgePreLogged t).admitEdge …`, and admitEdge is ANTI-monotone in edges. `ReachedByW3d.write`'s `hadm` (Cascade.lean:1273) will keep TYPE-CHECKING while no longer describing the fold taken -- an assurance step that fails by PASSING, the house failure mode.
THE TWO PREDICATES PROVABLY DISAGREE: UsStarWrite.lean:858::bridged_probe_refuses_the_cycle (`= false`) vs :865::unbridged_probe_admits_the_cycle (`= true`), both by `decide`. That is already the positive control docs/sabotage-procedure.md would ask for.
SIZE, RE-VERIFIED: the '24 sites / 21 move / 3 stay' figure REPRODUCES today but is a property of a counting convention recorded ONLY in the FROZEN formal/history/leaf-family-split-scope-2026-08-05.md §7. Under it: 19 signature binders (Cascade.lean:1273, CascadeEnum:673, CascadeSettle:234/:329, CascadeStable:2077/:2146/:1737, CascadeStrata:427/:837, CascadeStrataAssemble:432, CascadeStrataSettle:2668/:2931/:3816/:4091/:4529/:4574/:4860/:4905/:5073) + 3 stay + 2 foldAdmitsB runtime gates. Under the plainer reading 'declarations 3b must touch' it is ~36. The convention must travel with the number.
THE 3 STAY-SITES, NOW RESOLVED TO SYMBOLS (this discharges TK67's whole open obligation): RulesComplete.lean:115 inside `ReachedByRulesAdmitted.step` (binder is over the PLAIN closure, on the unbridged σ0 chain); RestrictBase.lean:470 inside exists_admitted_restrict; RestrictBase.lean:531 inside exists_admitted_ofAcyclicTarget. Only line numbers drifted -- PROOF_STATUS.md:4897's `RulesComplete.lean:91` and the scope doc's `Exec.lean:72/:376` (live: :82 in graphRunAux, :454 in graphRunOpsAux) are stale.
⚠ RulesComplete.lean and RestrictBase.lean are UPSTREAM of Cascade (both inside its 36-module cone), NOT downstream -- one recon report's file list implies otherwise. A twin CANNOT live beside writeBridgedOne in UsStarWrite, because UsStarWrite does not import RulesComplete; put FoldAdmitsBridged (name verified free) in Cascade.lean.
IF DEFERRED: log the gap in formal/CORRESPONDENCE.md §7 per the §8 drift rule and say so on the P6 row. Also note `FoldAdmits` (headline_definitions.txt:56) and `foldAdmitsB` (:209) are BOTH def-pinned, so the move is a second pin regeneration.

### Step 15. Pin regeneration, CORRESPONDENCE, and the gate

*files:* `formal/headline_definitions.txt`, `formal/headline_statements.txt`, `formal/CORRESPONDENCE.md`, `formal/conformance/extractor.py`  
*build after:* yes

THE DEF PIN WILL FIRE -- this is not a maybe. Verified first-hand in formal/headline_definitions.txt (244 `def:` rows): row 89 carries GraphState.writeLoggedOne's body VERBATIM, row 86 removeLoggedOne, row 90 writeLoggedRules, row 88 writeDirect, row 56 FoldAdmits, row 209 foldAdmitsB, rows 116/117/118 the three ReachedByW3d2* inductives. Re-point #1 and #3 CHANGE rows 89 and 86; re-point #2 touches NO headline pin (writeRulesRaw is absent from both pin files, present only at audited_theorems.txt:25).
PREDICTED DIFF: 2 CHANGED + ~8 NEW (bridgePreLogged, ensureInBridgesLogged, ensureInBridges, bridgedInConcrete, releasePostLogged, releaseInBridgesLogged, releaseInBridges, inBridgeOnly) -- the definition closure GROWS, which moves MIN_PINNED_DEFS the safe direction (floor asserted with -ge). wAnyNode, addNode, pushDelta, Schema.isSubjectWildcardUserset, isStarTuplesetThrough and STAR are already pinned.
STATEMENT PIN: +2 rows changed for runCascade2_no_abort and cascade2_drains (step 10). That is the one that needs the written justification.

LITERAL PROCEDURE, from the TK68 precedent (formal/history/tk68-uswild-admission-field-2026-09-13.md §5), with PY=C:/Users/user/anaconda3/envs/graph-reachability-zanzibar-index/python.exe:
  1. `"$PY" formal/conformance/statement_pin.py` -- run it RED FIRST and capture the failure text; the history note must show the diff the gate printed BEFORE regeneration.
  2. `"$PY" formal/conformance/statement_pin.py --generate` -- rewrites BOTH goldens.
  3. `git diff --stat formal/headline_definitions.txt formal/headline_statements.txt`; record the row-count transitions.
  4. If any `#print axioms` line was added or removed: `bash formal/regen_audit_pin.sh`.
  5. `PYTHONPATH=<repo> "$PY" -m formal.conformance.doc_counts --generate` if any counted quantity moved.
  6. New FROZEN note formal/history/p6-step3b-<topic>-2026-09-13.md listing EVERY row: name, changed-vs-added, and why it is a fidelity fix. Update CORRESPONDENCE.md §6 for any structure whose field list moved.

CORRESPONDENCE.md EDITS OWED IN THE SAME COMMIT (§7.3's entry at :1177 'THE LIVE WRITE LEG STILL DOES NOT BRIDGE' becomes FALSE the moment 3b lands):
  KEEP the surviving narrowings: out-bridges + entity middles (:1199-1204, inert -- object_wildcard_shapes is empty on this fragment), node GC (Cascade.lean:238-245), star-self-edge rejection (index_v4/wildcard.py:515-518).
  ADD two NEW ones neither previously recorded: (a) Python's `_maybe_remove_bridges` guard is `fresh.implicit AND reference_count == degree` (index_v4/wildcard.py:375) and the cascade STICKY-PROMOTES raw-write subject nodes to explicit (index_v4/processor.py:1012-1018), after which Python KEEPS the bridge forever (:1063-1067) -- Lean's `inBridgeOnly` (Cascade.lean:225) models no explicitness, so `releasePostLogged` strips where Python declines. Do NOT 'fix' the Python; the guard is load-bearing. (b) the cascade-side bridge call sites index_v4/processor.py:958 and :1076, which 3b does not model.
  ADD a third: on a cycle-REJECTED bridge Python aborts the ENTIRE write (wildcard.py:274 has no try/except; the try at :527 wraps only the grant), while Lean skips the bridge and proceeds.
⚠ formal/conformance/extractor.py's P2 docstring sentence 'the Lean model never creates them' becomes FALSE at 3b and must be edited. The derived-arm multiplicity golden (formal/conformance/derived_arm_multiplicity.json, two_stratum_cascade rows [5,1]/[4,1]/[3,1]) is LIKELY unchanged -- P2 drops 0 of 498 raw rows because bridged_in_shapes compiles EMPTY on all 25 in-fragment corpora -- but re-measure, and regenerate only with ZANZIBAR_UPDATE_SNAPSHOTS=1.

GATE, cap-safe, ONE PHASE PER COMMAND, fresh log each time, never `| tail`, never a fixed /tmp path:
  LOG=$(mktemp /tmp/gate-XXXXXX.log); bash formal/verify.sh <phase> > "$LOG" 2>&1; rc=$?; echo "EXIT=$rc log=$LOG"; tail -20 "$LOG"
Order: lean, conf-tile:1/5..5/5, tests-tile:1/4..4/4. Run `lean` FIRST (it builds the zcli the conf phases preflight on). Read the PASSED line, not just $rc. `python scripts/gate_status.py` says which phases are green on the CURRENT tree.
⚠ Editing any *.md stales the `lean` verdict (t2a tree id), so write the session records FIRST, then re-run `lean`, then commit.

## Toolbox gaps — declarations that do not exist and must be written

- foldl_writeBridgedOne_edges_sound (+ one-step writeBridgedOne_edges_sound) -- THE KEYSTONE. Nothing of this shape exists (0 hits tree-wide; the only *_edges_sound theorems are foldl_writeDirect_edges_sound at RulesCorrect.lean:98, two disjuncts, and reconcileJobsD_edge_sound at Cascade.lean:1288). Needs a THIRD disjunct `(σ.schema.isSubjectWildcardUserset a.type a.pred = true ∧ b = wAnyNode (a.type, a.pred))` keyed on the fold's START state, legal because schema_foldl_writeBridgedOne (UsStarWrite.lean:731) makes the schema fold-invariant. Home: UsStarWrite.lean. Consumed at 6+ sites in CascadeStrataSettle, 6 in CascadeStable, 5 in CascadeStrata, 1 each in Cascade/CascadeSettle/RemoveOccCount/RemoveConfluence.

- edgesClosed_writeBridgedOne / edgesClosed_foldl_writeBridgedOne -- mirror CascadeStable.lean:213, BARE-PREDICATE form (not StructInv); four CascadeStable proofs plus CascadeStrataSettle:2498/:4601 call the writeDirect version directly. Home: UsStarWrite.lean. TRUE and easy (ensureInBridges interns wAnyNode via addNode on every bridged branch before it can add the edge; bridgePre addNodes both endpoints first).

- foldl_writeBridgedOne_edges_mono (mirror RulesComplete.lean:39) and foldl_writeBridgedOne_nodes_mono (mirror CascadeStable.lean:177). Home: UsStarWrite.lean. Their one-step cores are the MOVED ensureInBridges_edges_mono (from UsStarClosure.lean:133) and the existing ensureInBridges_mono (UsStarWrite.lean:363).

- foldl_writeBridgedOne_nodes_sound -- STATEMENT MUST WIDEN from CascadeStable.lean:192 by two wAnyNode disjuncts (one per endpoint). Home: UsStarWrite.lean.

- foldl_writeBridgedOne_nodesFromEdges (twin of CascadeStrataSettle.lean:681) -- FLAGGED NON-TRIVIAL, do not budget it as a copy. The reject branch is safe (writeBridgedOne returns the ORIGINAL σ, discarding bridgePre's addNodes), but the accept branch can intern a wAnyNode with NO incident edge via the `admitEdge`-fails sub-branch of ensureInBridges; that case is refutable only through edgesClosed (an isolated wAnyNode reaches nothing so admitEdge cannot fail), so the lemma needs edgesClosed as a side condition.

- ensureInBridgesLogged_outbox_mono, bridgePreLogged_outbox_mono, releaseInBridgesLogged_outbox_mono, releasePostLogged_outbox_mono -- NOTHING relating the bridge legs to the outbox exists (3a landed releasePostLogged_schema/nodes/watermark at Cascade.lean:839/:843/:847 but no outbox twin). Home: Cascade.lean. Needed by CascadeStable.lean:78 and :2337.

- releaseInBridgesLogged_edges_subset / releasePostLogged_edges_subset -- Home: Cascade.lean, mirroring the existing proof at CascadeStable.lean:2313 (removeEdgeOne_edges + List.mem_of_mem_erase) over the existing releaseInBridges_edges_subset (Cascade.lean:464). Needed by CascadeStable.lean:2309.

- ensureInBridgesLogged_edge_delta and releasePostLogged_edge_delta -- the per-leg delta-existence lemmas that let writeLoggedRules_edge_delta (CascadeStable.lean:118) and removeLoggedRules_edge_delta (:2363) keep their statements. Shape: a bridge edge added/erased comes with an outbox row `d` with `σ.watermark < d.id ∧ d.node = ab.2`. The id bound reuses the existing @[simp] ensureInBridges_watermark (UsStarWrite.lean:592) / releaseInBridges_watermark (Cascade.lean:806) exactly where the current proofs use writeDirect_watermark / removeEdgeOne_watermark. Home: Cascade.lean.

- BridgeNode (S : Schema) (k : NodeKey) : Prop := ∃ ty p, S.isSubjectWildcardUserset ty p = true ∧ k = wAnyNode (ty, p) -- the third extras disjunct for the shadow. Home: CascadeStable.lean beside DerNode (:551). Genuinely new; there is no wAny-shaped node predicate anywhere.

- isSubjectWildcardUserset_false_of_notLeafName (working name; verified free) -- the bridge from `TtuTargetsSat S NotLeafName` + `DirectRestrictionsNotLeaf S` to `S.isSubjectWildcardUserset ty p = false` at a leaf predicate p. This is what avoids minting a new GraphAdmission field for the shadow's LeafNode term obligation. The hard half is connecting `exprTtus` (which isStarTuplesetThrough scans, UsStarWrite.lean:89-95) to `schemaRewrites`' `RuleKind.ttu` (which TtuTargetsSat quantifies over); precedent for exactly that shape is ReconcileCorrect.lean:658::rewriteStep_subject_pred_gen. Home: UsStarWrite.lean or CascadeStable.lean depending on which cone the ttu machinery sits in.

- The hbrT / hbrS hypothesis pair on untaintedShadow_writeLoggedOne (CascadeStable.lean:1260) and their list-quantified forms on untaintedShadow_writeLegL (:1411) and untaintedShadow_writeLeg (:1325), plus their discharge at reachedByW3d_shadow's write case (:1774) from BareStarStore + DirectRestrictionsNotLeaf + TtuTargetsSat + NoBridgedDerived. No landed design; this IS the undesigned block.

- FoldAdmitsBridged (name verified free) -- the writeBridgedOne recursion probing `(σ.bridgePre u).admitEdge`, plus `foldAdmitsBridgedB` and an iff to foldAdmitsB. Home: Cascade.lean (NOT UsStarWrite -- it does not import RulesComplete where FoldAdmits lives). The positive control already exists and needs no invention: UsStarWrite.lean:858::bridged_probe_refuses_the_cycle (false) vs :865::unbridged_probe_admits_the_cycle (true), both by decide, are direct machine evidence the two predicates differ. Deferrable if recorded in CORRESPONDENCE.md §7.

- foldl_writeBridgedOne_edge_complete (twin of RulesComplete.lean:87) -- BLOCKED on the FoldAdmits decision, since the original is stated over FoldAdmits. Needed by untaintedShadow_writeLegL's `sub` and `nodesSub` arms.

- A `decide` pin stated on the RE-POINTED writeLoggedOne / removeLoggedOne round trip -- the composition's only possible red-to-green evidence, since every existing 3a witness is stated on the pieces directly and cannot flip. Home: Cascade.lean beside BridgedLegWitness (:864).

## Corrections to the `P6` row

Each of these contradicts something the row, its `Traps`, or its `2026-09-13d`/`2026-09-13e`
Log entries assert. They are recorded here and summarised onto the row itself.

- FALSE, and it is the brief's central standing claim: 'The carry terminates in GraphAdmission so NO HEADLINE GAINS A HYPOTHESIS.' `runCascade2_no_abort` (CascadeStrata.lean:1743) and `cascade2_drains` (:1911) are headlines that consume the carry (via reachedByW3d2_Rnode_not_source at :1793 and :1842), are STATEMENT-pinned in formal/headline_statements.txt (rows 7 and 8), are in verify.sh's HEADLINE_AUDITS, and bind only {σ S T jobs1 jobs2} (hterm) (hLU2) (hjv1) (hjv2) (hscope2) (h : ReachedByW3d2 σ S T) -- no GraphAdmission bundle, and nothing in that list mentions isSubjectWildcardUserset. Two statement-pinned headlines WILL gain (hNBD : NoBridgedDerived S).

- WRONG, and it halves the substantive work: 'two verbatim TWIN PAIRS go FALSE'. Only the two *_edge_source_ne_R go false. Both *_Rnode_not_source STATEMENTS stay TRUE at a bridged state -- their node is `objNode ⟨dt, on⟩ R`, whose .type is dt (State.lean:145::objNode_type) and .pred is R (:147::objNode_pred), i.e. exactly the derived key `hder` names, which is exactly the key NoBridgedDerived un-bridges; the type-index escape hatch ([x:*#R] at an untainted (x,R)) cannot reach a node whose type is pinned to dt. They need the new hypothesis but NO restatement. 10 of the 12 application sites therefore consume an UNCHANGED conclusion.

- UNDERCOUNT: 'THE COMPOSITION IS EXACTLY THREE RE-POINTS.' It is four edits -- LeafRules.lean has no `import ZanzibarProofs.GraphIndex.UsStarWrite` (`grep -n '^import' LeafRules.lean` -> only GraphIndex.Leaf at :1 and GraphIndex.RulesSound at :10), so re-point #2 does not typecheck without it. Cycle-free: UsStarWrite's full transitive cone is 9 modules (Core.Ident/Refs/Schema/Store, Spec.Stratify, GraphIndex.Closure/State/Write/ObjStarWrite) and contains no LeafRules.

- STALE LINE NUMBER: 'CascadeStrata.lean:1595::reachedByW3d2_edge_source_ne_R'. The `theorem` keyword is at 1596; 1595 is the last line of the docstring. :1667 for reachedByW3d2_Rnode_not_source is correct.

- NAME DOES NOT EXIST: the brief's fifth LeafRules corollary `schema_writeRulesRaw`. `grep -rn 'schema_writeRulesRaw' formal/lean/` returns nothing; the live name is `writeRulesRaw_schema` (LeafRules.lean:396), suffix form, unlike the other four (structInv_/residueEmpty_/inv_/quiescent_writeRulesRaw at :380/:384/:388/:392).

- ARITHMETIC DOES NOT CLOSE: 'Cascade.lean -- 7 errors, 6 mechanical + 1 real' followed by SEVEN mechanical names. All seven ARE mechanical and each has an existing step-3a discharger (Cascade.lean:639 writeBridgedOne_logged_evalEq, :701 bridgePreLogged_watermark, :839/:843/:847 releasePostLogged_schema/nodes/watermark). The real Cascade-local error is reachedByW3d_edge_source_ne_R (:1316), which the brief files separately under 'the substantive obligation'. Also the Cascade fallout list is short by at least two declarations: reachedByW3d_Rnode_not_source (:1349) and reconcileJobsL_Rnode_not_source (:1360, consumes the former at :1369). Budget Cascade at ~10, not 7.

- OFF BY ONE (task row :1085 and the brief): 'CascadeStable.lean -- 27 errors across 17 declarations'. The named list is 18 DISTINCT declarations, each resolving to exactly one `^theorem` line (70, 78, 107, 118, 254, 292, 362, 485, 506, 1260, 1354, 1411, 1969, 2002, 2309, 2337, 2363, 2586), and the per-name error counts sum to exactly 27. The error count is right; the declaration count is not.

- WRONG SUBDIVISION: 'the untaintedShadow_* family (4 declarations, 11 of the 27 error sites)'. The 11 errors come from THREE declarations -- untaintedShadow_writeLoggedOne (:1260, x2), untaintedShadow_foldAdmits (:1354, x1), untaintedShadow_writeLegL (:1411, x8). The fourth member, untaintedShadow_writeLeg (:1325), did NOT error in the 3a measurement because it only calls the step lemma, whose signature the composition alone did not move. It is owed work but is not one of the 27, and reporting the module done on that count would be wrong.

- NOT A RESTATEMENT (brief + task row :687 and :1052): `writeLoggedRules_edge_delta` (CascadeStable.lean:118, audited_theorems.txt:613). Its statement SURVIVES VERBATIM. `ensureInBridgesLogged` (Cascade.lean:216) pushes `pushDelta (wAnyNode (c.type, c.pred)) c.pred true` while the edge `ensureInBridges` adds is exactly `(c, wAnyNode (c.type, c.pred))` (UsStarWrite.lean:257-263), so `d.node = ab.2` holds on the bridge arm at the TARGET, the same denormalisation the routed arm uses; pushDelta leaves the watermark fixed (Cascade.lean:153-155). Only the proof's case analysis widens. The forecast predates the step-2 choice to emit at the TARGET, which is the choice that saves it. Same reasoning saves removeLoggedRules_edge_delta (:2363).

- IMPRECISE in a way that will send the executor hunting for a clause that is not there (task row :163-164): 'the logged variant makes writeLeg_reach_stable's hunmapped premise exclude the bridged key by design'. CascadeStable.lean:367 reads `hunmapped : (dt, R, on) ∉ cascadeKeys S (σ.writeLoggedRules S t)` -- the POST-write state. The bridge rows ARE in that state's outbox and the watermark is unmoved (bridgePreLogged_watermark, Cascade.lean:701), so a bridged key is a DIRTY key and simply FAILS the premise. The bridge is INCLUDED, not excluded; nothing is added to the statement.

- INCOMPLETE: 'LeafRules.lean -- 6 errors, all mechanical.' (a) `writeRulesRaw_untaintedSchema` (:368) is a RESTATEMENT, not a mechanical fix, and its NAME is gate-pinned (audited_theorems.txt:616 + `#print axioms` at Audit.lean:1986) so it may be restated but never deleted -- its zero-consumer status is confirmed for APPLICATIONS only. (b) A SEVENTH declaration in the file touches the re-pointed def: LeafRuleWitness.lrV_writeRulesRaw_edges_ne (:1934, `by decide` over `((emptyState SlV).writeRulesRaw SlV tlEditor).edges`, audited_theorems.txt:37 / Audit.lean:2001).

- INCOMPLETE (the brief did say 'at least'): there are FIVE independent false-statement families, not two. Beyond the four named theorems: (3) *_edges_target_plain -- CascadeStable.lean:292 (AUDITED, audited_theorems.txt:415) and its unmeasured twin CascadeStrataSettle.lean:193 (not audited); a bridge target is `wAnyNode sh = ⟨sh.1, STAR, sh.2, Variant.wAny⟩` (State.lean:122), never plain. (4) CascadeStable.lean:2586 mem_removeLoggedOne_edges_iff_of_ne -- its `hne` guard no longer names every edge the wrapped release can erase. (5) the occurrence-count stack: CascadeStrata.lean:1286 reachedByW3d2_srcOccCount (a bridge edge has `c.pred ≠ BARE` GUARANTEED by isSubjectWildcardUserset's outer guard, so its own guard cannot fence it out, and it carries no admission hypothesis), CascadeStrata.lean:813/:836 count_foldl_writeDirect/count_writeLoggedRules, :738 count_removeLoggedRules, RemoveOccCount.lean:143 reachedByW3d2E_untOccCount, and the four RemoveConfluence decls on top of it.

- MISPLACED IN THE DOWNSTREAM SET: RulesComplete.lean and RestrictBase.lean are strictly UPSTREAM of Cascade.lean -- both are inside Cascade's 36-module import cone (measured first-hand). Neither contains writeLoggedOne/writeRulesRaw/writeLeg. They enter 3b's scope only via the FoldAdmits obligation, and a FoldAdmits twin CANNOT live beside writeBridgedOne in UsStarWrite (which does not import RulesComplete).

- SILENT SCOPE OMISSION: 'Nothing past CascadeStable was measured.' The downstream set is exactly 19 modules (measured by reverse-import BFS): CascadeSettle, CascadeInv, CascadeEnum, CascadeStrata, CascadeStrataSettle, CascadeStrataResettle, CascadeStrataEnum, CascadeStrataAssemble, CascadeStrataInv, CascadeStrataEdge, RemoveOccCount, RemoveConfluence, Equiv, FullScope, Exec, TtuStarWide, Cli, Scratch4cii, Audit. Scratch4cii.lean is a LIVE build target (ZanzibarProofs.lean:86) with 37 decides that computes writeRulesRaw edge lists at :79, :212-228, :457, :921 -- easy to skip because of the name.

- STALE CITE inside the file 3b edits: Cascade.lean:4, :531 and :687 all cite `GraphState.writeRulesRaw (LeafRules.lean:255)`. The live def is LeafRules.lean:324 (:255 is rewriteStep_subset_rewriteStepL).

- UNENFORCEABLE TRAP CITING A NONEXISTENT SYMBOL (the repo's own named failure mode): UsStarWrite.lean:255 says the multiset property is 'pinned by `ensureInBridges_idem` + `InBridgeIdemWitness` below -- do not weaken either', and UsStarCorrect.lean:60 repeats it. `grep -rn 'ensureInBridges_idem' formal/` returns exactly two hits, both inside those docstrings; no declaration of that name has ever existed. The real pin is `ensureInBridges_count_le_one` (UsStarWrite.lean:405).

- DOCSTRING ARITHMETIC WRONG: UsStarWrite.lean:131-132 states the carry's consumer cone as '1 + 2 + 8' = 11 application sites. A re-run census confirms TWELVE -- it omits `reachedByW3d2_edge_source_ne_R`'s single site at CascadeStrata.lean:1674. (The 12 are Cascade:1356; Cascade:1369, CascadeSettle:595; CascadeStrata:1674; CascadeStrata:1793, :1842, CascadeStrataEdge:307, CascadeStrataResettle:922/:975/:2233/:2283, CascadeStrataSettle:2361.)

- UNRESOLVED NUMBER, immaterial to the work: the brief says the union cone is '47 declarations, 12 files'; a first-hand transitive-consumer scan by one recon agent measured 49 across the same 12 files, the two extras being FullScope.lean:1974::correct_applies_nonfence and :2191::w3d2E_correct_applies_nonfence, which are untouched either way. I did not re-derive the count myself. Record 12 application sites (verified) and treat 47-vs-49 as unpinned.

- STALE LINE CITES in FROZEN docs, substance intact (this discharges TK67): formal/history/PROOF_STATUS.md:4897 cites FoldAdmits stay-site 1 as RulesComplete.lean:91 (live: the ReachedByRulesAdmitted.step ctor at :113 with the hadm binder at :115); formal/history/leaf-family-split-scope-2026-08-05.md §7 cites the two foldAdmitsB runtime gates at Exec.lean:72/:376 (live: :82 in graphRunAux, :454 in graphRunOpsAux). All three stay-sites resolve at the right SYMBOLS -- RulesComplete ReachedByRulesAdmitted.step, RestrictBase:470 exists_admitted_restrict, RestrictBase:531 exists_admitted_ofAcyclicTarget. Also: the '24 sites / 21 move / 3 stay' figure is a property of a counting convention that lives ONLY in that frozen §7; under the plainer reading it is ~36, and the convention must travel with the number.

- PREMATURE (one recon report's recommendation, not the task row): 'the LeafNode half of the shadow term obligation has NO provider in the tree; add a new LeafScope + GraphAdmission field.' `reachedByW3d_shadow` (CascadeStable.lean:1774) ALREADY binds `DirectRestrictionsNotLeaf S` (CascadeStable.lean:1028, which forbids a leaf-named restriction and so kills disjunct (a) of isSubjectWildcardUserset) and `TtuTargetsSat S NotLeafName` (ReconcileCorrect.lean:652, which should kill disjunct (b) since isStarTuplesetThrough requires p to be a TTU target) and `BareStarStore T`. Derive before minting; a new GraphAdmission field is a TK68-sized job.

- UNVERIFIED BY ME, flag on the row: the Python-fidelity report's two substantive claims are marked 'inferred', not first-hand-verified against a run -- (a) the `_maybe_remove_bridges` explicitness gap (`fresh.implicit AND reference_count == degree`, index_v4/wildcard.py:375, vs the cascade's sticky promote at index_v4/processor.py:1012-1018) meaning Lean's releasePostLogged strips where Python declines; and (b) the same report's `_map_rows` item (index_v4/processor.py:1418) was self-marked 'uncertain'. Both are CORRESPONDENCE.md §7.3 boundary lines, not code changes -- but confirm (a) against the Python before writing it into a tracked file. Do NOT change the Python: processor.py:1067 says outright '⚠ The strip guard is NOT the thing to relax'.

