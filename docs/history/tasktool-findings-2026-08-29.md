# `TK*` findings — snapshot taken 2026-08-29, before the task-tool trial verdict

> **FROZEN 2026-08-29 — provenance, not a living document.** Status lines below are
> as-of-then and several may now be false; live state: `HANDOFF.md` + the session
> ledger. Corrections are appended dated at the top, never edited into the body.

**Why this file exists.** The `tasks/` trial ends 2026-08-30
([`CLAUDE.md`](../../CLAUDE.md), the "ON TRIAL" bullet). Its exit plan reads *"delete
`tasks/`, `scripts/task.py` and this bullet, which is one revert"* — but
[`docs/tasktool-trial-protocol.md`](../tasktool-trial-protocol.md) §6 (2026-08-24c, T2)
recorded that this is **one revert for the tool and a deletion of the `TK*` findings for
the repo**, and that those are separable decisions the wording bundles into one. This
snapshot separates them: it makes a DELETE verdict **non-destructive**, so the verdict
can be decided on whether the query beats the file, and not on fear of losing findings.

**This is a snapshot, NOT a second live home.** While `tasks/` exists, `tasks/` is
authoritative for every id below ([`docs/README.md`](../README.md) §1, one home per
statement). Read this file only if `tasks/` is gone, or to check what a finding said on
2026-08-29.

**It does not answer the second question.** Making DELETE safe is not the same as
deciding which findings deserve promotion to board rows, appending to a live doc, or an
explicit write-off. That adjudication is owed and is tracked as board row `TK52`.

## Census, measured 2026-08-29

`TK1`–`TK51`, **51 ids, no gaps**: **47 open** (all reproduced below, in id order) and
**4 closed** (`TK1`, `TK14`, `TK46`, `TK47`, retained in `tasks/closed/` and *not*
reproduced here). This supersedes the 2026-08-24c census of *"49 open, 2 closed"* by
exactly the two ids closed since: `TK46` and `TK47`, both closed 2026-08-24 in `01a2916`.

## How isolated these findings actually are

Verified 2026-08-29 by grep over the repo excluding `tasks/`, `.scratch/`, `.git/` and
vendored `formal/lean/.lake/`:

| where the id appears outside `tasks/` | count | ids |
|---|---|---|
| nowhere at all | **34** | `TK2` `TK3` `TK4` `TK5` `TK7` `TK9` `TK10` `TK15` `TK16` `TK17` `TK18` `TK21` `TK22` `TK23` `TK24` `TK26` `TK27` `TK28` `TK29` `TK30` `TK31` `TK32` `TK33` `TK34` `TK35` `TK36` `TK37` `TK39` `TK40` `TK41` `TK42` `TK43` `TK45` `TK50` |
| session log and/or trial protocol only (narrative, not a tracked item) | 12 | `TK6` `TK8` `TK11` `TK12` `TK13` `TK19` `TK20` `TK25` `TK38` `TK44` `TK48` `TK51` |
| on the board (`HANDOFF.md`) | **1** | `TK49`, in the `HS-5` row (`HANDOFF.md:60`) |

⚠ **The often-repeated claim "only `TK49` is referenced outside `tasks/`" is false as
stated** — 13 of the 47 are. It is true only of `HANDOFF.md`: the board mentions exactly
one `TK` id. The other 12 appear solely in `docs/history/session-log.md` and
`docs/tasktool-trial-protocol.md`, which are narrative records, so they would survive the
revert as prose but not as tracked work. The original claim
(`docs/history/session-log.md:407`) was true when written and was invalidated by later
sessions; it is corrected here rather than edited there.

## Entry format

Each entry is `### <ID> — <title verbatim>`, then the frontmatter facts, then the
substance: what is wrong or missing, where, and why it matters. Citations are copied
character-for-character from the task file and were **not** re-verified against the live
tree — treat every `file:line` below as as-of-2026-08-29 and re-check before acting.
Per-entry `## Traps` / `## Read first` / `## Log` sections were folded into the prose;
`tasks/` at commit `74b4a7e` is the byte-exact original if one is ever needed.

---

### TK2 — lookup at_least has no set-engine fallback rung: ZT-P1-8b closed only the raise half
`pri=LATER size=M deps=- opened=2026-08-21b`

`check(..., at_least=N)` falls back to the set engine when the index is behind; `lookup`/`lookup_reverse` cannot, so they accept the token and raise `LookupNotFresh` (`connectedstore/store.py:42`, `::ConnectedStore._require_index_freshness`). `ZT-P1-8b` closed the accept-and-raise half and is CLOSED as filed, but the decision log (`docs/architecture/decision-log.md:166-189`, with the future-work shape at `:187-189`, LIVING) names the remaining rung in three explicit steps: unify the lookup result contract on keys/markers, add the same `at_least` plumbing `check` has, and extend the lookup-oracle gate with a lagging-index leg. Nobody owns those steps, so this is a closed id under-covering its filed scope: the ledger says `ZT-P1-8b` closed, and it did, but the reader who wants "is `at_least` done?" gets the wrong answer. Trap: the decision log calls the last step "mostly test-writing", and that is the expensive half - the lagging-index leg belongs in `tests/test_lookup_oracle.py`, which `CLAUDE.md` names as the lookup-surface oracle gate, and any divergence it finds gets a POSITIVE PIN, never an xfail (`MAX_TESTS_XFAILED` is budgeted in `formal/verify.sh` and is currently 0). Trap: `_fresh_enough(None) -> True` is the DESIGN, not a fail-open default - `store.py:328-336` says so in a comment; do not "harden" it while plumbing the token.

### TK3 — I9 fixpoint audit is test-only and 'writes always cascade' is convention, not a check
`pri=LATER size=M deps=- opened=2026-08-21b`

`install_paranoia` (`index_v4/invariants.py::install_paranoia`, `:773-803`) wires, at `level='full'`, the invariant checker plus the delta-scoped verifier, and at `level='residue'` only `check_residue_hygiene`; `audit_fixpoint` is not among them, deliberately - `invariants.py:184` says "Not here, by design: I9 is the processor's fixpoint audit" - and every I9 call site is a test (e.g. the "paranoia dose" call at `tests/parity.py:98`). Second half, same gap: nothing structural forces `run_cascade` to run on a boolean write; cascade-as-precondition is convention, enforced by review and by `GraphBackend.apply` in the test harness, not by a checked invariant. `formal/SEMANTICS.md:891-899` raises the escalation in as many words - "do you want a Phase-7-style check that the write paths always cascade?" - and it was never answered; `grep -n 'I9\|audit_fixpoint' HANDOFF.md` returns 0 hits. The deliverable is the ADJUDICATION first (is the scope boundary accepted, or is a per-commit/per-write check wanted?), then whichever of the two it implies. Traps: this is an assurance gap, so read `docs/sabotage-procedure.md` BEFORE adding any check - a cascade-precondition check that cannot fire converts an acknowledged gap into a false green; and cost is the reason I9 is not already on (`ZANZIBAR_PARANOIA=residue` exists because the full tier is too expensive for production, ~+5% on writes for the residue tier alone), so any proposal must state which TIER it lands in and what it costs. Other anchors: `formal/SEMANTICS.md:549-554`, `index_v4/processor.py::DeltaProcessor.audit_fixpoint`.

### TK4 — spec mandates a documented lenient-mode hook that does not exist in code (adjudicate)
`pri=HOLD size=S deps=- opened=2026-08-21b`

`docs/specs/wildcard-materialization-spec.md:141` (§3.4, *Strict forall=>exists, pinned*) ends: "Leave a documented hook (a per-shape config flag that would add a single `w_all(S) -> w_any(S)` edge for the lenient/vacuous reading) but do not implement it." No such flag or hook exists in `index_v4/wildcard.py` or `zanzibar_utils_v1.py` (the three `lenient`/`vacuous` hits there are unrelated prose). It is HOLD because the item is an adjudication, not a build: §10 (line 324) lists lenient mode under non-goals as "hook only", so the sentence may already be satisfied by the spec text itself, and it is a real spec-vs-code deviation only if *hook* means a code affordance. Decide which, then either (a) add the flag or (b) reword §3.4 so it stops mandating an artifact, and record the outcome in `docs/spec-deviations.md`. Traps: `CLAUDE.md`'s "where a spec and the code disagree on a name, the CODE wins" settles naming, not existence, so this needs a human call and the cheap outcome (b) is as legitimate as (a); and do not implement the lenient reading itself - §3.4 pins strict forall=>exists as the default and only mode, which the oracle and both backends agree on, so only the HOOK is at issue.

### TK5 — write throughput under PostgreSQL writer contention remains untested, and no row owns it
`pri=LATER size=M deps=- opened=2026-08-21b`

`docs/architecture/correctness.md:125-140` (LIVING) argues multi-writer admission is correct by construction and that the `FOR UPDATE` semantics are now *observed* on PostgreSQL - lock ordering, contiguous exactly-once log rows under 4 concurrent writers, an index identical to a single-writer replay - and then ends, verbatim at `:139-140`: "Throughput under contention remains untested beyond the retry-on-busy convergence tests." That is an explicit gap in a LIVING architecture doc with no owning row: correctness under contention is covered, but what is not measured is whether the lock ordering (source lock, then store lock, with an evaluator catch-up inside the section - `connectedstore/source.py::TupleSource._lock_source` then `index_v4/core.py::ReachabilityIndex._lock_store`) degrades usefully or pathologically as writers are added; the deliverable is a measurement and a recorded number, not necessarily a fix. Trap: the default CI run cannot answer this, since SQLite renders `_lock_store` to a no-op and serializes writers itself, so this needs the opt-in leg (`bash scripts/pg_local.sh start`, export the DSN as `ZANZIBAR_TEST_DSN`, consider `ZANZIBAR_PG_REQUIRED=1` so a missing DSN is a hard error rather than a silent green), extending `tests/test_postgres_ha.py`. Trap: the PostgreSQL leg falsified three things this document used to assume (`docs/spec-deviations.md` 2026-07-27) and found three real bugs its first day, so budget for the measurement changing the prose rather than for a rubber stamp.

### TK6 — leaf-family split: is storage=True/False modellable as one leaf? unresolved at 4 dates
`pri=LATER size=S deps=- opened=2026-08-21b`

Open question §8.2 item 2 of the leaf-family-split scope doc (`formal/history/leaf-family-split-scope-2026-08-05.md:530-534`, ACTIVE-PLAN): whether the `storage=True` / `storage=False` leaf distinction needs to be modelled at all, or whether one undifferentiated leaf node per index suffices for `Inv`'s purposes. The doc's own answer is "this probably **does** need modeling, but it was not verified in this pass" - Python's reason for the split is TTU stored-parent enumeration, and TTU parents are stored tupleset tuples, a pinned semantic. It is flagged unresolved at four separate dates in that one doc (`:467-468`, `:530-534`, `:772-775`, `:819-822`) and has never been folded into any board row's scope pointer; the doc is ACTIVE-PLAN and the leg it scopes is the one currently executing, so this constrains LIVE work (`P3`/`P4`, recorded as `related`), which is why it is LATER rather than SOMEDAY despite being a question. Traps: answering it "no" is the expensive direction and is not obviously wrong - one undifferentiated leaf is cheaper to model but throws away the distinction TTU enumeration depends on, so whoever answers must say which theorems the answer buys and which it costs (an unrecorded answer is how this reached four dates); and `deps` is deliberately empty because this scopes `P3`/`P4` rather than waiting on them - a dep edge would invert the direction and make it invisible to `ready` for the wrong reason.

### TK7 — relNameOK admits the EMPTY relation name, so leafPublic BARE = "" -- constrains unwritten Lean
`pri=HOLD size=S deps=P3 opened=2026-08-21b`

`formal/lean/ZanzibarProofs/Core/Schema.lean:64` is `def relNameOK (name : String) : Prop := ¬ name.contains '.'`, which admits `""`, and `WF` (`:70-71`) carries only the `relNames` clause with no nonempty-name clause; `leafPublic` takes the dot-free prefix, so `leafPublic BARE = ""`, and a pathological schema declaring a derived relation named `""` would make bare-subject nodes classify as `LeafNode`. The fix is one clause: carry `leafPublic p ≠ ""` in the `LeafNode` definition, add a WF nonempty-name clause, or prove the empty name unreachable. This was DOWNGRADED from COVERAGE.md's tier-1 filing ("a soundness hole in a well-formedness condition"): `LeafNode` does not exist in mainline Lean - every occurrence in `formal/lean/` is in `GraphIndex/Scratch4cii.lean`, the Route-B scratch file added 2026-08-21, and `formal/history/PROOF_STATUS.md:121` agrees ("`LeafNode` does not exist and must be written"), so it constrains code not yet written and nothing in today's tree is false because of it; `deps: [P3]` is the honest edge, since there is nothing to patch until `P3`'s Route B writes `LeafNode`. Traps: its capture is already good and its risk is closure, not loss - the residual is recorded three times (`PROOF_STATUS.md:217-220`, `PROOF_STATUS.md:123`, and `Scratch4cii.lean:279-284`, the last beside a positive pin `bare_publicOfLeaf_none` proving the CURRENT carrier is safe), so do not "close" it by adding a fourth prose copy; and do not raise its severity back on re-read - the tier-1 framing came from reading a design constraint as a live defect, the same class of error that produced the sweep's other refuted tier-1 item.

### TK8 — node GC / flag lifecycle is unmodelled in Lean, and TWO named bugs landed inside it
`pri=LATER size=L deps=- opened=2026-08-21b`

Lean models the node-flag RULE and explicitly disclaims the collection ALGORITHM - `ReconcileDiff.lean` and `Cascade.lean` both say "node GC is a modeled-away optimization" (`formal/CORRESPONDENCE.md:975-996`). What makes it a filed row rather than a declared boundary is the empirical record: two named correctness bugs have now landed inside that one unmodeled region - `ZT-P0-1` (the unsound `_keys_referencing` elision) and, added to the map on 2026-08-21, `BL-1` (the released-userset bridge leak, found by the hypothesis campaign), both named at `formal/CORRESPONDENCE.md:983-986`/`:992`. Two bugs in one modeled-away region is an argument for filing it, not evidence it is handled; the deliverable is not necessarily a model - it may be a recorded decision that the differential + hypothesis nets are the intended net, written where the next reader will find it - but the current state is an exclusion whose justification the evidence has moved against. Traps: this is distinct from `P19` (the READ surfaces, recorded as `related`) and from the `reconcile_subject` cheap-path row - three separate regions, three separate arguments, do not merge them; and the map records the bugs but not what they imply, with `CORRESPONDENCE.md` §8 explicit that the anchor gate keeps the map navigable, not true, so adding a third bug to the list is not progress.

### TK9 — reconcile_subject's cheap path is unmodelled and has grown real logic twice
`pri=LATER size=M deps=- opened=2026-08-21b`

Lean models only the full-object reconcile (`reconcileStarsKey`/`reconcileStarsKeyD`); the per-subject cheap path (`index_v4/processor.py::DeltaProcessor.reconcile_subject`) has no model and has grown real logic twice - *promote-on-record* (2026-07-17) and *escalation to the full reconcile* (2026-07-26, the `ZT-P0-1`/`ZT-P0-2` fix). `formal/ARCHITECTURE.md:759-774` states the consequence plainly - "the gap is no longer plausibly characterizable as 'a thin fast path'" - and `CORRESPONDENCE.md` §7.1 (third bullet) puts it as "a per-subject path that can escalate to a full reconcile and can mutate node flags is a real algorithm, and none of it is in the model." Both landed fixes are pinned Python-side by regression tests, so this is a modelling gap, not a live defect; it is filed because the justification for the exclusion ("a thin fast path") is the thing that stopped being true. Traps: it is adjacent to but distinct from the node-GC region - both are `CORRESPONDENCE.md` §7.1 bullets and both unmodeled, but different code and different arguments, filed separately on purpose; and read `formal/ARCHITECTURE.md` with suspicion - it carries no liveness banner and the sweep found several of its claims stale (it still presents the T2a fork as undecided; it was decided 2026-08-05), so although its statement of this gap was re-confirmed, check anything else there against the code.

### TK10 — set-engine write admission is modelled only Python-vs-Python, and it gates what the gates see
`pri=HOLD size=M deps=- opened=2026-08-21b`

`SetEngine._validate` step (1) (object-wildcard gating) and step (3) (cycle rejection, via `::SetEngine._would_cycle` -> `::SetEngine._flow_reaches` over the bridge-aware flow graph) are modelled only as a Python-vs-Python differential; only step (2) has any Lean counterpart, and even that appears as a premise. `CORRESPONDENCE.md:943-954` states why this one is load-bearing rather than merely uncovered: a bug here silently shrinks the enumerated space, and nothing formal watches it - admission decides which stores the conformance and enumeration gates (`formal/conformance/corpus.py`) can enumerate at all, so a wrongly-rejecting validator makes every downstream green cheaper without saying so. It is HOLD rather than LATER because the exclusion is declared with a stated reason, and closing it properly means deciding what a Lean-side admission model would even buy over the differential - that decision is the work, and it is not queued. Trap: this is the "green because it stopped looking" shape one level up from the usual one - the instrument is not a check that could pass vacuously, it is the input filter that decides how much there is to check; a cheaper mitigation such as a non-vacuity floor on how many corpora survive admission, in the style of `anchor_check.py`'s `MIN_*` floors, may be worth more than a model and is a legitimate outcome for this row. Other anchors: `setengine/engine.py::SetEngine._validate`, `::SetEngine._ensure_flow_graph`.

### TK11 — ResidueV1.version (I7) is modelled by nothing formal; ZT-P4-5 declared it and never refiled
`pri=LATER size=M deps=- opened=2026-08-21b`

`index_v4/models.py::ResidueV1` carries a `version` column, incremented in `index_v4/processor.py::DeltaProcessor._store_residue` and checked by I7 (monotonicity per residue row) in `index_v4/invariants.py::_check_residue_rows`; Lean's `Residue` has no version field, so I7 is pinned only by `tests/` paranoia runs. `CORRESPONDENCE.md` (`:515-534`, and the same gap in `formal/FINAL_REVIEW.md:443-448`) calls it a MODELLING GAP, not a representation difference, and declares it as projection `P7` (`ZT-P4-5(b)`, 2026-07-27). The reason it needs an id: the closed record `ZT-P4-5` is titled "residual declared: I7 ungated by formal" - the residual was declared AT CLOSURE and never re-filed as an open row, and a declared residual with no open carrier is indistinguishable, six months later, from a closed one. Traps: the `P7` collision is real and is how this looks captured when it is not - Lean's projection `P7` (in `FINAL_REVIEW.md` prose) is not board id `P7` (`ttuStarFree (iii)+(iv)`), which sweep-i flagged, so do not resolve "P7" by grep; and version is about to become load-bearing for a read path, since Appendix lead A14 (`docs/perf-round6-audit-2026-08.md:919`) proposes a persistent derived-check cache validated by `ResidueV1.version`, which if it lands makes every derived read's correctness rest on I7 - adjudicate them together.

### TK12 — the faithful Lean fix for the P3 edge-multiplicity divergence was never attempted
`pri=LATER size=M deps=- opened=2026-08-21b`

On the derived arm the Lean model over-counts edge multiplicity: Python does a presence diff on the reconcile fold where the model does not. The faithful fix is named and narrow - mirror the presence diff by adding a `¬ hasEdge` conjunct to `GraphIndex/CascadeStrata.lean::reconcileKeyDR`'s fold guard - and it was "left open, deliberately" (`formal/CORRESPONDENCE.md:687-689`, §7.2 item 6) because it ripples through the edge-characterisation and settledness stacks. This addresses the BASELINE `n -> 2n` derived-edge stacking artifact; `P3` covers the closed headline finding, not this residual, and sweep-n's "captured (own doc: `CORRESPONDENCE` §7.2 item 6)" is the error worth naming - a doc section is not a board id - while `docs/latent-gaps.md` (`:158-166`, LIVING) exists precisely to index what is open and points here, so the carrier is live. Trap: DO NOT instead make `GraphIndex/Write.lean::GraphState.admitEdge` reject a present edge - that global version breaks the untainted arm, which is load-bearing for `untOccCount` / erase-one removal; both `latent-gaps.md:163-166` and `spec-deviations.md:1047-1049` carry this warning, since it is the obvious wrong fix. Trap: distinct from the second, OPPOSITE untainted-arm divergence (model `rewriteClosure` vs `RuleSet.apply` dedup), which is CLOSED - the dedup landed 2026-08-08. Other anchors: `docs/spec-deviations.md:1045-1052`, `formal/history/echain-widening-plan-2026-07-28.md:446-449`.

### TK13 — no Lean statement covers the BATCHED apply schedule (async catch_up / build_index)
`pri=HOLD size=M deps=- opened=2026-08-21b`

"Nothing in the Lean tree quantifies over 'apply N ops, then one cascade'." The model covers the synchronous per-write schedule; the async `catch_up` / offline `build_index` schedules - where many ops are applied and then a single cascade drains - have no statement (`formal/CORRESPONDENCE.md:392-423`, the apply-step map and the missing quantification). It is distinct from `P18` (the concurrency / multi-instance layer, recorded as `related`): this is SINGLE-WRITER batching, and it is what production actually runs on the async path. HOLD because the exclusion is declared and the sizing question - does this need a new schedule inductive, or does an existing drainedness theorem generalize? - has not been asked. Traps: Appendix lead A13 would change the default batch size on exactly this path (`ConnectedStore.catch_up` defaults to one unbounded batch today; if that becomes bounded, the batched schedule stops being an opt-in shape and becomes the normal one), so it is worth knowing which order those two land in; and `advance_index`'s docstring (`connectedstore/apply.py::advance_index`) is an ARGUMENT, not a proof - it argues batch size "affects only latency/granularity, not the final materialized state or any semantic guarantee", which is exactly the claim this row observes has no Lean statement, so do not cite it as one.
### TK15 — model n-ary Expr so leg-7 leaf allocation is faithful to nested-union parenthesization
`pri=SOMEDAY size=L deps=- opened=2026-08-21b`

Lean's `Expr` is binary where Python's compiler flattens n-ary unions, so leaf allocation under leg 7 is faithful only up to parenthesization. `CORRESPONDENCE.md:807-851` declares it not-fixed with a stated reason: it is a `Core/` trust-root change touching every downstream proof, "far outside leg 7's scope". SOMEDAY is the honest pri: the decision was made and recorded, so this is revisited only on concrete need, e.g. if a corpus is found where the parenthesization actually changes leaf allocation observably. Trap: a trust-root edit invalidates every proof beneath it - anything touching `Core/` re-runs the whole Lean phase and can strand in-flight legs, so do not start this while leg 7 is executing. The Python side that flattens is `zanzibar_utils_v1.py::compile_ruleset`; provenance is COVERAGE.md PART 1 U-15 (`CD-3`, tier 2, sweep-d only), anchor re-resolved by COVERAGE.md §C4 at formal/CORRESPONDENCE.md:807-851.

### TK16 — joint inhabitation of a drained, non-trivially-reached state is empirical, not kernel-checked
`pri=SOMEDAY size=M deps=- opened=2026-08-21b`

`W4Witness` machine-checks that both hypothesis BUNDLES are inhabited by a real compiled boolean schema, so the final theorems are not vacuous. The honesty caveat (`formal/FINAL_REVIEW.md` §2, restated at `formal/ARCHITECTURE.md:208-213`) is that joint inhabitation of a drained, non-trivially-reached state is demonstrated empirically - the conformance driver plus the proved `cascade2_drains` (`formal/lean/ZanzibarProofs/GraphIndex/CascadeStrata.lean::cascade2_drains`) - rather than as a single kernel-checked term. This is published honesty about a residual, already stated in two places with a real empirical net underneath it; filed at SOMEDAY so it has an address, not because it is queued. Trap: do not read the caveat as "the theorems might be vacuous" - bundle inhabitation IS kernel-checked, it is the conjunction that is not, and overstating this is how a fair caveat becomes a false alarm. Provenance: COVERAGE.md PART 1 U-16 (`ARCH-H-12`, tier 2, sweep-h only).

### TK17 — unverified R6 lead A1: SetEngine.rebuild replays a full ORM row per tuple, no bulk bitmap load
`pri=HOLD size=? deps=- opened=2026-08-21b`

`setengine/engine.py::SetEngine.rebuild` - lead A1 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:763`. `rebuild()` materializes a full SQLModel entity per stored tuple and then applies them one at a time - two interner acquires, a `node_sets.get` and a per-element `.add` into a roaring bitmap, the access pattern roaring is worst at - for every tuple in the store, on every `__init__`/refresh/rollback; the sketch is column projection plus a two-pass batch apply that preserves id-assignment order and lands each bucket with one `ops.update` call. Finder: category build-speed, filed impact medium, algorithm change no. UNVERIFIED is the whole point of the row: the appendix preserves its leads "verbatim and UNVERIFIED" (`:757`), no adversarial pass confirmed the code does what is claimed or that the fix sketch is semantics-preserving, the lead carries no motivating measurement, and `docs/perf-next-round.md`'s reopening rule requires one before anything lands - the HOLD decision is the audit's own at `:756` (each finder returned 5-6 findings and only the top 3 by filed impact went to verification); promote to LATER only when a profile or `benchmarks/stmt_bench.py` run puts a number on it. Traps: re-verify against the code (the verified set above the appendix had fix sketches corrected and one fix refuted outright while its finding stood); read the audit's §"Traps the numbers do not carry" and count the bullets rather than trusting prose, since `migrate.py::R6_ROUND_WIDE_TRAPS` pins that count only for the 14 GENERATED `R6-N` rows and this hand-filed row rides no such refusal; a cited symbol may have MOVED (`R6-6`'s did - `BL-2` split `index_v4/wildcard.py::WildcardIndex.check` into a public deny fence plus `::WildcardIndex._check_internal` on 2026-08-21 and the audit still names `::check`, which `formal/conformance/anchor_check.py` cannot catch because it reads only `formal/CORRESPONDENCE.md` and both names exist). Instance-local interner ids are observable state - the sketch's whole soundness argument is that pass 1 keeps the acquire order byte-identical, since any reordering silently renumbers ids that `SetOps` bitmaps and every `(type, name, predicate)` surrogate lookup ride on, so pin the id assignment, not just the memberships. Provenance: COVERAGE.md PART 1 U-17, the `R6-A1..R6-A16` block (tier 3, sweep-g only; sweep-l never reached the appendix), appendix at :754-953, CONFIRMED OPEN AND UNCHANGED by COVERAGE.md §C3 (`grep -c 'R6-A'` -> 0, no id anywhere); lead 1 of 16.

### TK18 — unverified R6 lead A2: population() allocates a throwaway empty set on every call, hit or miss
`pri=HOLD size=? deps=- opened=2026-08-21b`

`setengine/engine.py::SetEngine.population` - lead A2 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:775`. `dict.get(k, self.ops.new())` evaluates its default EAGERLY, so every `population()` call constructs a fresh C-level `BitMap()` even on a hit; `population` is the `pop` callback threaded through the whole `MemberSet` algebra (`setengine/memberset.py::_starpop` calls it once per star shape inside every `_ext`/`_normalize`), i.e. several times per algebra step. The sketch is the two-line `m = get(k); return m if m is not None else new()`. Finder: category repeated-work, filed impact low, algorithm change no. UNVERIFIED is the whole point of the row: the appendix preserves its leads "verbatim and UNVERIFIED" (`:757`), the lead carries no motivating measurement, and `docs/perf-next-round.md`'s reopening rule requires one before anything lands - the HOLD decision is the audit's at `:756` (each finder returned 5-6 findings, only the top 3 by filed impact were verified); promote to LATER when a profile or `benchmarks/stmt_bench.py` run puts a number on it. Traps: re-verify against the code (fix sketches in the verified set were corrected and one refuted outright); read the audit's §"Traps the numbers do not carry" and count its bullets, since `migrate.py::R6_ROUND_WIDE_TRAPS` pins that count only for the 14 generated `R6-N` rows; and a cited symbol may have MOVED, as `R6-6`'s did when `BL-2` split `index_v4/wildcard.py::WildcardIndex.check` into a deny fence plus `::WildcardIndex._check_internal` on 2026-08-21. Provenance: COVERAGE.md PART 1 U-17, the `R6-A1..R6-A16` block (tier 3, sweep-g only), source :775 inside the appendix at :754-953, confirmed open by COVERAGE.md §C3; lead 2 of 16.

### TK19 — unverified R6 lead A3: _check_derived resolves the object node twice and decodes all 3 residue blobs
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/wildcard.py::WildcardIndex._check_derived` - lead A3 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:787`. In the `...`-subject branch, `_check_derived` resolves the object node for the edge probe and then, on a probe miss, calls `_residue_state`, whose body re-runs the identical point SELECT - outside a reconcile the `P3` `_residue_cache` is `None`, so every failed-probe derived check pays a duplicated node resolution; `_residue_state` also decodes all three JSON blobs and rebuilds fresh `set()`s even when the caller wants one O(1) membership answer, making a point check O(|neg|). Finder: repeated-work, medium, no. UNVERIFIED is the point of the row (appendix preserves leads "verbatim and UNVERIFIED", `:757`); no motivating measurement exists and `docs/perf-next-round.md`'s reopening rule requires one, so HOLD per the audit's own decision at `:756`, promoting to LATER only once a profile or `benchmarks/stmt_bench.py` run puts a number on it. Traps: re-verify against the code; read the audit's §"Traps the numbers do not carry" and count its bullets (`migrate.py::R6_ROUND_WIDE_TRAPS` covers only the 14 generated `R6-N` rows); a cited symbol may have MOVED, as with `BL-2`'s split of `WildcardIndex.check` into `::WildcardIndex._check_internal`. Critically, this lead and lead A14 (`:919`) are the same neighbourhood from two sides - A3 removes the duplicate resolve and memoizes per call, A14 proposes a persistent version-keyed snapshot cache on `WildcardIndex` - so adjudicate them together or the second lands on a moved target; both key invalidation on `ResidueV1.version` (I7), which is modelled by nothing formal (see the separately filed I7 modelling-gap row). Provenance: COVERAGE.md PART 1 U-17, `R6-A1..R6-A16` block, tier 3 sweep-g only; lead 3 of 16.

### TK20 — unverified R6 lead A4: EdgeV4 read indexes are not covering, so every probed row costs a heap fetch
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/models.py::EdgeV4.__table_args__` - lead A4 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:799`. The read queries project or filter columns the indexes do not carry: the check probe adds `indirect_edge_count > 0` on top of the `(store_id, subject_id, object_id)` unique-constraint seek, and `lookup_reverse` needs `subject_id` + `indirect_edge_count` while `ix_edge_v4_store_object` carries neither - so on PostgreSQL every matching edge of a popular object costs a heap fetch and on SQLite a rowid lookup per row; sketch is to widen the object-keyed index and optionally add a subject-keyed covering companion (pure DDL). Finder: lookup-speed, filed impact low, algorithm change no. UNVERIFIED is the point of the row (`:757`); no motivating measurement, so HOLD per the audit's decision at `:756`, promote to LATER only on a profile or `benchmarks/stmt_bench.py` number. Traps: re-verify against the code; read §"Traps the numbers do not carry" and count its bullets (`migrate.py::R6_ROUND_WIDE_TRAPS` guards only the 14 generated rows); a cited symbol may have MOVED (`BL-2`'s split of `WildcardIndex.check` into `::WildcardIndex._check_internal`). This collides with `R6-18`, which is LATER and MOTIVATED at 53.1% off the same table: `R6-18` drops the surrogate PK and the redundant unique constraint, while widening indexes here adds bytes and write amplification to the biggest table in the system - land `R6-18` first or co-design them, and measure with `benchmarks/stmt_bench.py` before adopting either (the N5 audit's one-index-per-question house style is the standard). Related edge `R6-18` was added 2026-08-24b by the related-edge sweep (trial finding F1) because R6-18 did not name this row back and was blind to the collision. Provenance: COVERAGE.md PART 1 U-17, `R6-A1..R6-A16`, tier 3 sweep-g only; lead 4 of 16.

### TK21 — unverified R6 lead A5: bridge maintenance issues up to ~8 point existence probes per tuple write
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/wildcard.py::WildcardIndex._ensure_own_bridges` - lead A5 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:811`. `_ensure_own_bridges` probes each bridge with its own point SELECT, `_add_tuple_trusted` runs it on BOTH endpoints, and `_ensure_entity_middles` repeats a node resolve plus both probes per crossable shape on every write - in steady state (bridges already exist, the common case) that is 4-8 sequential round trips per `add_tuple` that all return "exists"; the read path already shows the fix, since `check` puts every candidate probe key into ONE row-value `IN`, and the remove side repeats the pattern in `_strip_bridges`. Finder: write-speed, filed impact medium, algorithm change no. UNVERIFIED is the point of the row (`:757`): no adversarial pass, no motivating measurement, and `docs/perf-next-round.md`'s reopening rule requires one - HOLD is the audit's own decision at `:756`, promote to LATER only when a profile or `benchmarks/stmt_bench.py` run puts a number on it. Traps: re-verify against the code; read §"Traps the numbers do not carry" and count its bullets (`migrate.py::R6_ROUND_WIDE_TRAPS` guards only the 14 generated `R6-N` rows); a cited symbol may have MOVED, as `R6-6`'s did with `BL-2`'s split into `::WildcardIndex._check_internal`. Provenance: COVERAGE.md PART 1 U-17, `R6-A1..R6-A16`, tier 3 sweep-g only, appendix :754-953; lead 5 of 16.

### TK22 — unverified R6 lead A6: full-tier invariant checker ORM-loads all nodes and edges twice per commit
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/invariants.py::_load` - lead A6 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:823`. `_load` does full ORM entity scans of `NodeV4` and `EdgeV4` at the top of `check_invariants`, which at the full tier runs per commit pre-commit AND again post-commit in a fresh session - and the same file already learned this lesson for the cheap tier, where the comment records ORM materialization as the bulk of the tier's cost and the `_NodeFacts`/`_ResidueFacts` NamedTuple pattern is already in place; the checker only ever READS attributes. Finder: write-speed, filed impact medium, algorithm change no. UNVERIFIED is the point of the row (`:757`); no motivating measurement, so HOLD per the audit's decision at `:756`, promote to LATER only on a profile or `benchmarks/stmt_bench.py` number. Traps: re-verify against the code; read §"Traps the numbers do not carry" and count its bullets; a cited symbol may have MOVED (`BL-2`'s split of `WildcardIndex.check` into `::WildcardIndex._check_internal`). Above all, this is the paranoia instrument, not the subject: paranoia mode is ON by default via `make_wildcard_index`, so a column-select rewrite that quietly narrows what a check can see weakens every test in the suite at once and would fail by PASSING - the house failure mode - so sabotage it per `docs/sabotage-procedure.md`, breaking an invariant each tier is supposed to catch and watching it go red BEFORE and AFTER. Provenance: COVERAGE.md PART 1 U-17, `R6-A1..R6-A16`, tier 3 sweep-g only; lead 6 of 16.

### TK23 — unverified R6 lead A7: facade edge ops re-pay the _require_live_nodes SELECT under the held lock
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/wildcard.py::WildcardIndex._add_tuple_trusted` - lead A7 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:835`. `_add_tuple_trusted` resolves endpoints under `_lock_store()` and then calls `add_edge_by_id`, which re-takes the lock and runs `_require_live_nodes` - one extra IN-SELECT; Core's own name-based `add_edge` documents skipping exactly this ("resolved under the lock: live by construction, no re-verification round trip"), and every facade edge op repeats it - the grant, each bridge add, each bridge strip, and `remove_tuple`'s grant removal - so one bridged add pays up to 3 redundant liveness queries. Finder: repeated-work, filed impact low, algorithm change no. UNVERIFIED is the point of the row (`:757`); no motivating measurement, so HOLD per the audit's decision at `:756`, promote to LATER only on a profile or `benchmarks/stmt_bench.py` number. Traps: re-verify against the code; read §"Traps the numbers do not carry" and count its bullets; a cited symbol may have MOVED (`BL-2`'s split into `::WildcardIndex._check_internal`). Decisive trap: this deletes a defense-in-depth check on a write path for a LOW-impact win - the finder says so itself and requires a sabotage test on the trusted entry before it is trusted, since a liveness check removed on a wrong argument is a fail-open on the write side; weigh that against "filed impact: low" before spending the risk. Provenance: COVERAGE.md PART 1 U-17, `R6-A1..R6-A16`, tier 3 sweep-g only; lead 7 of 16.

### TK24 — unverified R6 lead A8: five cascade sites use per-id session.get N+1 loops beside the batch helper
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/processor.py::DeltaProcessor._fan_out` - lead A8 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:847`. `_fan_out`'s ttu and userset branches, `_map_deltas_to_keys`' ttu arm, `_keys_referencing` and the subject-GC pre-pass each issue one `session.get` per id; `lookup_reachable` returns the FULL transitive-closure descendant set, so a well-connected entity turns one fan-out into hundreds of point SELECTs. `_nodes_by_ids` exists precisely for this - its docstring says "replacing per-id session.get N+1 loops" - and these five sites do not use it; the sketch is to batch through the helper, or push the `(type, predicate)` filter into SQL. Finder: write-speed, filed impact medium, algorithm change no. UNVERIFIED is the point of the row (`:757`); no motivating measurement and `docs/perf-next-round.md`'s reopening rule requires one, so HOLD per the audit's decision at `:756`, promote to LATER only on a profile or `benchmarks/stmt_bench.py` number. Traps: re-verify against the code; read §"Traps the numbers do not carry" and count its bullets (`migrate.py::R6_ROUND_WIDE_TRAPS` guards only the 14 generated rows); a cited symbol may have MOVED (`BL-2`'s split into `::WildcardIndex._check_internal`). Provenance: COVERAGE.md PART 1 U-17, `R6-A1..R6-A16`, tier 3 sweep-g only; lead 8 of 16.

### TK25 — unverified R6 lead A9: lookup_reachable/lookup_reverse hydrate full ORM rows to project one id
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/core.py::ReachabilityIndex.lookup_reachable` - lead A9 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:859`. Both helpers select whole `EdgeV4` entities and then keep one int column, discarding `indirect_edge_count == 0` rows in Python after transferring them; these are the cascade's hottest primitives, since `_incoming_concretes` calls `lookup_reverse` per closure leaf of every full-object reconcile and `_fan_out`/`_map_deltas_to_keys` call `lookup_reachable` per dependency edge per changed object. Sketch: project and filter in SQL. Finder: write-speed, filed impact medium, algorithm change no. UNVERIFIED is the point of the row (`:757`); no motivating measurement, so HOLD per the audit's decision at `:756`, promote to LATER only on a profile or `benchmarks/stmt_bench.py` number. Traps: re-verify against the code; read §"Traps the numbers do not carry" and count its bullets; a cited symbol may have MOVED (`BL-2`'s split into `::WildcardIndex._check_internal`). The audit itself marks this "overlaps verified `R6-5`", which is LATER and MOTIVATED at 32.7% of profiled time for the read-path twin of the same ORM-hydration defect - do not work this row first and do not double-count the win: land `R6-5`, re-profile, and only then decide whether a cascade-side residue remains; this was deliberately NOT encoded as a `deps` edge because no row in this round carries one. Related edge `R6-5` added 2026-08-24b by the related-edge sweep (trial finding F1), since R6-5 was blind to the overlap and is the end where the double-count would happen. Provenance: COVERAGE.md PART 1 U-17, `R6-A1..R6-A16`, tier 3 sweep-g only; lead 9 of 16.

### TK26 — unverified R6 lead A10: residue version bumps escalate every dependent to a full reconcile (Lean)
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/processor.py::DeltaProcessor._run_cascade` - lead A10 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:871`. Every `_store_residue` bump fans out with a `None` value, meaning full-object reconcile, and `_fan_out`'s computed branch does the same - so a cheap-path flip of ONE subject forces each dependent object through the complete `_reconcile` pipeline (stars_fn, full candidate/audit enumeration, check_fn over EVERY member), re-evaluating subjects whose inputs did not change; sketch is to propagate granularity as `(key, changed_subjects | None)`, routing computed dependents through the existing cheap `reconcile_subject` path while ttu/userset dependents keep full reconciles. Finder: write-speed, filed impact medium, algorithm change: YES. UNVERIFIED is the point of the row (`:757`); no motivating measurement, so HOLD per the audit's decision at `:756`, promote to LATER only on a profile or `benchmarks/stmt_bench.py` number. Traps: re-verify against the code; read §"Traps the numbers do not carry" and count its bullets; a cited symbol may have MOVED (`BL-2`'s split into `::WildcardIndex._check_internal`). Uniquely among the sixteen, THIS ONE CHANGES THE MODELED ALGORITHM and owes a Lean update: it rewrites the cascade invalidation rule (boolean spec §5.2), exactly `CLAUDE.md`'s "Perf work & the Lean model" case where the corresponding Lean definition would describe dead code - update the model and re-run `formal/verify.sh` phased per gate-runbook §2, or log the gap in `formal/CORRESPONDENCE.md` §7 (§8: do not let code and model drift unrecorded). The finder's own note adds that the §5.4 symbolic-delta full-object rule must be preserved (star-covered members hold no edges to invalidate them individually); budget the Lean work as part of this row, not as a surprise. Provenance: COVERAGE.md PART 1 U-17, `R6-A1..R6-A16`, tier 3 sweep-g only; lead 10 of 16.
### TK27 — unverified R6 lead A11: Phase-W node write still builds ~165k ORM objects while siblings use dicts
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/bulk_build.py::bulk_build` — lead A11 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:883`. Phase W builds a full dict of `NodeV4` ORM objects, `add_all`s them, flushes, reads ids back off the live instances and manually expunges each one — while edges, residues, residue-refs and outbox rows already go through `session.execute(insert(Model), chunk)` with plain dicts; the N18 comment in the code records the scale (~165k objects), and on SQLAlchemy 2.0.51 the flush SQL is already batched so the residue is pure Python churn. Sketch: chunked core insert over `sorted(nodes)` (same insertion order, so ids are assigned identically) plus one read-back SELECT. Finder: build-speed, filed impact medium, algorithm change no. UNVERIFIED is the whole point of the row — the appendix preserves its leads "verbatim and UNVERIFIED" (`:757`), no adversarial pass confirmed the code does what is claimed, and there is no motivating measurement, which `docs/perf-next-round.md`'s reopening rule requires before anything lands; promote to `LATER` only when a profile or `benchmarks/stmt_bench.py` run puts a number on it. Traps: re-verify against the code (the verified set above the appendix had fix sketches corrected and one fix refuted outright while its finding stood); read the audit's §"Traps the numbers do not carry" and count the bullets (`migrate.py::R6_ROUND_WIDE_TRAPS` pins that count for the 14 GENERATED `R6-N` rows but this hand-filed row rides no such refusal); a cited symbol may have MOVED, as `R6-6`'s did when `BL-2` split `index_v4/wildcard.py::WildcardIndex.check` into a public deny fence plus `::WildcardIndex._check_internal` on 2026-08-21 while the audit still names `::check`, and `formal/conformance/anchor_check.py` cannot catch that class since it reads only `formal/CORRESPONDENCE.md`; and node id assignment order is load-bearing — the sketch's correctness rests on inserting in the same sorted order, with `bulk_build`'s Python-vs-Python identity gate as the net (see `R6-19`'s lesson about the bulk edge audit before assuming the gate is cheap). Parent `R6` carries the re-run recipe (`python -m benchmarks.profile_r6[_write] --target <t>`, never beside another bench or a pytest run).

### TK28 — unverified R6 lead A12: boolean bulk build holds three full-closure copies in RAM at once
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/bulk_build.py::bulk_build` — lead A12 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:895`. On a boolean schema the backfill's `reach_out`/`reach_in` (full transitive reachability in both directions, as Python sets) stay referenced for the rest of the function even though only `derived_pairs`/`explicit`/`residues` are read afterwards — so ~2x closure sits in RAM while Phase P builds `pvec` (the closure again, as dict-of-dicts) and a third full-closure list `edge_pairs` is materialized; N18 already had to bound Phase-W row dicts because peaks hit ~3x the DP at 200k tuples, and these two allocations are the same RSS class and are untouched. Sketch: drop `bf` before `pvec` exists, and iterate instead of materializing `edge_pairs`. Finder: space, filed impact medium, algorithm change no. UNVERIFIED per the appendix's own words (`:757`) with no motivating measurement, which `docs/perf-next-round.md`'s reopening rule requires; `HOLD` is `docs/README.md` §5's "deferred by an explicit recorded decision", the decision being the audit's at `:756` (each finder returned 5-6 findings, only the top 3 by filed impact went to verification). Traps: re-verify against the code; read §"Traps the numbers do not carry" and count the bullets rather than trusting prose; a cited symbol may have MOVED (the `BL-2` split of `::WildcardIndex.check` into `::WildcardIndex._check_internal`); and this is space, not time — `benchmarks/profile_r6*` measures wall time and statement counts, so an RSS change needs its own instrument, decided before deciding whether to land it or the "win" is unfalsifiable.

### TK29 — unverified R6 lead A13: catch_up defaults to one unbounded batch under a single lock and commit
`pri=HOLD size=? deps=- opened=2026-08-21b`

`connectedstore/store.py::ConnectedStore.catch_up` — lead A13 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:907`. `catch_up(batch=None)` makes `advance_index` call `log_rows(..., limit=None)`, which materializes EVERY `TupleLogV1` row past the cursor as ORM objects in one list, builds the contiguity check over all of them, and runs the entire drain under one `_lock_store()` FOR UPDATE and one commit; `advance_index`'s own docstring already proves batch size "affects only latency/granularity, not the final materialized state or any semantic guarantee", and the per-batch loop and commit already exist — the batching is simply never engaged by default. Finder: space, filed impact medium, algorithm change no. UNVERIFIED per the appendix (`:757`), no motivating measurement, `HOLD` by the audit's `:756` decision; promote to `LATER` only when a profile or `benchmarks/stmt_bench.py` run puts a number on it. Traps: re-verify against the code; count the bullets in §"Traps the numbers do not carry"; a cited symbol may have MOVED (the `BL-2` `::check` / `::_check_internal` split); and this is a live-behaviour change to the HA/replica path, not a micro-optimization — changing the default shortens the writer-blocking `FOR UPDATE` hold and splits one transaction into many, visibly changing replica catch-up latency and what a concurrent reader observes mid-drain, so exercise it on the opt-in PostgreSQL leg (`bash scripts/pg_local.sh start`, then `ZANZIBAR_TEST_DSN=<dsn>`), since a SQLite-only green proves little. `docs/architecture/correctness.md` holds the multi-writer/HA argument this default sits inside.

### TK30 — unverified R6 lead A14: derived check re-SELECTs and re-decodes the whole residue outside reconcile
`pri=HOLD size=? deps=- opened=2026-08-21b`

`index_v4/wildcard.py::WildcardIndex._residue_state` — lead A14 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:919`. `_residue_state` memoizes only when a per-reconcile cache is installed, and that cache is set exclusively inside `DeltaProcessor._residue_cache_scope` — `None` on the ordinary read path — so every derived-relation `check` performs a node SELECT, a `ResidueV1` SELECT and three full JSON decodes, then materializes fresh sets, all to answer one `subj.id in neg` probe: O(|residue|) per check on exclusion-heavy objects instead of O(1). Sketch: a persistent decoded-snapshot cache on `WildcardIndex` keyed by `(o_type, relation, o_name)` and validated by `ResidueV1.version`. Finder: lookup-speed, filed impact medium, algorithm change no. UNVERIFIED per the appendix (`:757`) with no motivating measurement, `HOLD` by the audit's `:756` decision. Traps: re-verify against the code; count the bullets in §"Traps the numbers do not carry"; a cited symbol may have MOVED (the `BL-2` split); and critically, a cross-call cache on a read path is the W2 staleness hazard — `R6-6`'s verifier explicitly praised its own fix for having "no cross-call cache lifetime, so the W2 staleness/rowid-reuse hazard is not reintroduced", and this lead reintroduces exactly that lifetime, so if `ResidueV1.version` is the invalidation token then the correctness of every derived read rests on I7, the invariant that NOTHING FORMAL MODELS and that today is pinned only by `tests/` paranoia runs (see the separately filed I7 modelling-gap row; lead A3 at `:787` touches the same function from the other side). `index_v4/models.py::ResidueV1` holds the `version` column and what I7 claims about it.

### TK31 — unverified R6 lead A15: set-engine check/expand re-walk the SchemaAST with isinstance dispatch
`pri=HOLD size=? deps=- opened=2026-08-21b`

`setengine/engine.py::SetEngine.check.sat_expr` — lead A15 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:931`. `sat_expr` and `do_expr` re-interpret a lifetime-stable AST on every call, running a chain of up to six `isinstance` tests per node visit — and the codebase already has the compile-once pattern for exactly this (`zanzibar_utils_v1.py::_compile_check_fn` builds closure-composed plans with "no AST walk, no per-node dispatch, short-circuit"), but only for the graph side's derived plans, never for the set engine's evaluator, whose `check` is the inner loop of `lookup`. Sketch: AOT-compile each `(type, relation)` `Expr` at `__init__` into generator-closures mirroring `sat_expr`'s exact statement order. Finder: repeated-work, filed impact medium, algorithm change no. UNVERIFIED per the appendix (`:757`), no motivating measurement, `HOLD` by the audit's `:756` decision. Traps: re-verify against the code; count the bullets in §"Traps the numbers do not carry"; a cited symbol may have MOVED (the `BL-2` split); and "byte-identical control flow" is the claim while short-circuit ORDER is what can silently move — the `_drive` heap-stack protocol and the lowlink memo guard must survive verbatim, with the validation matrix under BOTH `SetOps`, hypothesis and conformance as the net, and check that the `CORRESPONDENCE.md` anchors on `check`/`expand` still resolve while remembering they resolve on NAME alone, so keeping the name is not evidence the model still describes the code (the `R6-6` lesson).

### TK32 — unverified R6 lead A16: Interner keeps key_of/refcount as int-keyed dicts though ids are dense
`pri=HOLD size=? deps=- opened=2026-08-21b`

`setengine/engine.py::Interner.__init__` — lead A16 of the 16-item appendix, `docs/perf-round6-audit-2026-08.md:943`. Ids are allocated densely from `_next` with a free-list precisely so they stay compact for roaring — yet `key_of` and `refcount` are hash maps keyed by those dense ints, costing ~80-100 bytes/entry of dict overhead plus boxed keys against 8 bytes/slot for a list indexed by id; for an in-memory backend meant to hold whole stores this is an O(live-entities) constant-factor memory multiplier on two of the three interner tables. Sketch: plain lists indexed by id, sentinel on release. Finder: space, filed impact low, algorithm change no. UNVERIFIED per the appendix (`:757`), no motivating measurement, `HOLD` by the audit's `:756` decision. Traps: re-verify against the code; count the bullets in §"Traps the numbers do not carry"; a cited symbol may have MOVED (the `BL-2` split); and recycled int32 ids plus a sentinel is a use-after-free waiting to be written — `release` writing a sentinel instead of `del` means a stale id reads `None`/`0` rather than raising `KeyError`, turning a loud failure into a silent one, so decide what a read of a released id must DO, and pin it, before changing the container.

### TK33 — store-level rebuild-instead-of-K-deltas IVM amortisation, measured crossover K* ~ 30-40
`pri=HOLD size=L deps=- opened=2026-08-21b`

The evaluated proposal ("if we detect a DoS-causing fan-out, do a bulk rebuild instead of adding the thing the usual way") was refuted as stated — the measurement table at `docs/spec-deviations.md:805-816` separates three costs it conflated. But the same entry records a different idea that survived: store-level rebuild instead of applying K pending deltas is a sound IVM amortisation with a measured crossover at K* ~ 30-40, described as a better idea than the per-write version and composable with async; it was recorded and not adopted. `HOLD` because it inherits the same blockers 2-4 as the refuted version — mid-stream rebuild refusal, the quiescence requirement, and reintroducing a lock — and none has been adjudicated. Traps: the neighbouring proposal in the same entry is REFUTED and they read alike, so quote the K* line (`:859`), not the entry's headline, or you implement something the repo already measured and rejected; and a rebuild that can refuse mid-stream is a fail-open shape — the standing rule for the closure fan-out cap is that removals are EXEMPT because a cap that can refuse a revocation is a fail-open (`CLAUDE.md`, `ZANZIBAR_MAX_CLOSURE_FANOUT`), so any amortisation that can decline to apply pending deltas owes the same argument. Read `docs/spec-deviations.md:855-862` (surviving idea and crossover) and `:801-816` (refuted proposal plus the measurement table that killed it); `connectedstore/apply.py::advance_index` is the apply step any amortisation replaces.

### TK34 — benchmarks/canary.py nightly perf tripwire: proposed, decision pending, never built
`pri=LATER size=M deps=- opened=2026-08-21b`

`docs/gate-runbook.md:752-762` carries a concrete lightweight proposal: build one fixed store (e.g. `simple` N=8000), time a fixed op mix (K writes, K lookups, K checks), print rates, and compare to a recorded baseline with a generous threshold (flag only a >2x regression), "runs in seconds" — sitting between grepping test durations (free, noisy) and a full `scale_bench` sweep (accurate, minutes). The decision was left pending and `benchmarks/canary.py` still does not exist. Filed `LATER` rather than `HOLD` because, unlike the appendix leads, it is fully specified and the only open question is whether anyone wants it — and perf round 6 is about to land a series of changes with no regression tripwire behind them. Traps: the runbook's own recommendation is DO NOT GATE ON IT, since machine variance makes a hard threshold flaky and the proposal is a nightly alarm, not a gate phase (wiring it into `formal/verify.sh` would make the gate flaky, which is worse than having no canary); and a tripwire is an assurance step so it can fail by passing — a canary whose baseline is regenerated on every run alarms never, so sabotage it per `docs/sabotage-procedure.md` by planting a 2x slowdown, watching it fire, and recording the literal output in the file's docstring. `benchmarks/scale_bench.py` / `benchmarks/stmt_bench.py` are the existing instruments and store builders to reuse.

### TK35 — sink/source bridge elision for provably-safe shapes -- a spec'd future optimization
`pri=SOMEDAY size=M deps=- opened=2026-08-21b`

`bridged_out_shapes` returns every declared object-wildcard shape, with the comment "(Sink-shape elision is a future optimization; be conservative now.)" — in the spec at `docs/specs/wildcard-materialization-spec.md:100` and mirrored in the code at `zanzibar_utils_v1.py:376`. A shape that can never be a bridge source (or sink) does not need its bridges materialized; this is deliberately unbuilt and has never carried an id. `SOMEDAY` because it is an optimization with no measurement behind it, in an area where being conservative was a deliberate choice. Trap: bridges are correctness furniture, not just cost — eliding one on a wrong safety argument is a missing grant or a missing revocation, and `BL-1` (the released-userset bridge leak) is the recent reminder of how that class shows up, so whatever "provably safe" means here has to be provable; §10 line 324 lists this under non-goals for now. Relevant code: `zanzibar_utils_v1.py::SchemaInfo.bridged_out_shapes` (the conservative implementation and its comment) and `index_v4/wildcard.py::WildcardIndex._ensure_own_bridges` (what elision would skip).

### TK36 — delta post-processing layer to expand symbolic wildcard PermissionDeltas -- no id anywhere
`pri=SOMEDAY size=L deps=- opened=2026-08-21b`

`docs/specs/wildcard-materialization-spec.md:259`: "`PermissionDelta`s that mention a wildcard node id are **symbolic** ('everyone of shape S gained/lost X'). Pass them through untouched and document this in the README delta section; expansion is a future post-processing layer, out of scope." The pass-through shipped; the expansion layer has no id and no later doc tracking it. `SOMEDAY` because a consumer that needs expanded deltas is the concrete need that would justify it, and none exists. Trap: "never enumerate a marker into concretes" is a design invariant elsewhere in the same spec (`:258`, for `lookup`/`lookup_reverse` results), and an expansion layer is exactly that enumeration done deliberately in one place — whoever builds it must say why the invariant holds for reads and not here, or the layer will be copied into a read path by the next reader. Also read §10 line 324 (the deferral) and the `README.md` delta section, where the symbolic pass-through is documented.

### TK37 — hot multi-reader deployments want a shared invalidation signal, not per-reader log tailing
`pri=SOMEDAY size=M deps=- opened=2026-08-21b`

A stale tokened read tails only the committed log delta into the evaluator (`catch_up_evaluator` -> `apply_logged`, O(delta)), not a full rebuild. `docs/architecture/correctness.md:104-110` calls that "fine at human scale" and names the scaling gap: "a hot multi-reader deployment would still want a shared invalidation signal to avoid per-reader tailing" — a documented scaling gap with no row. `SOMEDAY` by the vocabulary's definition: revisit on a concrete need, and the concrete need is a deployment shape this repo does not have. Trap: this is a correctness-adjacent surface, not just throughput — the freshness contract (`at_least`, `StaleRead`) is what makes tokened reads correct across sessions, and a shared invalidation signal is a new way for a reader to believe it is fresh, so any design must state what happens when the signal is LOST, and the answer must fail closed. The per-reader tail is `connectedstore/store.py::ConnectedStore.catch_up_evaluator`; the gap's freshness context in `docs/architecture/correctness.md:104-110` is LIVING.

### TK38 — pin the referenced-vs-invisible NodeV4 split (217/49): the definition has to be written first
`pri=HOLD size=S deps=- opened=2026-08-21b`

The `NodeV4` endpoint split re-derives as 217/49 (from an earlier 194/41) but is deliberately NOT under the count pin: unlike the edge ledger it has no in-repo implementation to reuse, so "referenced" had to be reconstructed (edge endpoints union residue object nodes union residue `neg`/`upos` subject nodes) and the number is method-sensitive. The recorded instruction at `formal/CORRESPONDENCE.md:560-565` is: "Treat **266** as solid and 217/49 as provisional; pinning it needs the definition written down first." `HOLD` because the blocking step is a definition, not a patch, and nobody has committed to one. Traps: pinning a method-sensitive number is how you get a pin that agrees with itself by construction — the definition has to come from something other than the script that measures it, or the pin proves only that the script is deterministic, which is the `ZT-P3-5` family this repo keeps re-learning; and this is distinct from `P16`, which widens enumeration/state bounds (K, store counts) while this is a node-classification definition (a `related: [P16]` link was added 2026-08-24b by the related-edge sweep, trial finding F1, because "Distinct from P16" is stated only here and P16 was the blind end). Read `formal/HANDOFF.md` first for any formal item (the corpus convention; `migrate.py::check_formal_pointer` enforces it for the generated rows and 18/18 formal tasks satisfy it), and `formal/conformance/doc_counts.py` is the generated-counts machinery a pin would ride (`verify.sh` step 4e).
### TK39 — cross-namespace relations support (README open question, no design)
`pri=SOMEDAY size=L deps=- opened=2026-08-21b`

`README.md:399` — *"cross-namespace relations?"* — sits in the author's open-question list. Today a relation resolves within its declaring namespace; supporting relations that cross namespaces is an unscoped feature question with no design, no tests and no consumer, so it is SOMEDAY by definition and should be revisited only on a concrete need. Trap: this is a SCHEMA-shape change, and schemas are static here — `SchemaV4` is write-once and a new schema means a new store/index (`CLAUDE.md`, `connectedstore/`), so any design has to survive that constraint. It also touches the parser (`zanzibar_utils_v1.py::parse_openfga_schema`), `compile_ruleset`, both backends, and `tests/oracle.py`, which parses the DSL itself and must be changed SEPARATELY or the independence contract is broken. Provenance: COVERAGE.md PART 1 U-24 (`J-9`, tier 4, sweep-j only); README.md last changed 5e4b770, 2026-08-16.

### TK40 — shared tuples / shared state support (README open question, no design)
`pri=SOMEDAY size=L deps=- opened=2026-08-21b`

`README.md:400` — *"shared tuples / state?"* — from the author's open-question list, with no design, no consumer and no tests; SOMEDAY by definition. Trap: the source of truth is per-store and the index is a materialized view of it — `TupleV1` + `TupleLogV1` are the truth and the graph index is derived, so anything "shared" across stores has to say what that means for the log, the watermark, and `_lock_store`'s per-store serialization. That design does not exist. Read `connectedstore/source.py::TupleSource`, the per-store source of truth. Provenance: COVERAGE.md PART 1 U-25 (`J-10`, tier 4, sweep-j only).

### TK41 — schema type checking so every relation resolves to a single type (README open question)
`pri=SOMEDAY size=M deps=- opened=2026-08-21b`

`README.md:463-464` — *"schema type checking, so that all relations always resolve to a single type?"*, with the author's own alternative on the next line: *"or resolve by relations and do duck-typing checks instead? this is more correct maybe but also more effort"*. Two candidate designs, neither chosen, and no forcing need. Trap: reads are LENIENT by contract and that is load-bearing — an out-of-charset or undeclared name simply never matches, on both backends and in the oracle (`CLAUDE.md`, Identifiers); a type checker that turns a non-match into an ERROR changes the read contract and would have to be adjudicated against that rule, not just implemented. Relevant existing write-side admission: `zanzibar_utils_v1.py::validate_write_identifiers`. Provenance: COVERAGE.md PART 1 U-26 (`J-11`, tier 4, sweep-j only); question re-read at `README.md:462-465`.

### TK42 — conditional transitions / default-condition support (README open question, no design)
`pri=SOMEDAY size=L deps=- opened=2026-08-21b`

`README.md:403-404` — *"conditional transitions?"* and *"default condition exists?"*. OpenFGA has conditions; this implementation has none, and the questions are recorded without a design. Trap: conditions are evaluated at CHECK time while this design materializes closure at WRITE time — that is the whole cost model of the graph index (`check` is O(1) because the transitive closure is materialized), so a condition that depends on request context cannot be materialized. This is therefore not a feature addition but a question about which backend can support it at all, and the answer may be "the set engine only"; see [`docs/architecture/overview.md`](docs/architecture/overview.md) for the two cost models it would have to live in. Provenance: COVERAGE.md PART 1 U-27 (`J-8`, tier 4, sweep-j only).

### TK43 — two README editorial TODOs: 'continue story another day' and the MAFSA word-count trick
`pri=SOMEDAY size=S deps=- opened=2026-08-21b`

Two author's-voice editorial markers in `README.md`: `:142` *"todo: continue story another day"* (§*Overcomplicating things*) and `:150` *"todo: either write something or reference"* (the MAFSA word-count trick). Both are restated in the FROZEN `formal/history/handoff-status-2026-08-16.md:569-570`, which is why the sweep found them twice. These are author's-voice edits only — nobody else can write them, and nothing depends on them. Trap: do not "resolve" them by deleting the markers; they mark deliberately unfinished prose in the author's own voice, and a silent deletion is a worse outcome than an honest TODO, since this repo's whole doc discipline is that a residue is published rather than rounded away. Provenance: COVERAGE.md PART 1 U-28 (`J-1`+`J-2`+`J-30`, tier 4; sweep-j twice plus a self-dupe across two files).

### TK44 — ~30% of the generator pair-cell space is unreached even at deep budget, with UNKNOWN residue
`pri=HOLD size=M deps=- opened=2026-08-21b`

Best measured union is **891/1275** pair cells (`docs/design/generator-coverage/README.md:566`, FROZEN): some of the residue is REJ-with-witness and legitimately unreachable, the rest is UNKNOWN with no witness explaining it. The LIVING carrier is [`docs/sabotage-procedure.md`](docs/sabotage-procedure.md)`:482-487`, which uses it as a methodological caution: *"read a green gate as 'the instruments we have found nothing', never as 'there is nothing'."* It is HOLD and not a defect because it is published honesty, deliberately not rounded away, and the design doc's own position is that the number should be published rather than eliminated; what has never been decided is whether the UNKNOWN residue is worth attacking, or whether publishing it is the whole intended answer — that decision is the work. Traps: the obvious "fix" is the forbidden one — `tests/test_generator_coverage.py:257` explicitly argues AGAINST a hand-written `EXPECTED_UNREACHABLE` list (*"a future silent gap"*) and does not have one, so closing cells by exempting them is the failure mode, not the fix; and do not quote a percentage, since the two carriers say ~30% and "roughly a quarter" of the same measurement — cite the fraction at its source with the date and re-measure first. Provenance: COVERAGE.md PART 1 U-29 (`J-27`, tier 4; sweep-j + sweep-k, two documents); sweep-k guessed `GS-1`, which is a closed gate-tree-id row and does not cover it. Measurement source: `docs/design/generator-coverage/README.md:564-570` §6 (FROZEN).

### TK45 — two doc-hygiene residuals from the frozen migration map: banner drift and stale inbound links
`pri=HOLD size=M deps=- opened=2026-08-21b`

Two residuals recorded in the FROZEN migration map [`docs/history/handoff-migration-map-2026-08.md`](docs/history/handoff-migration-map-2026-08.md) and never adjudicated: **(a)** frozen-banner enforcement was a ONE-TIME sweep, with no ongoing check for design docs that land afterwards (`:591-595`); **(b)** a long tail of stale inbound `HANDOFF.md` citations across the tree — `README.md`, `scripts/pg_local.sh`, several tests citing it by line or section (`:637-641`). HOLD because neither was verified: the map is frozen provenance and the first step is a measurement, not a fix; (a) is partly the class the banner row tracks generically, (b) is its own thing. Traps: (b) is about to get much worse or much cheaper depending on timing — if the `HANDOFF.md` board is replaced by a short stub, every citation naming a LINE or SECTION of it breaks at once, and `scripts/handoff_lint.py::check_doc_links` catches only a broken LINK, never a stale DESCRIPTION, so measure the inbound tail BEFORE that lands; and (a) wants a mechanical refusal, not a doc warning, per `CLAUDE.md`'s standing rule, because a one-time sweep with no ongoing check is precisely a doc warning. Liveness states and the banner rule (a) would enforce live in [`docs/README.md`](docs/README.md) §2. Provenance: COVERAGE.md PART 1 U-30 (`J-17`+`J-18`, tier 4, sweep-j only), ranked lowest there because both are frozen-provenance and were never confirmed or refuted.

### TK48 — perf audit banner says 'ten to land, five declined'; its own body says eleven and four
`pri=LATER size=S deps=- opened=2026-08-21b`

The status banner of `docs/perf-round6-audit-2026-08.md` (`:3-5`) reads *"ALL EIGHTEEN are now MEASURED (2026-08-17) — ten recommended to land in a stated order, five declined on an upper bound, three unreachable by any benchmarked workload."* The document's own body says **eleven** ids in the recommended order and **four** declines (exactly four `NOT MOTIVATED` verdict rows); both splits sum to 18, which is how it survived review. This is a live `ZT-P3-5` (a stale figure in a durable place) in the first file a perf session reads, and it is the SOURCE the task corpus was generated from — the corpus correctly declined to inherit the banner's split and counted the children instead, but the doc was never corrected. `.scratch/tasktool/PROOF2.md` §1 independently re-derived 11 / 4 / 3 twice from the audit body alone (11 ids in 10 steps, 4 NOT MOTIVATED rows, 3 unreachable = the audit's own "ALL EIGHTEEN"). SECOND DEFECT IN THE SAME BANNER, found 2026-08-24d while closing TK47: `docs/perf-round6-audit-2026-08.md:3` also says 'Nothing here is landed', now false twice over — R6-10 landed 2026-08-20b (2.54x) and R6-6 landed 2026-08-24d (4.75 -> 1.75 statements/check); both clauses are stale, so fix them in ONE reviewed edit rather than two, and the landed items now carry a LANDED marker in the R6-6 verdict row and entry so the body can be counted for that too. Traps: correct the banner from the BODY (the verdict tables and recommended-order list), not from any other row or task, and the safest form carries no number at all and points at the tables; `R6`'s parent-row prose quotes the wrong cell ON PURPOSE ("9 to land, 5 declined", the board cell it replaced, with the correction beside it) — that is provenance, do not "fix" it into agreement; and this is a `docs/` edit, so file it on its own with the derivation in the commit message, not as a by-product of any perf item. Parent row: R6.

### TK49 — HS-5 undercounts its own scope: eleven docs lack a liveness banner, its title says six
`pri=LATER size=S deps=- opened=2026-08-21b`

`HS-5`'s title and body both said **six** always-living docs declare no liveness state; the measured count is **eleven**: `formal/CORRESPONDENCE.md`, `formal/ARCHITECTURE.md`, `formal/FINAL_REVIEW.md`, `formal/SEMANTICS.md`, `formal/HANDOFF.md`, `docs/gate-runbook.md`, `docs/specs/wildcard-materialization-spec.md`, `docs/specs/set-engine-spec.md`, `docs/specs/graph-boolean-ivm-spec.md`, `benchmarks/results/BASELINE_2026-07-13.md`, `benchmarks/results/R6_PROFILE_2026-08-17.md`. That is `ZT-P3-5` recurring inside a board row filed the day before it was measured; this row is a child of `HS-5` because it is a scope correction, not separate work. Deliverable (2) is DONE 2026-08-24: HS-5 was retitled to 'always-living docs lack the liveness state docs/README.md sec 2 requires; count lives in TK49' and the HANDOFF.md row edited to match, with the count deliberately NOT copied into the new title. Deliverable (1) is STILL OPEN and is the actual work: adjudicate which of the eleven are deliberately exempt from `docs/README.md` §2, and record the exemption in `docs/README.md` §2 (a task Log is a fact with no home). Traps: do not just change six to eleven — the measured figure was six-then-nine-then-eleven across three readings on three days, so a title carrying a count will rot again; and the sweeps' "at least nine" and this "eleven" are DIFFERENT measurements (the sweeps named docs they happened to open; this is a direct read of the first 8 lines of a candidate list), so re-measure and say which method you used — the count is method-sensitive, exactly like the `NodeV4` 217/49 split. Measurement source: `.scratch/tasktool/COVERAGE.md` §C5; re-measured independently 2026-08-21b: 11/11 still banner-less. Trial finding: 9 of 9 BOARD-arm agents answered 'six' from the board row, and the tree agents answered 'six' too, because they stopped at HS-5's title rather than following 'children: TK49' one hop.

### TK50 — the _bumped residue-version channel is a second dirty-key source with no Lean model
`pri=HOLD size=M deps=- opened=2026-08-21b`

`formal/CORRESPONDENCE.md:436-452` records a second dirty-key source with no Lean model: `self._bumped`, appended by `index_v4/processor.py::DeltaProcessor._store_residue` on every residue write and drained into a fan-out (drained in `::DeltaProcessor._run_cascade`). The consequence is stated plainly at `:352` and in §7: **T5 (`runCascade2_no_abort` / `cascade2_drains`) is a claim about a STRICTLY WEAKER abort condition than the one Python ships**, because Python's abort fires iff the outbox is empty AND the pending `_bumped` fan-out is empty — so T5 does NOT entail *"Python's abort is dead code"*, which is how it is easy to read it. This is not a crash or a correctness bug (a version bump with no outbox row would abort a real transaction in a case the theorem does not cover, i.e. it fails loudly and in the safe direction); it is a live gap in what a landed theorem actually proves, and it had no id. Filed against COVERAGE.md's drop: the consolidated list dropped it as frozen-provenance with no living carrier, but the carrier is `formal/CORRESPONDENCE.md`, the gate-anchored model↔code map, so the drop is recorded here as visible and reversible. Traps: the risk is a citation, not a crash — the failure mode is someone quoting T5 as "the abort is dead code" in a review or doc, so if this is closed by ACCEPTING the gap the acceptance must land next to T5's statement, not only in a task Log; and Appendix lead A10 changes the fan-out this channel feeds (making residue bumps carry granularity instead of always escalating to a full reconcile) and already owes a Lean update, so sequence them or the model gets rewritten twice. Theorem: `formal/lean/ZanzibarProofs/GraphIndex/CascadeStrata.lean::runCascade2_no_abort`. Read [`formal/HANDOFF.md`](formal/HANDOFF.md) first for any formal item (`migrate.py::check_formal_pointer` enforces it for generated rows; 18/18 formal tasks satisfy it). Provenance: inventory-formal.md MISS #4, citing formal/CORRESPONDENCE.md §7.1 ("★ NEW -- the `_bumped` residue-version channel is a SECOND dirty-key source with no model", `ZT-P4-3a`); anchors `:352` and `:436-452` resolve in the current tree.

### TK51 — Fail-open detection sensitivity gap in generator-coverage design (dense-regime pass unbuilt)
`pri=LATER size=M deps=- opened=2026-08-21c`

The generator-coverage subset-driving discipline is tuned for **fail-closed** detection and detuned for **fail-open**: over-grants hide when the store is nearly empty and only appear as tuples are added, which is the opposite of how the sabotage-4 masking argument (the discipline's actual design target) is framed. The design's own author names the fix and prices it: "A complete design would drive both a sparse and a dense regime per config; at 125 ms/run that doubles C4 to ~60 s, which is still inside budget and is my recommendation if the fail-open case is a priority." (`docs/design/generator-coverage/README.md:586-591`, FROZEN 2026-08-16, §6 point 7). Fail-open is this repo's declared house failure mode (`docs/sabotage-procedure.md`, "An assurance step that fails by PASSING"), so an assurance instrument specifically weaker against that mode is exactly the class of gap the sabotage procedure exists to surface — and the recommendation is concrete, bounded (~60s, inside budget per the author's own numbers), and targets the exact bug class this project treats as its worst case. Traps: do not conflate with `TK44` — TK44 is "some cells are never driven" (~30% of pair cells UNREACHED at any regime), this is "cells that are driven don't catch fail-open as reliably as fail-closed", and fixing TK44 does not fix this; and no living carrier exists for this specific recommendation (`docs/sabotage-procedure.md` names fail-open as the house failure mode in general but does not cite this design's dense/sparse proposal), so re-check before assuming it has since been folded into a living doc. The gate this would extend is `tests/test_generator_coverage.py`. Provenance: COVERAGE.md 'Dropped, with reasons' (PART 1, J-26; sweep-j only), which itself flags "J-26 is the one worth a second look before it stays dropped"; grep over the corpus/filing-log.md/notes.md/make_filing_plan.py found neither a task nor a recorded decline, and it was adjudicated 2026-08-21c as filed rather than declined. 2026-08-24b related-edge sweep (trial finding F1) added `related: [TK44]`, because closing TK44 could over-claim fail-open coverage.
