2026-08-30c — all ten gate phases green on this tree; ledger `2026-08-30c`.

🧭 **`P3`'s middle was splittable too.** `UntaintedShadow` is now an `abbrev` for
`ShadowOver (P : NodeKey → Prop)`, generic in its EXTRAS predicate, so the widening is a
**one-line re-instantiation, not a re-proof of the cascade chain** (green, 1089 jobs, 3
in-cone cycles of 10; the 65 `CascadeStrataSettle` sites never moved). Left: repoint the
abbrev at `DerNode` OR `LeafNode`, discharge ~20 tier-1 sites in 3 files.

⚠ **Sizing settled — 21 modules recompile, not 42** (42 was a census rooted at the wrong
module). A site count means nothing without its symbol list and counting unit; that, not
the tree, is what four sessions of censuses disagreed about. Traps moved to scope doc
§11.13, superseding §11.10; `Scratch4cii.lean:51` shadows the real `leafNodeB` — delete it
first. Trial runs to 2026-09-06, `tasks/` is NOT being deleted; every board edit still gets
its mirrored `task.py` op, same `--session`. `TK53`: 15 appends remain.
